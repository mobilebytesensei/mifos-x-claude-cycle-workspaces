# Workspace-level secrets — NEVER commit. Live secrets materialize into
# the source submodule (source/kmp-project-template/secrets/) via /secrets pull.
# A historical mistake committed kmp-project-template/secrets/ here at SHA 2cd190b
# (Jun 17, 2026) exposing live Match SSH key + Android upload keystore + tester PII.
# That commit was scrubbed via git-filter-repo on 2026-06-19; this pattern
# prevents recurrence.
kmp-project-template/secrets/

# macOS noise
.DS_Store

# Personal scratch files
.notes/
