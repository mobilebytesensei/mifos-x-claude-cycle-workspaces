# ============================================================
# CAPABILITY_STATE.yaml — Project-Side Idea Capability Tracker
# ============================================================
# Generated by: /idea init (Phase 1 backfill) and updated by each
#               /idea {capability} runtime on success (Phase 3+).
# Read by:      /idea dashboard (Layer Sanity row + Capabilities grid)
#               PROJECT_MIGRATION (drift detection)
# Spec:         core/registries/IDEA_CAPABILITY_REGISTRY.yaml (framework-side)
#
# DO NOT hand-edit unless you know what you're doing — auto-maintained.
# To force re-init: delete the file and run /idea init.
# ============================================================

schema_version: "1.0.0"
project: "mifos-x/kmp-project-template"
generated: "2026-07-25"
generator: "/idea init (Phase 1 backfill)"
framework_version_at_init: "2.86.0"
init_mode: "backfill"

# ============================================================
# Capabilities used (rows added by detection)
# ============================================================
# NOTE: screens use the v4.0 sibling layout — screens/{feature}/{ui,api,flow,tests,docs}.yaml
#       (15 feature dirs) rather than flat screens/*.yaml. `enrich` detection applied
#       in-spirit against the sibling ui.yaml presence.

capabilities_used:
  import:
    last_run: "inferred-2026-07-25"
    framework_version_at_run: "2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "reverse-engineered idea-layer produced via /idea import — 15 feature sibling-sets + IDEA/FEATURES/REQUIREMENTS/ROADMAP"
    artifact_count: 15
    drift: false
  enrich:
    last_run: "2026-07-25"
    framework_version_at_run: "2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: false
    detection_rule: "/idea sync STEP 3 enrichment — quality_score normalized, initial_state explicit, i18n keys extracted, flow-groups + entry points wired (15 features)"
    artifact_count: 15
  verify:
    last_run: "2026-07-25"
    framework_version_at_run: "2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: false
    detection_rule: "/idea verify coherence pass — verdict PASS-WITH-GAPS (100% action_contract coverage, clean DTO/nav-param resolution; gaps = enrich + P1 implement targets)"
  sync:
    last_run: "2026-07-25"
    framework_version_at_run: "2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: false
    detection_rule: "/idea sync — verify → enrich(15) → flows/ index (16 flow-groups + ROUTE_SCREEN_INDEX) → artifact regen (APP_FLOW.mmd + DEV_STATUS overwritten)"
    artifact_count: 15

# ============================================================
# Init metadata (audit trail)
# ============================================================

init:
  ran_at: "2026-07-25"
  detected_count: 4
  not_detected_count: 0
  skipped_due_to_error: []
  notes: >-
    /idea import reverse-engineered all 15 demo feature modules from source (96 sibling YAMLs +
    IDEA/FEATURES/REQUIREMENTS/ROADMAP + infra requirements), then /idea sync enriched + verified +
    generated flows/ index and regenerated nav artifacts. Coherence verdict PASS-WITH-GAPS.
    NOT yet run: export / approve / plan / analytics / train / mockups (Stitch not configured).
    Next: /idea-agent --auto to drive the P1 gap list (crypto-detail UI, watchlist UI, alerts UI +
    persistence-bug fix, bills delete/toggle) — needs a connected device for the behavior-executed gate.

# ============================================================
# Phase 3+ fields (placeholder)
# ============================================================

drift_check:
  last_run: null
  drifted_capabilities: []

per_capability_writes_enabled: false
