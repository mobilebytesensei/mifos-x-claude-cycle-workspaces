appId: org.mifos.backoffice
---
# Biometric Setup — happy path: open from M17 settings biometric toggle,
# tap Enroll, complete enrollment, verify success state and done.
- launchApp
# Navigate to M17 settings → biometric toggle
- tapOn:
    id: "nav-shell-assembler_root_m17-sync-settings"
- assertVisible:
    id: "m17-sync-settings_biometric"
- tapOn:
    id: "m17-sync-settings_biometric"
# biometric-setup should open
- assertVisible:
    id: "biometric-setup_header"
- assertVisible:
    id: "biometric-setup_illustration"
- assertVisible:
    id: "biometric-setup_enroll"
- assertVisible:
    id: "biometric-setup_skip"
# Tap Enroll
- tapOn:
    id: "biometric-setup_enroll"
# Platform biometric dialog appears (enrolling state)
- assertVisible:
    id: "biometric-setup_header"
# After enrollment success → success state
- assertVisible:
    id: "biometric-setup_done"
- tapOn:
    id: "biometric-setup_done"
# Back in M17 settings
- assertVisible:
    id: "m17-sync-settings_biometric"
