<!-- source: screens/collection-sheet/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=659dd90d5f6d docs=87d71d956369 flow=788f6b06a7c5 api=ea8bef57a248 -->
<!-- generated: 2026-07-31T03:19:28Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Collection Sheet (generate route · capture · save batch) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/collection-sheet/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: designed

---

## Capabilities

- **has_ui** — Compose list screen: a grouped center→group→client route with per-row editable actual-amount capture, attendance toggles and running totals.
- **has_offline_cache** — the generated sheet reads CACHE_FIRST_SWR through Store5; a whole route is capturable with no signal.
- **has_flow** — entered from the M01 'today's route' tile / M06 default surface; loading → content/empty/error with an in-content capture loop and a batch Save.
- **requires_auth** — generate + save run under the authenticated Fineract session, gated by READ_COLLECTIONSHEET / SAVECOLLECTIONSHEET.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| collection-sheet | CollectionSheetViewModel | loading, content, empty, error | The field-officer's daily collection sheet entered from the M01 dashboard 'today's route' tile. Generates the staffId-scoped sheet (POST /v1/collectionsheet?command=generateCollectionSheet for office + staffId + date) into a grouped route — centers → groups → clients — each row showing loan installment dues and savings deposit dues. The officer captures the ACTUAL amount per row inline (defaulting to the due), optionally records attendance, then saves the whole route (?command=saveCollectionSheet) as ONE transactional batch enqueued to the offline outbox with a durable idempotency key, so an entire route captured with no signal replays exactly-once. Dues read cache-first through Store5 — fully usable offline. Archetype: list. |

## State Model

### CollectionSheetViewModel

- **State**: sheet (CollectionSheet? — centers→groups→clients with per-loan installment dues + per-savings deposit dues), captured (Map<String, Money> — actual amounts per loan/savings row, default = due), attendance (Map<Long, AttendanceType> — per-client present/absent/late on attendance-enabled sheets), office (OfficeStaffDate — office, staffId, meeting/transaction date), uiState (CollectionSheetScreenState)
- **ScreenState**: loading, content, empty, error
- **Errors**: generate_failed (retry=true; error state — sheet failed to generate with no cache), validation_failed (a captured amount fails a row bound; blocks Save until corrected), save_failed (retry=true; error state — Save failed hard, captured amounts preserved from local state for re-attempt), permission_denied (403 — Save hidden, capture-only; permission drift refresh)
- **Events**: (none — the sheet resolves in-place; a FAILED save replay surfaces in needs-attention-inbox for triage)
- **Actions**: OnGenerate (generate the staffId-scoped sheet / read cache-first), OnAmountChange (amount_change → in-VM capture, revises per-group + sheet totals, no network), OnToggleAttendance (toggle_attendance → in-VM attendance set), OnSaveSheet (save_sheet → POST …?command=saveCollectionSheet as one batch enqueued to draft_outbox), OnRetry (retry → re-generate / re-attempt save with captured amounts preserved)
- **DI**: core/store, core/data, core/database, core/permissions, core/network

## API Endpoints (2)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| generate_collection_sheet | POST | command(String), officeId(Long), staffId(Long), transactionDate(String) | CollectionSheetDto | 401, 403 | cached_collection_sheets |
| save_collection_sheet | POST | command(String), body(captured amounts + attendance) | CommandProcessingResultDto | 401, 403, 409 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_COLLECTIONSHEET (generate) + SAVECOLLECTIONSHEET (Save visibility, capture-only otherwise) |
| offline-sync-engine | uses | true | durable outbox + idempotency key + POST /v1/batches (enclosingTransaction) for exactly-once whole-route save |
| fineract-auth-session | requires | true | authenticated session required to generate + save the sheet |

## Navigation

- **Route**: collection-sheet(officeId, staffId, date)
- **From**: m01-dashboard (today's-route tile), m06-collections (daily sheet surface), needs-attention-inbox (overdue-collections drill-in)
- **To**: (none — capture + save resolve in-place)

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Sheet generates | the staffId-scoped sheet generates (or reads cache-first) | content | (see empty/error) | POST …?command=generateCollectionSheet, CACHE_FIRST_SWR |
| Nothing due | no dues for the chosen date | empty ('nothing due today') | content | no meetings / all paid |
| Generate hard-fail | sheet failed AND no cache available | error (message + Retry) | content | generate failure with empty cache |
| Capture | an amount is captured / attendance toggled | content — running totals revised in-VM | content | pure transform_state, no network |
| Save | Save tapped (SAVECOLLECTIONSHEET held) | content — whole route enqueued to draft_outbox as one batch | error (hard fail) | POST …?command=saveCollectionSheet via POST /v1/batches, all-or-nothing |
| Retry | 'Retry' tapped in error | loading → re-generate / re-attempt save | error | captured amounts preserved from local state |

## Testing (7 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_route_shimmer | The loading state shows a grouped route shimmer with no error | medium |
| content_shows_grouped_dues_and_capture | Rows group center→group→client with loan+savings dues, an editable actual-amount field, and running per-group/sheet totals | high |
| empty_when_nothing_due | A 'nothing due today' panel when there are no dues for the date | low |
| error_when_generate_or_save_fails | The error state is shown and Retry re-generates / re-attempts save with captured amounts preserved | medium |
| capturable_offline | The route renders from cache and captured amounts persist in local state with no signal | high |
| save_commits_whole_route_as_one_batch | Save enqueues the whole route as ONE command with a durable key; sync replays via POST /v1/batches (all-or-nothing); no double-post | high |
| save_gated_by_permission | No Save action is shown (capture-only) when the user lacks SAVECOLLECTIONSHEET | medium |
