<!-- source: screens/client-list/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=b8e94faa19cb docs=c4a9368f50a1 flow=bf5c4fde13a5 api=cd74a181bebe -->
<!-- generated: 2026-07-31T03:19:28Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Client Roster (search · filter · paged list) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/client-list/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: designed

---

## Capabilities

- **has_ui** — Compose list screen: search box + status/office filter chips over a paged client roster with a permission-gated New-client FAB.
- **has_offline_cache** — the roster reads CACHE_FIRST_SWR through a Store5 paging source and browses fully offline (atomic pages, no pagination race).
- **has_flow** — entered from the M01 dashboard / M02 default surface; loading → content/empty/error, rows drill into client-detail-360, FAB into the create form.
- **requires_auth** — the roster + search reads run under the authenticated Fineract session, gated by READ_CLIENT.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| client-list | ClientListViewModel | loading, content, empty, error | The searchable, office-scoped client roster entered from the M01 dashboard 'active clients' tile / 'new client' quick action. Loads GET /v1/clients (offset/limit paged) into rows (display name, account no, status chip, office) under a search box (escalating to GET /v1/search?resource=clients) and status/office filter chips. Each row drills into client-detail-360. A permission-gated New-client FAB (CREATE_CLIENT) opens the dynamic create form. Reads cache-first through a Store5 paging source, so the roster is fully browsable offline and offline-created clients appear immediately. Archetype: list. |

## State Model

### ClientListViewModel

- **State**: clients (List<ClientSummary> — display name, account no, status, office, assigned staff), query (String — search text over name/account-no/external-id), statusFilter (ClientStatus? — null = All), canCreate (Boolean — PermissionSet holds CREATE_CLIENT; drives FAB), uiState (ClientListScreenState)
- **ScreenState**: loading, content, empty, error
- **Errors**: clients_load_failed (retry=true; error state — first page failed with no usable cache, Retry offered), search_failed (surfaced inline; local filter still applies), page_load_failed (next-page failure surfaced at the list footer; prior pages retained)
- **Events**: NavigateToClientDetail(clientId) → client-detail-360 (row tapped), NavigateToNewClientForm → dynamic-template-forms (FAB / empty-state affordance, CREATE_CLIENT held)
- **Actions**: OnLoad (read first page cache-first from Store5), OnSearch (search_clients → debounced in-VM filter, escalates to GET /v1/search when local pages exhausted), OnFilterStatus (filter_status → in-VM status narrow, no refetch), OnOpenClient (open_client → navigate client-detail-360), OnNewClient (new_client → navigate dynamic-template-forms create form), OnLoadNextPage (paged next-page load on scroll), OnRetry (retry → re-read first page)
- **DI**: core/store, core/data, core/database, core/permissions, core/network

## API Endpoints (2)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_clients | GET | officeId(Integer)?, status(String)?, offset(Integer)?, limit(Integer)?, orderBy(String)? | ClientSummaryDto[] (paged) | 401, 403 | cached_clients |
| search_clients | GET | query(String), resource(String) | SearchResultDto[] | 401, 403 | cached_search |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_CLIENT (list) + CREATE_CLIENT (New-client FAB visibility) |
| offline-sync-engine | uses | true | Store5 paging source + cache-first reads so the roster browses offline; offline-created clients surface immediately |
| fineract-auth-session | requires | true | authenticated session required for the roster + search reads |
| dynamic-template-forms | navigate | true | the New-client FAB opens the dynamic client-create form (GET /v1/clients/template → POST via outbox) |

## Navigation

- **Route**: client-list()
- **From**: m01-dashboard (active-clients tile, new-client quick action), m02-clients (default list surface)
- **To**: client-detail-360 (row), dynamic-template-forms (New-client FAB / empty-state affordance)

## Flow Logic (7 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| First page resolves | the first page of the office-scoped roster resolves (cache-first) | content | (see empty/error) | Store5 paging source CACHE_FIRST_SWR over GET /v1/clients |
| Empty roster | the roster (or active filter) returns zero clients | empty (panel + New-client affordance if permitted) | content | zero rows |
| First-page hard-fail | first page failed AND no cache available | error (message + Retry) | content | GET failure with empty cache |
| Search escalation | search term matches no loaded row | GET /v1/search?resource=clients | in-VM filter over loaded pages | debounced; escalate only when local pages exhausted |
| Row drill | a client row is tapped | navigate client-detail-360 | content | — |
| New client | New-client FAB tapped (CREATE_CLIENT held) | navigate dynamic-template-forms | FAB hidden | permission-gated create form |
| Retry | 'Retry' tapped in error | loading → re-read first page | error | re-read GET /v1/clients page 1 |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_list_shimmer | The loading state shows a list shimmer with no error | medium |
| content_shows_paged_rows | Rows show name/account-no/status/office; search + filter chips present; scrolling loads the next page | high |
| empty_when_no_clients_or_filter_clears_all | The empty panel shows with a New-client affordance when CREATE_CLIENT is held | low |
| error_when_first_page_fails_no_cache | The error state is shown and Retry re-reads the first page | medium |
| roster_browsable_offline | Cached pages render from the Store5 paging source offline; pagination is atomic (no race) | high |
| search_escalates_to_global_when_pages_exhausted | The in-VM filter runs first; GET /v1/search issues only when the local page set is exhausted | medium |
| row_drills_into_360 | Tapping a client row navigates to client-detail-360 | high |
| new_client_gated_and_opens_form | The New-client FAB shows only when CREATE_CLIENT is held and navigates to the dynamic create form | medium |
