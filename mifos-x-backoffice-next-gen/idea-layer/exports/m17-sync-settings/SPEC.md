<!-- source: screens/m17-sync-settings/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=102149388ae1 docs=2f923fa718de flow=c387391b817f api=b7972fe98474 -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M17 · Sync & Settings -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m17-sync-settings/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose settings-console screen (sync banner + preference rows).
- **has_api** — Fineract instance-mode / batch-drain / delta-audit / reference-refresh endpoints (consumed as-is; no server layer).
- **has_offline_cache** — CACHE_FIRST_SWR reference stores + read-only outbox derivation; preferences render offline.
- **has_local_preferences** — theme / language / biometric-lock persisted via multiplatform-settings (biometric flag encrypted).
- **has_system_integration** — DynamicBaseUrlPlugin tenant/server swap + encrypted key store + biometric app-lock.
- **has_flow** — hydrate prefs + derive sync status → sync/pull delegate → re-auth on server switch.
- **requires_auth** — every operation authenticated; settings visible to every authenticated persona (no server permission gate).

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| m17-sync-settings | SyncSettingsViewModel | idle, content, syncing, empty, needs-attention, error | App-level control panel: manual sync console (Sync now / Pull latest delegating to offline-sync-engine) with a badged Needs-Attention entry, tenant/server switch (dev/sandbox/prod via DynamicBaseUrlPlugin with forced re-auth), reference-cache clear/refresh, local preferences (theme, language, biometric app-lock via multiplatform-settings), and About/diagnostics + logout. Archetype: settings_console. |

## State Model

### SyncSettingsViewModel

- **State**: pendingCount (Int), failedCount (Int), syncedCount (Int), totalCount (Int), lastSyncedAt (Instant), activeTenant (String), activeServerEnv (ServerEnvironment), themeMode (ThemeMode), locale (String), biometricLockEnabled (Boolean), referenceDataFreshAt (Instant)
- **ScreenState**: idle, content, syncing, empty, needs-attention, error
- **Errors**: settings.read_failed (retry=true; cached prefs still render), sync.rejected (route rejected replays → needs-attention-inbox with failed-count badge), network.offline (sync CTAs disabled, prefs stay editable), server_switch.reauth_required (route to permission-capability-engine login after base-URL swap), cache.clear_confirm (confirm dialog before destructive clear)
- **Events**: NavigateToNeedsAttention → needs-attention-inbox, NavigateToAboutDiagnostics → m17-sync-settings (About panel), NavigateToLogin → login-tenant
- **Actions**: OnSyncNowClick (trigger_sync → offline-sync-engine drain POST /v1/batches → syncing), OnPullLatestClick (trigger_delta_pull → changeListSync GET /v1/audits → syncing), OnNeedsAttentionClick (open_needs_attention → navigate), OnSwitchServerClick (switch_tenant_server → DynamicBaseUrlPlugin swap + force re-auth), OnClearCacheClick (clear_reference_cache → confirm → clear Room reference tables), OnRefreshReferenceClick (refresh_reference_data → CACHE_FIRST_SWR re-fetch → syncing), OnThemeToggle (toggle_theme → persist theme_mode), OnLanguageChange (change_language → persist locale), OnBiometricToggle (toggle_biometric_lock → persist encrypted flag), OnAboutClick (open_about_diagnostics → navigate), OnLogoutClick (logout → clear session + drafts + permission cache → login-tenant), Internal.ReceiveSyncStatus (fold outbox observe() + last-sync watermark into banner), Internal.ReceiveSettings (load persisted prefs on mount)
- **DI**: core/datastore, core/store, core/data, core-base/network

## API Endpoints (5)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| get_instance_mode | GET | (none) | InstanceMode | 401, 403 | reference_cache |
| sync_batches | POST | enclosingTransaction(Boolean), BatchRequest([]) | BatchResponse[] | 401, 500 | draft_outbox |
| delta_changelist | GET | officeId(Long), makerDateTimeFrom(String) | AuditEntry[] | 401, 403 | fetched_at |
| refresh_reference_codes | GET | (none) | CodeValue[] | 401, 403 | reference_cache |
| refresh_reference_offices | GET | (none) | Office[] | 401, 403 | reference_cache |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| offline-sync-engine | uses | true | owns the durable outbox drain (POST /v1/batches, AllOrNothing + durable idempotency key) + changeListSync delta-pull; the console delegates, never owns replay logic |
| needs-attention-inbox | navigate | true | triage (Retry / Edit / Discard) of FAILED / server-rejected replays; the badged Needs-Attention entry routes here |
| permission-capability-engine | gates | true | re-resolves the permission set after a tenant/server switch forces a re-auth |

## Navigation

- **Route**: m17-sync-settings
- **From**: nav_root (Sync & Settings module root — always available once authenticated, no server permission gate)
- **To**: needs-attention-inbox, login-tenant, m17-sync-settings (About/diagnostics panel)

## App-Shell

- **Bottom navigation**: visible (Sync & Settings is a settings tab, reachable to every authenticated persona).
- **Top app bar**: visible (settings header + sync-status context).
- **FAB**: none.
- **Snackbar**: enabled (queued-offline / cache-cleared / permission-drift / retry toasts).
- **Safe-area**: standard top-app-bar inset; preference list scrolls under the bar.

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Outbox depth | outbox has N pending commands on mount | content (N pending + Sync-now CTA) | idle (Up to date · 0 pending) | fold outbox observe() → ScreenState |
| First-run vs rest | fresh install / just logged in, no outbox + no delta ever pulled | empty (offer first full Pull latest) | idle/content | derive from watermark absence |
| Sync outcome | Sync now / batch drain completes | syncing → idle (zero rejects) | syncing → needs-attention (rejected replays) | offline-sync-engine POST /v1/batches result |
| Sync failure | batch or delta-pull request fails (network/server) | syncing → error (banner + Retry) | syncing → idle | error map; cached prefs still render |
| Server switch | user picks a different tenant/server env | swap DynamicBaseUrlPlugin URL + tenant header → force re-auth → permissions re-resolved | idle (no change) | core-base/network DynamicBaseUrlPlugin |
| Destructive clear | user taps Clear cached reference data | confirm dialog → empty Room reference_cache + reset watermark | cancel → no-op | Room 3 delete + freshness invalidation |

## Testing (12 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| settings_hydrate_on_mount | Saved preferences (dark theme, en locale, biometric on) render on mount with the last-synced watermark | high |
| sync_console_drains_outbox | Tapping Sync now drains a 3-command outbox via offline-sync-engine POST /v1/batches; banner returns to Up to date | high |
| pull_latest_delta | Pull latest runs changeListSync over GET /v1/audits, advances the freshness watermark, leaves the outbox untouched | medium |
| needs_attention_entry_visible | A server-rejected replay badges the Needs-Attention entry and routes to needs-attention-inbox | high |
| tenant_server_switch_reauths | Switching sandbox→prod changes the base URL + tenant header, forces re-auth, and re-resolves permissions | high |
| clear_reference_cache_confirm | Clear cached reference data shows a confirm dialog before emptying reference_cache and resetting the watermark | medium |
| refresh_reference_data | Refresh reference data re-runs the CACHE_FIRST_SWR stores and advances the freshness watermark | low |
| theme_toggle_persists | Toggling dark theme persists theme_mode, recomposes immediately, and survives relaunch | medium |
| language_change_persists | Selecting a locale persists it and re-applies via Compose Resources so screens re-render in the chosen language | low |
| biometric_lock_persists | Enabling biometric app-lock writes the encrypted flag and enforces the lock on the next cold launch | medium |
| logout_clears_session | Log out (after confirm) clears the encrypted session + permission cache, preserves queued drafts, and navigates to login-tenant | medium |
| settings_read_error_shows_retry | A settings-store read failure shows the error state (never a blank screen) with a Retry that re-reads settings | medium |
