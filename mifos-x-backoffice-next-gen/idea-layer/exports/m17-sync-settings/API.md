<!-- source: screens/m17-sync-settings/api.yaml -->
<!-- source_hash: api=b7972fe98474 -->
<!-- generated: 2026-07-21T18:23:01Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# M17 · Sync & Settings -- API Reference

> **Source of Truth**: `idea-layer/screens/m17-sync-settings/api.yaml` -> `endpoints[]`
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0

## Endpoints (5)

| Function | Method | Table | Auth | Params | Response | Cache |
|----------|--------|-------|------|--------|----------|-------|
| get_instance_mode | GET | reference_cache | Yes | (none) | InstanceMode: mode (read/write/batch) | CACHE_FIRST_SWR |
| sync_batches | POST | draft_outbox | Yes | enclosingTransaction(Boolean), BatchRequest([]) | BatchResponse[]: statusCode, body per step | durable-outbox |
| delta_changelist | GET | fetched_at | Yes | officeId(Long), makerDateTimeFrom(String) | AuditEntry[]: changed resources since watermark | no-cache |
| refresh_reference_codes | GET | reference_cache | Yes | (none) | CodeValue[]: code values | CACHE_FIRST_SWR |
| refresh_reference_offices | GET | reference_cache | Yes | (none) | Office[]: office hierarchy | CACHE_FIRST_SWR |

## Error Handling

All endpoints follow standard error mapping:
- 401 -> Auth error (redirect to signin)
- 403 -> NoAuthorization -> permission-capability-engine drift refresh + re-resolve (not a crash)
- 404 -> NotFound error
- 422 -> Validation error
- 500 -> Server error (retryable; error state + Retry)

Notes:
- Sync & Settings is mostly app-local (device/session prefs via multiplatform-settings); it builds no server layer (`has_server: false`) and consumes these Fineract endpoints as-is.
- `sync_batches` is NOT owned here — the console delegates the outbox drain to offline-sync-engine, which replays each PENDING command as one all-or-nothing batch (`enclosingTransaction=true`) with a durable Idempotency-Key per entry (exactly-once, zero double-post). Rejected replays route to needs-attention-inbox.
- `delta_changelist` powers "Pull latest" via changeListSync — scoped by `officeId` + a `makerDateTimeFrom` watermark; it refreshes CACHE_FIRST_SWR sources without touching the write outbox.
- `get_instance_mode` gates whether the console can drain writes (surfaced in About/diagnostics); `refresh_reference_*` re-hydrate the shared reference caches used by scope pickers app-wide.

## Full Contracts

See `idea-layer/screens/m17-sync-settings/api.yaml` -> `endpoints[]` for complete request/response schemas.
