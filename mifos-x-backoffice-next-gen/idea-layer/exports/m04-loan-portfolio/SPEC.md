<!-- source: screens/m04-loan-portfolio/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=db62056b8145 docs=b0eff5884539 flow=5dd704ab5553 api=8a1b6a1601af -->
<!-- generated: 2026-07-21T18:17:24Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M04 · Loan Portfolio (origination · servicing · delinquency · reschedule) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m04-loan-portfolio/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0 | **Status**: designed

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| M04 · Loan Portfolio (`m04-loan-portfolio`, archetype `master_detail`) | LoanPortfolioViewModel | loading, content, empty, action-disabled, schedule-preview, submitting, error | Loan list → 360° detail (summary, repayment schedule, transactions, charges, guarantors, collateral, documents) → application via GET /v1/loans/template with a LIVE calculateLoanSchedule preview → lifecycle commands (approve/reject/withdraw/disburse/undo) → servicing (repayment/prepay/waive/writeoff/foreclosure, charge pay+waive) → reschedule. Reads CACHE_FIRST_SWR (offline); every monetary write funnels through the offline outbox with a DURABLE idempotency key (exactly-once); every command gated by its exact {ACTION}_LOAN permission; maker-checker aware. |

## Capabilities

### loan_list_read
`Store<PageKey,List<Loan>>` over Room (`LoanAccountEntity`) + `asPagingScreenStream(CACHE_FIRST_SWR)`; offset/limit ≙ Fineract paging; GET /v1/loans scoped by office/staffId; renders all 7 ScreenState variants free via `PagingScreenContent`. Permission `READ_LOAN`. Errors: `loan_list_failed`, `loan_list_offline_no_cache`.

### loan_detail_read
GET /v1/loans/{id}?associations=all + GET /v1/loans/{id}/charges → 360° detail (schedule/transactions/charges/guarantors/collateral/documents) stored in `payload_json`; offline via `CACHE_FIRST_SWR` SourceOfTruth over Room. Permission `READ_LOAN`. Errors: `loan_detail_failed`.

### schedule_preview
POST /v1/loans?command=calculateLoanSchedule — live repayment schedule preview from entered principal/term/rate/disbursementDate + template defaults; no loan created; debounced; SWR-cached template for offline. Permission `READ_LOAN`. Errors: `schedule_calc_failed`.

### loan_lifecycle_command
POST /v1/loans/{id}?command=approve|reject|withdrawnByApplicant|disburse|undoapproval|undodisbursal — queued through `RoomSubmitOutbox` with a durable idempotency key; maker-checker aware (queued commandId + empty changes → PENDING_APPROVAL); exactly-once replay over POST /v1/batches. Permission `APPROVE_LOAN`. Errors: `command_rejected`, `command_pending_approval`, `permission_drift_403`, `backdate_forbidden`.

### loan_servicing_transaction
POST /v1/loans/{id}/transactions?command=repayment|waiveinterest|writeoff|foreclosure — `DraftSubmitHandler` + `RoomSubmitOutbox`, DURABLE idempotency_key generated at draft-save + reused on retry → EXACTLY ONCE across flap/crash/reinstall (zero double-post); backdated needs `*INPAST_LOAN`. Permission `REPAYMENT_LOAN`. Errors: `transaction_rejected`, `transaction_backdate_forbidden`, `transaction_offline_queued`.

### loan_charge_command
POST /v1/loans/{id}/charges (add) + POST /v1/loans/{id}/charges/{chargeId}?command=pay|waive — queued, durable-idempotent; form from GET /v1/loans/{id}/charges/template via dynamic-template-forms. Permission `PAY_LOANCHARGE`. Errors: `charge_command_rejected`.

### loan_reschedule
POST /v1/rescheduleloans — reschedule REQUEST from GET /v1/rescheduleloans/template; approved separately (`APPROVE_RESCHEDULELOAN` via checker inbox); queued, durable-idempotent, maker-checker aware. Permission `CREATE_RESCHEDULELOAN`. Errors: `reschedule_rejected`.

### loan_application_submit
POST /v1/loans — dynamic-template-forms `JsonObject` payload + client-generated externalId (address before server id); queued through outbox; maker-checker aware. Permission `CREATE_LOAN`. Errors: `application_rejected`, `application_offline_queued`.

## State Model

### LoanPortfolioViewModel

- **State**: `uiState: LoanPortfolioUiState`, `loans: List<Loan>` (in-memory CACHE_FIRST_SWR working set), `searchQuery: String`, `selectedTab: LoanDetailTab` (schedule | transactions | charges | guarantors | collateral | documents), `schedulePreview: LoanScheduleDto?`, `loanStatus: LoanLifecycleStatus`, `loanBalance: Money`
- **ScreenState**: Loading (`loading`), Content (`content`), Empty (`empty`), ActionDisabled (`action-disabled`), SchedulePreview (`schedule-preview`), Submitting (`submitting`), Error (`error`)
- **Errors**: `loan_list_failed`, `loan_list_offline_no_cache`, `loan_detail_failed`, `schedule_calc_failed`, `command_rejected`, `command_pending_approval`, `permission_drift_403`, `backdate_forbidden`, `transaction_rejected`, `transaction_backdate_forbidden`, `transaction_offline_queued`, `charge_command_rejected`, `reschedule_rejected`, `application_rejected`, `application_offline_queued`
- **Events**: `NavigateToLoanDetail(loanId)`, `NavigateToLoanApplicationWizard`, route rejected command → `needs-attention-inbox`, 403 drift → `permission-capability-engine`
- **Actions**: `LoadLoans()` → loansStream(CACHE_FIRST_SWR); `SearchLoans(query)` → in-memory filter; `OpenLoanDetail(loanId)` → NavigateToLoanDetail; `ViewLoanDetailTab(tab)` → selectedTab drill; `RecalculateSchedule(input)` → calculatePreview (debounced); `StartLoanApplication()` → NavigateToLoanApplicationWizard; `ApproveLoan` / `RejectLoan` / `WithdrawLoan` / `DisburseLoan` / `UndoApproval` / `UndoDisbursal` → enqueue({ACTION}_LOAN, idempotencyKey); `RecordRepayment` / `MakePrepayment` → DraftSubmitHandler + RoomSubmitOutbox durable-idempotent (exactly-once); `WaiveInterest` / `WriteOffLoan` / `ForecloseLoan` → enqueue transaction; `RescheduleLoan` → enqueue(CREATE_RESCHEDULELOAN); `AddLoanCharge` / `PayLoanCharge` / `WaiveLoanCharge` → enqueue charge command; `RetryLoad()` → refreshFresh()
- **DI**: `LoanAccountsRepository`, `LoanScheduleRepository`, `LoanOutboxRepository`, `RoomSubmitOutbox`, `cmp-network-monitor`; required modules `core/store`, `core/data`, `core/network`, `core/database`, `core/permissions`

## API Endpoints (13)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_loans | GET | offset(int), limit(int), orderBy(string), sortOrder(string), staffId(long), officeId(long) | LoanAccountDto[] | 401, 403 | loan_accounts |
| get_loan_detail | GET | associations(string), exclude(string) | LoanAccountDto | 401, 403 | loan_accounts |
| get_loan_charges | GET | — | LoanChargeDto[] | 401, 403 | loan_charges |
| get_loan_template | GET | clientId(long), productId(long), templateType(string) | LoanTemplateDto | 401 | cached_templates |
| get_transaction_template | GET | command(string), dateFormat(string), locale(string) | LoanTransactionTemplateDto | 401 | cached_templates |
| get_reschedule_template | GET | — | RescheduleTemplateDto | 401 | cached_templates |
| calculate_schedule | POST | command(string), body(CalculateScheduleRequest) | LoanScheduleDto | 401 | cached_templates |
| submit_loan_application | POST | body(LoanApplicationRequest) | CommandProcessingResultDto | 401, 403, 422, 500 | framework_submit_drafts |
| loan_lifecycle_command | POST | body(LoanCommandRequest) | CommandProcessingResultDto | 401, 403, 422, 500 | framework_submit_drafts |
| loan_transaction | POST | body(LoanTransactionRequest) | CommandProcessingResultDto | 401, 403, 422, 500 | framework_submit_drafts |
| loan_charge_command | POST | body(LoanChargeCommandRequest) | CommandProcessingResultDto | 401, 403, 422, 500 | framework_submit_drafts |
| add_loan_charge | POST | body(AddLoanChargeRequest) | CommandProcessingResultDto | 401, 403, 422, 500 | framework_submit_drafts |
| create_reschedule | POST | body(RescheduleRequest) | CommandProcessingResultDto | 401, 403, 422, 500 | framework_submit_drafts |

## Dependencies (Tier 1 — foundation)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | feature | true | Resolves every {ACTION}_LOAN / *_LOANCHARGE / *_RESCHEDULELOAN + *INPAST_LOAN permission; HIDE for nav roots + create FABs, DISABLE-with-reason for makers; drives 403-drift refresh + re-resolve |
| offline-sync-engine | feature | true | RoomSubmitOutbox + DraftSubmitHandler durable idempotency-key outbox (framework_submit_drafts); exactly-once /v1/batches replay for every monetary write |
| dynamic-template-forms | feature | true | Renders application / disburse / repayment / charge / reschedule surfaces from GET /{entity}/template allowed-value lists; never hardcoded |
| needs-attention-inbox | feature | true | Rejected loan commands/transactions surface here (Retry / Edit / Discard); never silently dropped |

## App-Shell

- **Bottom navigation**: visible (M04 reached as a permission-gated nav root on the loan group).
- **Top app bar**: visible (title `{strings.m04_title}` + loan-detail context).
- **FAB**: present — `new_loan_fab`, HIDE-gated (fail-closed) on `CREATE_LOAN`.
- **Snackbar**: enabled (queued / submitted-for-approval / permission-drift / retry feedback).
- **Safe-area**: standard top-app-bar inset; the loan list / detail scrolls under the bar.

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| List resolve | GET /v1/loans succeeds or cache hit | loading → content (or empty if 0 rows for office/portfolio) | loading → error (no cache) | Store5 CACHE_FIRST_SWR paging stream |
| Detail vs offline | detail fetch succeeds or served from cache | content (360° detail hydrated) | no-network (last cached loan stays usable) | CACHE_FIRST_SWR SourceOfTruth over Room |
| Command connectivity | monetary write queued through the outbox | content → submitting → SUBMITTED (exactly-once /v1/batches replay) | submitting → queued (durable outbox; auto-drains on reconnect) | RoomSubmitOutbox + durable idempotency key |
| Permission gate | user holds the command's {ACTION}_LOAN | control VISIBLE + enabled | create FAB HIDDEN (fail-closed); workflow button DISABLE-with-reason for a maker | permission-capability-engine on_denied HIDE/DISABLE |
| Maker-checker | queued commandId returns with empty changes | "Submitted for approval" badge (PENDING_APPROVAL, not applied) | optimistic content refresh on SUBMITTED | tenant maker-checker gate |
| 403 drift | a command replay returns 403 NoAuthorization | route to permission-capability-engine refresh + re-resolve (prune denied controls) | continue | never blind-retry; the last cached loan stays alive |

## Testing (12 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| repayment_posts_exactly_once_offline | A repayment queued offline and replayed twice posts EXACTLY ONCE via the durable idempotency key | high |
| approve_disabled_for_maker_without_permission | A maker sees Approve DISABLED-with-reason (not hidden) while Repayment stays enabled | high |
| approve_queues_as_pending_approval_under_maker_checker | Under maker-checker, approving queues a PENDING_APPROVAL command rather than applying optimistically | high |
| backdated_repayment_requires_inpast | A backdated repayment is blocked for a user without REPAYMENTINPAST_LOAN | high |
| live_schedule_preview_recomputes | During application the schedule preview recomputes live via calculateLoanSchedule; no loan created | high |
| disburse_only_on_approved_loan | Disburse is only actionable on an Approved loan; stage-gated off on Pending Approval | high |
| rejected_command_routes_to_needs_attention | A server-rejected loan command lands in Needs-Attention with the verbatim error, never dropped | high |
| loading_state_shows_cached_shimmer | The loading state shows a shimmer with the last cached content preserved underneath | medium |
| content_state_lists_loans | The content state lists loans with client, product, principal, status, outstanding, PAR days | medium |
| empty_state_offers_new_loan_cta_when_permitted | The empty state shows the New-loan CTA only if the user holds CREATE_LOAN | medium |
| no_network_repayment_stays_queued | Offline, a repayment stays queued and auto-drains exactly-once on regain | medium |
| error_state_403_drift_refreshes_permissions | A 403 refreshes permissions and re-resolves the surface rather than blind-retrying | low |
