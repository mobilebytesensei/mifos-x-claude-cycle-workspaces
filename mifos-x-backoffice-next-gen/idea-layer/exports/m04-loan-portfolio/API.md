<!-- source: screens/m04-loan-portfolio/api.yaml -->
<!-- source_hash: api=8a1b6a1601af -->
<!-- generated: 2026-07-21T18:17:24Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# M04 · Loan Portfolio (origination · servicing · delinquency · reschedule) -- API Reference

> **Source of Truth**: `idea-layer/screens/m04-loan-portfolio/api.yaml` -> `endpoints[]`
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0

## Endpoints (13)

| Function | Method | Table | Auth | Params | Response | Cache |
|----------|--------|-------|------|--------|----------|-------|
| list_loans | GET | loan_accounts | Yes | offset(int), limit(int), orderBy(string), sortOrder(string), staffId(long), officeId(long) | LoanAccountDto[]: paged {totalFilteredRecords, pageItems} | CACHE_FIRST_SWR |
| get_loan_detail | GET | loan_accounts | Yes | associations(string), exclude(string) | LoanAccountDto: summary, schedule, transactions, charges, guarantors, collateral | CACHE_FIRST_SWR |
| get_loan_charges | GET | loan_charges | Yes | — | LoanChargeDto[]: amount, dueDate, paid, waived | CACHE_FIRST_SWR |
| get_loan_template | GET | cached_templates | Yes | clientId(long), productId(long), templateType(string) | LoanTemplateDto: products, loanOfficers, charges, funds, strategies | CACHE_FIRST_SWR |
| get_transaction_template | GET | cached_templates | Yes | command(string), dateFormat(string), locale(string) | LoanTransactionTemplateDto: amount, paymentTypes, defaults | CACHE_FIRST_SWR |
| get_reschedule_template | GET | cached_templates | Yes | — | RescheduleTemplateDto: reasons, allowedAdjustments | CACHE_FIRST_SWR |
| calculate_schedule | POST | cached_templates | Yes | command(string), body(CalculateScheduleRequest) | LoanScheduleDto: installments, principal/interest/fees split, total | CACHE_FIRST_SWR |
| submit_loan_application | POST | framework_submit_drafts | Yes | body(LoanApplicationRequest), externalId | CommandProcessingResultDto: commandId, resourceId | durable-outbox |
| loan_lifecycle_command | POST | framework_submit_drafts | Yes | body(LoanCommandRequest) [approve\|reject\|withdrawnByApplicant\|disburse\|undoapproval\|undodisbursal] | CommandProcessingResultDto: commandId, resourceId | durable-outbox |
| loan_transaction | POST | framework_submit_drafts | Yes | body(LoanTransactionRequest) [repayment\|waiveinterest\|writeoff\|foreclosure] | CommandProcessingResultDto: commandId, resourceId | durable-outbox |
| loan_charge_command | POST | framework_submit_drafts | Yes | body(LoanChargeCommandRequest) [pay\|waive] | CommandProcessingResultDto: commandId, resourceId | durable-outbox |
| add_loan_charge | POST | framework_submit_drafts | Yes | body(AddLoanChargeRequest) | CommandProcessingResultDto: commandId, resourceId | durable-outbox |
| create_reschedule | POST | framework_submit_drafts | Yes | body(RescheduleRequest) | CommandProcessingResultDto: commandId, resourceId | durable-outbox |

## Error Handling

All endpoints follow standard error mapping:
- 401 -> Auth error (redirect to signin)
- 403 -> NoAuthorization -> permission-capability-engine drift refresh + re-resolve (not a crash)
- 404 -> NotFound error
- 422 -> Validation error (rejected command routed to needs-attention-inbox)
- 500 -> Server error (retryable; error state + Retry)

Notes:
- `list_loans` / `get_loan_detail` / `get_loan_charges` / `get_*_template` / `calculate_schedule` are reads (`command=calculateLoanSchedule` returns a preview, mutates nothing) — CACHE_FIRST_SWR so the loan list + 360° detail render offline.
- `submit_loan_application` + `loan_lifecycle_command` + `loan_transaction` + `loan_charge_command` + `add_loan_charge` + `create_reschedule` are enqueued through the offline outbox with a DURABLE idempotency key and replayed as an all-or-nothing `POST /v1/batches?enclosingTransaction=true` — money posts EXACTLY ONCE, zero double-post.
- Command endpoints are maker-checker aware: a `commandId` without a `resourceId` = submitted-for-approval (PENDING_APPROVAL), not applied optimistically.
- Backdated posting is a distinct capability: `REPAYMENTINPAST_LOAN` / `DISBURSEINPAST_LOAN` / `APPROVEINPAST_LOAN` gate the date picker's past range.

## Full Contracts

See `idea-layer/screens/m04-loan-portfolio/api.yaml` -> `endpoints[]` for complete request/response schemas.
