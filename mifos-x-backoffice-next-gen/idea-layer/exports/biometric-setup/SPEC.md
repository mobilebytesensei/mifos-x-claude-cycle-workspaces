<!-- source: screens/biometric-setup/ (v4.0 siblings: ui, docs, flow, demo-data) -->
<!-- generated: 2026-08-01T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->

# Biometric Setup — Implementation Specification

> **Source of Truth**: `idea-layer/screens/biometric-setup/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: enriched

---

## Capabilities

- **has_ui** — Compose enrollment prompt screen with adaptive illustration (archetype: form_screen).
- **has_flow** — Platform availability check on mount → prompt / unavailable / not_enrolled_in_os → enrollment dialog → success / error.
- **has_local_preferences** — Encrypted `multiplatform_settings` for `biometric_lock_enabled` + `biometric_type`.
- **has_system_integration** — Android `BiometricManager` / iOS `LocalAuthentication` (expect/actual KMP). OS settings deep-link for enrollment guidance.
- **requires_auth** — Post-login only; requires passcode-lock to be enabled first (biometric supplements passcode, not standalone).

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| biometric-setup | BiometricSetupViewModel | Prompt, Enrolling, Success, Unavailable, Not_Enrolled_In_OS, Error | First-time biometric enrollment. Checks BiometricManager.canAuthenticate(BIOMETRIC_STRONG) on mount — AVAILABLE → prompt (show enroll UI + Skip link); BIOMETRIC_ERROR_NO_HARDWARE or BIOMETRIC_ERROR_HW_UNAVAILABLE → unavailable; BIOMETRIC_ERROR_NONE_ENROLLED → not_enrolled_in_os (Open Settings deep-link). On Enroll: open platform enrollment dialog; on success persist biometric_lock_enabled=true + biometric_type; show success state. No Fineract API calls. Archetype: form_screen. |

## State Model

### BiometricSetupViewModel

- **State**: biometricAvailability (BiometricAvailability: AVAILABLE | UNAVAILABLE | NOT_ENROLLED), biometricType (BiometricType?: FINGERPRINT | FACE | IRIS | null), isEnrolling (Boolean)
- **ScreenState**: Prompt, Enrolling, Success, Unavailable, Not_Enrolled_In_OS, Error
- **Errors**: biometric_unavailable (Unavailable state — "Biometric authentication is not available on this device. You can still use your passcode." — biometric_lock_enabled stays false), biometric_not_enrolled_in_os (Not_Enrolled_In_OS state — "No biometrics enrolled in your device settings. Go to Settings → Security to add a fingerprint or face." — Open Settings deep-link), enrollment_failed (Error state — "Biometric setup failed — please try again or skip." with Retry + Skip; biometric_lock_enabled NOT written until success)
- **Events**: NavigateBack (on Skip, on Done, on Unavailable back)
- **Actions**: Internal.CheckBiometricAvailability (on mount: BiometricManager.canAuthenticate(BIOMETRIC_STRONG) → route to correct state), OnEnrollClick (start_enrollment → AVAILABLE check → platform enrollment dialog; on success write biometric_lock_enabled=true + biometric_type → Success state), OnSkipClick (skip_enrollment → navigate back; biometric_lock_enabled remains false), OnOpenSettingsClick (open_device_security_settings → OS Security deep-link; on return re-check availability), OnDoneClick (finish_setup → NavigateBack → M17 settings where toggle reflects enabled), OnRetryClick (start_enrollment retry → re-open platform enrollment dialog)
- **DI**: core/datastore

## API Endpoints

*(None — all state is device-local. BiometricManager is a platform API, not a network call.)*

## Local Store Schema (`secure_prefs`)

| Key | Type | Notes |
|-----|------|-------|
| biometric_lock_enabled | Boolean | Written to `true` only on successful enrollment completion. Not written on failure, skip, or unavailable. |
| biometric_type | String? | "fingerprint" \| "face" \| "iris" \| null. Written alongside biometric_lock_enabled on success. Used by passcode-lock to show the correct biometric icon. |

## BiometricAvailability States

| canAuthenticate() Result | Mapped State | UX |
|--------------------------|--------------|-----|
| BIOMETRIC_SUCCESS | Prompt | Show illustration + Enroll + Skip |
| BIOMETRIC_ERROR_NO_HARDWARE | Unavailable | "Not available on this device — use passcode." + Back |
| BIOMETRIC_ERROR_HW_UNAVAILABLE | Unavailable | Same as NO_HARDWARE (transient hardware issue) |
| BIOMETRIC_ERROR_NONE_ENROLLED | Not_Enrolled_In_OS | "No biometrics enrolled. Go to Settings → Security." + Open Settings |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| passcode-lock | requires | true | biometric is a second factor; passcode must be enabled before biometric setup is offered |
| fineract-auth-session | requires | true | user must be authenticated; biometric setup is a post-login configuration |
| m17-sync-settings | consumed_by | false | exposes the biometric enable/disable toggle entry point |

## Navigation

- **Route**: biometric-setup
- **From**: passcode-lock (post-setup prompt — "Want to also set up biometric unlock?"), m17-sync-settings biometric_lock_toggle (when no biometric enrolled yet)
- **To**: m17-sync-settings (on Done — biometric toggle reflects enabled), previous screen (on Skip / Unavailable back)

## App-Shell

- **Bottom navigation**: hidden.
- **Top app bar**: visible (title "Biometric Setup" + back arrow).
- **FAB**: none.
- **Snackbar**: enabled — enrollment_failed / success confirmation on return to M17.

## Flow Logic (4 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Biometric availability | BiometricManager.canAuthenticate(BIOMETRIC_STRONG) == BIOMETRIC_SUCCESS | Prompt state (show Enroll + Skip) | NOT_ENROLLED → not_enrolled_in_os; NO_HARDWARE / HW_UNAVAILABLE → unavailable | canAuthenticate() result on mount |
| Enrollment result | Platform enrollment dialog succeeds | write biometric_lock_enabled=true + biometric_type → Success state | failure or cancel → Error state (biometric_lock_enabled NOT written) | BiometricPrompt callback |
| User skip | skip_enrollment tapped from Prompt or Error | navigate back; biometric_lock_enabled remains false | — (one path only) | OnSkipClick → NavigateBack |
| Return from OS settings | user returns after enrolling in OS settings | re-check canAuthenticate → BIOMETRIC_SUCCESS → Prompt | still NOT_ENROLLED → stays in not_enrolled_in_os | ON_RESUME lifecycle check |

## Testing (5 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| fingerprint_enroll_success | Pixel 7 (fingerprint) — BiometricManager returns AVAILABLE → prompt shown → Enroll → OS dialog → success → biometric_lock_enabled=true persisted → Done → M17 toggle enabled | high |
| face_id_enroll_success | iPhone 15 (Face ID) — headline adapts to "Set up Face ID" → same enroll flow → biometric_lock_enabled=true | high |
| hardware_unavailable | Desktop/emulator — BiometricManager returns NO_HARDWARE → unavailable state → Back (no enrollment written) | medium |
| not_enrolled_in_os | Samsung A54 (sensor present, no fingerprint enrolled) — NOT_ENROLLED_ERROR → not_enrolled_in_os → Open Settings → user enrolls → returns → prompt shown | medium |
| enrollment_failed | Enrollment dialog fails (BIOMETRIC_ERROR_LOCKOUT) → error state → Retry → re-opens dialog | medium |
