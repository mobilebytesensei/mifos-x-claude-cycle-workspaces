<!-- source: screens/client-detail-360/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=9c94b2c8a590 docs=dd2671e3d5c7 flow=29f92626aeb9 api=af3a01d8fa8e -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Client 360 (identity · accounts · lifecycle) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/client-detail-360/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: draft

---

## Capabilities

- **has_ui** — Compose detail screen: identity/KYC header + tabbed body (General · Accounts · Notes) + a permission-gated lifecycle action bar.
- **has_offline_cache** — client aggregate + accounts read CACHE_FIRST_SWR through Store5; the client opens fully offline with a background-refresh badge.
- **has_flow** — drilled in from the M02 clients list; loading → content/empty/error, with account rows drilling into loan-detail.
- **requires_auth** — every read + lifecycle mutation runs under the authenticated Fineract session, gated by READ_CLIENT.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| client-detail-360 | ClientDetailViewModel | loading, content, empty, error | The 360° client profile drilled in from the M02 clients list. Loads the association-rich client aggregate (GET /v1/clients/{clientId}?associations=all) plus the client's accounts into an identity/KYC header (display name, account no, external id, status chip, office, assigned staff, legal form) over a tabbed body — General · Accounts (loans/savings/shares with balances) · Notes — and a permission-gated action bar exposing the client lifecycle commands. Each account row drills into loan-detail (savings/shares open their own detail). Reads are cache-first (opens offline); lifecycle mutations route through the offline outbox with a durable idempotency key. Archetype: detail. |

## State Model

### ClientDetailViewModel

- **State**: client (Client? — display name, account no, external id, status, office, staff, KYC/legal form), accounts (ClientAccounts — loan/savings/share accounts each with balance + status), selectedTab (ClientDetailTab — General | Accounts | Notes), permittedActions (Set<ClientAction> — lifecycle actions valid for the status AND held by the PermissionSet), uiState (ClientDetailScreenState)
- **ScreenState**: loading, content, empty, error
- **Errors**: client_not_found (empty state — clientId resolved to no client; not-found panel + back), client_load_failed (retry=true; error state — no usable cache, Retry re-reads aggregate), accounts_load_failed (degrade — Accounts tab shows an inline retry strip, identity header still renders, no whole-screen error), action_not_permitted (403 — action hidden; permission drift refresh + re-resolve)
- **Events**: NavigateToLoanDetail(loanId) → loan-detail (a loan account row tapped; savings/shares open their own detail)
- **Actions**: OnLoad (read client aggregate + accounts cache-first, resolve permitted actions), OnTabSelected (select_tab → in-VM tab switch, no refetch), OnOpenLoan (open_loan → navigate to loan-detail), OnActivateClient (activate_client → POST …?command=activate enqueued to draft_outbox), OnAssignStaff (assign_staff → POST …?command=assignStaff enqueued), OnCloseClient (…?command=close enqueued), OnAddNote (append a client note), OnRetry (retry → re-read aggregate + accounts)
- **DI**: core/store, core/data, core/database, core/permissions, core/network

## API Endpoints (3)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| get_client | GET | clientId(Long), associations(String)? | ClientDto | 401, 403 | cached_clients |
| get_client_accounts | GET | clientId(Long) | ClientAccountsDto | 401, 403 | cached_client_accounts |
| client_lifecycle_command | POST | clientId(Long), command(String) | CommandProcessingResultDto | 401, 403, 409 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_CLIENT (read) + per-command gates (ACTIVATE_CLIENT, ASSIGNSTAFF_CLIENT, …) against the status for the action bar |
| offline-sync-engine | uses | true | durable outbox + idempotency key + POST /v1/batches replay for exactly-once lifecycle mutations |
| fineract-auth-session | requires | true | authenticated session required for the aggregate + accounts reads and every command |

## Navigation

- **Route**: client-detail-360(clientId)
- **From**: m02-clients (open_client_360 row), needs-attention-inbox (transfer-proposed / KYC-alert drill-in)
- **To**: loan-detail (account row)

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Client resolves | client aggregate + accounts resolve (cache-first) | content | (see empty/error) | Store5 CACHE_FIRST_SWR over GET /v1/clients/{clientId} |
| Not found | the clientId resolves to no client | empty (not-found panel + back) | content | server 404 / deleted client |
| Load hard-fail | client failed AND no cache available | error (message + Retry) | content | GET failure with empty cache |
| Accounts degrade | aggregate loads but accounts fetch times out | content — Accounts tab shows inline retry strip, header renders | content (full) | partial load; identity unaffected |
| Account drill | a loan account row is tapped | navigate loan-detail | content | savings/shares open their own detail |
| Lifecycle action | a permitted action is tapped (confirm-gated) | content — command enqueued to draft_outbox, status updates in place | error | POST /v1/clients/{clientId}?command=… via outbox |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_identity_accounts_shimmer | The loading state shows an identity + accounts shimmer with no error | medium |
| content_shows_identity_tabs_accounts | Header shows name/account-no/status/office/staff; Accounts tab lists loans/savings/shares; only permitted-and-held actions shown | high |
| empty_when_client_not_found | A not-found panel with a back affordance when the clientId resolves to no client | low |
| error_when_client_load_fails_no_cache | The error state is shown and Retry re-reads the aggregate + accounts | medium |
| accounts_failure_degrades_to_strip | An accounts timeout shows an inline retry strip on the tab; the header still renders; no whole-screen error | high |
| account_row_drills_into_detail | Tapping a loan account row navigates to loan-detail (savings/shares open their own detail) | high |
| activate_enqueues_through_outbox | Activate (confirm) enqueues with a durable key; sync replays ?command=activate; the status chip flips to Active | high |
| assign_staff_gated_by_permission | Assign staff is shown only when ASSIGNSTAFF_CLIENT is held | medium |
