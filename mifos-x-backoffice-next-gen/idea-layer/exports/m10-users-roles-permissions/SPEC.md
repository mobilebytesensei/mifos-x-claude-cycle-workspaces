<!-- source: screens/m10-users-roles-permissions/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=715063a4da96 docs=dbc9392e67eb flow=ff5a1915c59c api=f4249d320679 -->
<!-- generated: 2026-07-21T17:40:32Z -->
<!-- generated_from_feature_version: 2.2.0 -->
<!-- generated_from_contract_version: 2.2.0 -->
<!-- prior_version: — -->

# M10 · Users, Roles & Permissions -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m10-users-roles-permissions/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 2.2.0 | **Contract version**: 2.2.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose master/detail admin control plane: Users tab, Roles tab, and the role→permission grid editor.
- **has_api** — Fineract /v1/users (+template), /v1/roles (+enable/disable command), /v1/roles/{id}/permissions, /v1/permissions.
- **has_offline_cache** — CACHE_FIRST_SWR roster + grid read cache; user/role writes queue via the durable outbox; grid saves + maker-checker are ONLINE-ONLY.
- **has_form_input** — user/role create/edit rendered by dynamic-template-forms; in-grid permission-cell + maker-checker toggles.
- **has_flow** — pick tab → open role → edit grid → save online (invalidates capability cache) / create user → queue.
- **requires_auth** — every entry point + command permission-gated (USER_ADMINISTRATION_SUPER_USER tier); self-referential (administers the very permissions the engine consumes).

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| M10 · Users, Roles & Permissions (`m10-users-roles-permissions`, archetype `master_detail`) | UsersRolesPermissionsViewModel | loading, content, empty, submitting, saving, error | The ADMIN editor twin of permission-capability-engine (the runtime evaluator). An authoriser creates app users and assigns office/staff/roles, resets passwords, manages roles (create/enable/disable), edits the role→permission grid, and toggles maker-checker per action. Rosters + grid read CACHE_FIRST_SWR; user/role create+edit route through dynamic-template-forms and queue via the offline outbox; the grid save + maker-checker config are ONLINE-ONLY and saving invalidates the runtime PermissionSet cache so affected users re-resolve on next login. |

## State Model

### UsersRolesPermissionsViewModel

- **State**: `users: List<AppUserRow>` (office-scoped roster — username, office, staff, roles, enabled/locked), `roles: List<RoleRow>` (name, description, disabled, member-count, permission-count), `selectedRoleId: String` (role whose grid is open; null until a role opened), `permissionGrid: List<PermissionGroupRow>` (grouped by Fineract grouping; each cell carries code/selected/makerCheckerable), `pendingGridChanges: Map<String, Boolean>` (in-memory code→selected diff; non-empty enables Save + drives dirty), `selectedTab: UserRoleTab` (Users | Roles), `fromCache: Boolean` (Room cache hit → offline banner)
- **ScreenState**: Loading (`loading`), Content (`content`), Empty (`empty`), Submitting (`submitting`), Saving (`saving`), Error (`error`)
- **Errors**: `users_read_failed` (retry=true; cached roster served), `role_permissions_read_failed` (retry=true; cached read-only grid served), `auth_config_offline_blocked` (grid save/maker-checker refused offline, no outbox row), `permission.revoked` (403 drift → refreshPermissions() + prune controls live), `command_rejected` (queued user/role command → needs-attention-inbox, never dropped), `maker_checker.queued` (commandId + empty changes → Submitted-for-approval), `role_permissions_conflict` (409 optimistic-lock on grid save → reload-grid prompt, edit preserved)
- **Events**: `NavigateToForm(entity, mode|command, userId)` → dynamic-template-forms
- **Actions**: `OnTabSelected` (select_tab → lazily hydrate pane from cache-first Store); `OnUserRowClick` (open_user_detail → NavigateToForm view); `OnRoleRowClick` (open_role_permission_grid → GET /v1/roles/{id}/permissions, editable iff PERMISSIONS_ROLE); `OnNewUserClick` (create_user → forms; queues CREATE_USER); `OnEditUserClick` (edit_user → forms; queues UPDATE_USER, own-user exempt); `OnResetPasswordClick` (reset_user_password → confirm → PUT /v1/users/{id}); `OnNewRoleClick` (create_role → forms; queues CREATE_ROLE); `OnToggleRoleEnabled` (toggle_role_enabled → POST /v1/roles/{id}?command=enable|disable); `OnPermissionCellToggle` (toggle_role_permission → in-memory diff, mark dirty); `OnMakerCheckerCellToggle` (toggle_maker_checker → PUT /v1/permissions ONLINE-ONLY); `OnSaveMatrixClick` (save_permission_matrix → PUT /v1/roles/{id}/permissions ONLINE-ONLY, invalidate capability cache); `OnRetryClick` (retry_load → re-issue fetch; 403 → drift protocol); `Internal.ReceiveUsersRoles`; `Internal.ReceivePermissionGrid`; `Internal.ReceiveCommandResult`
- **DI**: `core/store`, `core/data`, `core/database`, `core/network`, `core/permissions`, `cmp-network-monitor`

## API Endpoints (11)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_users | GET | offset(Int), limit(Int), orderBy(String), sortOrder(String) | AppUser[] | 401 | users_roles |
| get_user_template | GET | — | AppUserTemplate | 401 | cached_templates |
| create_user | POST | body(CreateUserRequest) | CommandProcessingResult | 401, 422, 500 | draft_outbox |
| update_user | PUT | userId(String), body(UpdateUserRequest) | CommandProcessingResult | 401, 422, 500 | draft_outbox |
| list_roles | GET | — | Role[] | 401 | users_roles |
| create_role | POST | body(CreateRoleRequest) | CommandProcessingResult | 401, 422, 500 | draft_outbox |
| toggle_role_status | POST | roleId(String), command(String) | CommandProcessingResult | 401, 422, 500 | draft_outbox |
| get_role_permissions | GET | roleId(String) | RolePermissions | 401, 403 | role_permissions |
| update_role_permissions | PUT | roleId(String), body(RolePermissionsRequest) | CommandProcessingResult | 401, 403, 409 | role_permissions |
| list_permissions | GET | makerCheckerable(Boolean) | Permission[] | 401 | role_permissions |
| update_permission_maker_checker | PUT | body(PermissionMakerCheckerRequest) | CommandProcessingResult | 401, 403 | role_permissions |

## Dependencies (Tier 1 — foundation)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | Resolves every READ_/CREATE_/UPDATE_USER, READ_/CREATE_/ENABLE_/DISABLE_ROLE, READ_/UPDATE_PERMISSION, PERMISSIONS_ROLE gate; grid save invalidates its runtime PermissionSet cache; 403-drift refresh (read-only cross-link) |
| dynamic-template-forms | renders_forms | true | Renders user/role create+edit surfaces from GET /v1/users/template allowed-value lists (offices, staff, roles, send-password); never hardcoded |
| offline-sync-engine | queues_writes | true | Durable outbox + idempotency key for CREATE_USER / UPDATE_USER / CREATE_ROLE / enable|disable via POST /v1/batches; auth grid + maker-checker EXCLUDED (online-only) |
| needs-attention-inbox | rejected_commands | true | A server-rejected queued user/role command is triaged here (Retry / Edit / Discard); never silently dropped |

## Navigation

- **Route**: m10-users-roles-permissions (nav root; gated on the authorisation permission group)
- **From**: nav_root (module root — hidden entirely unless the operator holds ≥1 of READ_USER / READ_ROLE)
- **To**: dynamic-template-forms (user/role create·edit·view), permission-capability-engine (read-only — cache invalidation), offline-sync-engine (queued writes), needs-attention-inbox (rejected commands)

## App-Shell

- **Bottom navigation**: hidden (reached as a permission-gated nav root, not a bottom tab).
- **Top app bar**: visible (title `{strings.m10_title}`; Users | Roles tab strip below).
- **FAB**: none — New-user / New-role are in-content buttons, each HIDE-gated on its CREATE_ permission.
- **Snackbar**: enabled (queued-offline / Submitted-for-approval / online-only-blocked / permission-drift toasts).

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| roster/grid resolve | GET users + roles (+ selected role grid) succeeds or cache hit | loading → content (or empty if the active tab has 0 rows) | loading → error (no cache) | Store5 CACHE_FIRST_SWR stream over Room (users_roles / role_permissions) |
| user/role write path | a user/role create/edit/enable/disable/password command is assembled | content → submitting → content (queued offline, or accepted online) | submitting → error (400/403/409; rejected → needs-attention-inbox) | durable outbox + idempotency key; maker-checker → Submitted-for-approval |
| auth config connectivity | connectivity available at grid save / maker-checker toggle | content → saving → content (PUT persisted; runtime capability cache invalidated) | content → error / auth_config_offline_blocked (refused offline, never queued) | ONLINE-ONLY; cmp-network-monitor gate; PUT /v1/roles/{id}/permissions or PUT /v1/permissions |
| permission gate | operator holds the control's permission (READ_/CREATE_/UPDATE_*, PERMISSIONS_ROLE, UPDATE_PERMISSION) | control rendered / enabled (grant path) | control HIDDEN or grid read-only (fail-closed deny → permission_denied) | permission-capability-engine PermissionEvaluator on_denied=HIDE/DISABLE |
| maker-checker / drift | command returns commandId with empty changes; or 403 authorization drift | Submitted-for-approval (a third state) | 403 → refreshPermissions() + prune denied controls, screen stays alive | maker-checker aware; 403 drift protocol re-resolves the surface |

## Testing (11 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_state_renders | Loading state renders while the users/roles/grid fetch is in flight | high |
| content_rosters_render | Users tab, Roles tab and the grid editor render from resolved data | high |
| empty_state_with_gated_cta | An empty pane shows a Create CTA only when the matching create permission is held | medium |
| control_plane_read_gated_readonly | A read-only operator sees the rosters but cannot edit the grid or create | high |
| create_user_routes_through_forms_and_queues | New user routes through dynamic-template-forms and queues CREATE_USER exactly-once | high |
| reset_password_online | Password reset submits online via PUT /v1/users/{id} (Submitted-for-approval if maker-checkerable) | high |
| enable_disable_role | Enable/disable a role via POST /v1/roles/{id}?command=enable\|disable; member sets change | high |
| edit_role_permission_grid_invalidates_cache | Editing + saving the grid persists online and invalidates the runtime capability cache | high |
| toggle_maker_checker_online | Toggling maker-checker for a permission is online-only via PUT /v1/permissions | high |
| auth_config_blocked_offline | Grid save + maker-checker toggle are blocked with no connectivity (online-only, nothing queued) | high |
| error_and_permission_drift | A fetch error retries; a 403 drift re-fetches permissions and prunes now-denied controls live | high |
