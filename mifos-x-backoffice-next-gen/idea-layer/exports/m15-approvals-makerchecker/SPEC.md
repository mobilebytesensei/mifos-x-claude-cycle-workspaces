<!-- source: screens/m15-approvals-makerchecker/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=eaa29114b6a8 docs=49a147d73055 flow=0f139c16974c api=5eb3fdb0179f -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M15 · Approvals (Maker-Checker) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m15-approvals-makerchecker/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose master/detail checker-queue screen (grouped list + payload inspect panel).
- **has_offline_cache** — CACHE_FIRST_SWR read of the self-scoped queue + search-template over Room; durable outbox for every command.
- **has_flow** — load → inspect payload → approve/reject/delete/bulk → atomic exactly-once replay.
- **requires_auth** — every operation authenticated + canCheck(action,entity) permission-gated.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| m15-approvals-makerchecker | MakerCheckerViewModel | Loading, Content, Empty, Submitting, Error | Self-scoped four-eye checker queue (the WRITE/action counterpart to needs-attention-inbox): queued Fineract commands grouped by entity/action/maker, per-entry payload inspect, approve/reject/delete + bulk-approve enqueued exactly-once through the offline outbox, every control gated purely by canCheck. Archetype: list. |

## State Model

### MakerCheckerViewModel

- **State**: entries (List<MakerCheckerEntry>), groups (List<MakerCheckerGroup>), groupBy (MakerCheckerGrouping), actionFilter (String?), entityFilter (String?), selectedEntry (MakerCheckerEntry?), selectedIds (Set<Long>), resolvingId (Long?), uiState (MakerCheckerScreenState)
- **ScreenState**: Loading, Content, Empty, Submitting, Error
- **Errors**: checker_queue_failed (retry=true), search_template_failed (degrades to unfiltered queue, not whole-screen error), command_enqueue_failed (hard local enqueue failure → error), permission_revoked_on_replay (BLOCKED_PERMISSION_REVOKED, never silently approved), command_rejected_on_replay (hard rejection routes to needs-attention-inbox)
- **Events**: NavigateToNeedsAttention → needs-attention-inbox (read-only cross-link)
- **Actions**: OnSwitchGroupBy (switch_group_by → in-VM re-bucket, no refetch), OnApplySearchTemplateFilter (apply_search_template_filter → GET ?actionName=&entityName=), OnOpenCommandPayload (open_command_payload → inspect panel), OnToggleEntrySelection (toggle_entry_selection → selectedIds), OnApproveEntry (approve_entry → outbox POST ?command=approve), OnRejectEntry (reject_entry → outbox POST ?command=reject), OnDeleteEntry (delete_entry → outbox DELETE, confirm-gated), OnBulkApproveSelected (bulk_approve_selected → outbox POST /v1/batches independent), OnOpenNeedsAttention (open_needs_attention → nav), OnRetryLoad (retry_load → re-issue GET /v1/makercheckers)
- **DI**: core/store, core/data, core/database, core/permissions, core-base/store, core-base/network

## API Endpoints (6)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_makercheckers | GET | actionName(String?), entityName(String?), makerCheckerable(Boolean?), makerDateTimeFrom(String?), offset(Int), limit(Int) | MakerCheckerEntryDto[] | 401, 403 | maker_checker_inbox |
| makerchecker_search_template | GET | — | MakerCheckerSearchTemplateDto | 401, 403 | maker_checker_search_template_cache |
| approve_entry | POST | auditId(Long), command(String) | CommandProcessingResultDto | 401, 403, 422 | draft_outbox |
| reject_entry | POST | auditId(Long), command(String) | CommandProcessingResultDto | 401, 403, 422 | draft_outbox |
| delete_entry | DELETE | auditId(Long) | CommandProcessingResultDto | 401, 403, 422 | draft_outbox |
| bulk_approve_entries | POST | enclosingTransaction(Boolean), BatchRequest([]) | BatchResponseDto[] | 401, 500 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves canCheck(action,entity) = CHECKER_SUPER_USER ∨ {ACTION}_{ENTITY}_CHECKER; drives per-control VISIBLE/DISABLED/HIDDEN + hasAnyChecker nav gate + 403 drift refresh |
| offline-sync-engine | uses | true | durable idempotency-key outbox (draft_outbox) + exactly-once /v1/batches replay so a command is never double-approved |
| needs-attention-inbox | navigate | true | read-only cross-link target + landing surface for rejected/permission-revoked replays (never silently dropped) |

## Navigation

- **Route**: m15-approvals-makerchecker
- **From**: nav_root (drawer/rail, gated on hasAnyChecker: ∃ *_CHECKER ∨ CHECKER_SUPER_USER), from_dashboard (m01-dashboard 'Approvals Waiting' tile), from_needs_attention (an approval row drilled from needs-attention-inbox)
- **To**: needs-attention-inbox

## App-Shell

- **Bottom navigation**: hidden (reached as a permission-gated nav root, not a bottom-tab; should_have/P2 module).
- **Top app bar**: visible (queue header + grouping/filter context).
- **FAB**: none (the checker does not author queued commands here).
- **Snackbar**: enabled (queued/exactly-once, permission-drift, retry toasts).
- **Safe-area**: standard top-app-bar inset; grouped list scrolls under the bar.

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Queue has entries | GET /v1/makercheckers returns ≥1 queued command the checker canCheck | content (grouped) | empty ("all caught up") | fold Store5 stream → ScreenState |
| Cache vs error | fetch failed | no cache → error + Retry (403 → permission-drift refresh) | serve cached queue offline | CACHE_FIRST_SWR SourceOfTruth over Room |
| Group-by switch | group-by (entity/action/maker) changed | re-bucket group headers in-VM, no refetch | — | pure in-ViewModel groupBy state change |
| Filter applied | action/entity filter chosen from searchtemplate | loading → re-issue GET ?actionName=&entityName= | clearing chips re-fetches full self-scoped queue | Store5 SoT refetch, refresh Room + FetchedAt |
| Command submit | approve/reject/delete (after confirm)/bulk-approve chosen | submitting → content (enqueued, row locked, exactly-once replay) | submitting → error (hard local enqueue failure) | offline outbox durable idempotency key → POST /v1/batches |
| Replay authorization | canCheck re-checked in domain layer at replay | approve/reject/delete applies exactly-once | BLOCKED_PERMISSION_REVOKED / hard reject → needs-attention-inbox (never dropped) | per-command canCheck revalidation on replay |

## Testing (17 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_grouped_shimmer | The loading state shows a grouped shimmer list until entries resolve | medium |
| content_lists_queued_commands_grouped | Rows appear grouped under entity/action/maker with per-group counts + pending badges | high |
| empty_state_when_no_pending | No pending approvals shows the 'all caught up' state with no approve/reject controls | medium |
| submitting_state_locks_row_on_action | Approve locks the row with an inline queued indicator; the rest of the queue stays interactive | high |
| error_state_on_fetch_failure_no_cache | A fetch failure with no cache shows the error state; Retry re-issues the queue fetch | medium |
| queue_is_self_scoped_by_can_check | A CREATE_CLIENT_CHECKER-only checker sees only CREATE_CLIENT entries; others absent | high |
| approve_reject_delete_only_for_authorized_entries | An unauthorized entry shows no Approve/Reject/Delete control and no bulk checkbox | high |
| row_opens_payload_inspect_panel | Tapping a row opens the payload panel with maker, action, entity, made-on, command JSON | high |
| group_by_rebuckets_without_refetch | Switching group-by re-groups rows in-VM with no network fetch | medium |
| search_template_filter_narrows_queue | Picking actionName/entityName issues the filtered GET; only matching entries remain | medium |
| approve_enqueues_exactly_once | Approve enqueues with a durable idempotency key, replays exactly-once, cannot be re-approved in flight | high |
| reject_notifies_maker | Reject enqueues, notifies the maker, discards the underlying command (never executed) | high |
| delete_requires_confirm_then_purges | Delete after confirm enqueues DELETE; the command is purged, never approved/executed/silently dropped | high |
| bulk_approve_is_independent_batch | Bulk-approve enqueues one approve per selected entry as an Independent batch; one failure never aborts the rest | high |
| replay_blocked_when_can_check_revoked | A revoked-permission replay surfaces BLOCKED_PERMISSION_REVOKED, never silently approved/dropped | high |
| rejected_replay_surfaces_in_needs_attention | A server-rejected replay surfaces in needs-attention-inbox, never silently swallowed | medium |
| open_needs_attention_is_read_only | 'Open Needs Attention' navigates read-only; no inbox state is mutated by the jump | medium |
