<!-- source: screens/m15-approvals-makerchecker/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=32b8c4847ba0 docs=59f5e2c8d232 flow=0f139c16974c api=3cb354c554fc -->
<!-- generated: 2026-07-31T03:23:09Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M15 · Approvals (Maker-Checker) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m15-approvals-makerchecker/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: designed

---

## Capabilities

- **has_ui** — Compose master/detail checker-queue screen (grouped list + payload inspect panel).
- **has_offline_cache** — CACHE_FIRST_SWR read of the self-scoped queue + search-template over Room; durable outbox for every command.
- **has_flow** — load → inspect payload → approve/reject/delete/bulk → atomic exactly-once replay.
- **requires_auth** — every operation authenticated + canCheck(action,entity) permission-gated.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| m15-approvals-makerchecker | MakerCheckerViewModel | Loading, Content, Empty, Submitting, Error | Self-scoped four-eye checker queue (the WRITE/action counterpart to needs-attention-inbox): queued Fineract commands grouped by entity/action/maker, per-entry payload inspect, approve/reject/delete + bulk-approve enqueued exactly-once through the offline outbox, every control gated purely by canCheck. Archetype: list_screen. |

## State Model

### MakerCheckerViewModel

- **State**: entries (List<MakerCheckerEntry>) -- self-scoped queued commands, permission-filtered to canCheck entries; groups (List<MakerCheckerGroup>) -- buckets over entries keyed by the active grouping, each with a count; groupBy (MakerCheckerGrouping) -- active grouping dimension (entity | action | maker); actionFilter (String?) -- optional actionName filter from the searchtemplate; entityFilter (String?) -- optional entityName filter from the searchtemplate; selectedEntry (MakerCheckerEntry?) -- entry whose payload is open in the inspect panel; selectedIds (Set<Long>) -- audit ids checked for bulk-approve; resolvingId (Long?) -- audit id of the entry a single approve/reject/delete is currently applied to; uiState (MakerCheckerScreenState) -- Loading | Content | Empty | Submitting | Error
- **ScreenState**: Loading, Content, Empty, Submitting, Error
- **Errors**: checker_queue_failed (retry=true — Retry CTA re-issues GET /v1/makercheckers), search_template_failed (retry=false — degrades to unfiltered content, never a whole-screen error), command_enqueue_failed (retry=false — hard local enqueue failure, transitions submitting→error), permission_revoked_on_replay (retry=false — surfaces BLOCKED_PERMISSION_REVOKED in needs-attention-inbox, never silently approved), command_rejected_on_replay (retry=false — hard server rejection surfaces in needs-attention-inbox, never silently swallowed)
- **Events**: NavigateToNeedsAttention → needs-attention-inbox (read-only cross-link, triggered by OnOpenNeedsAttention)
- **Actions**: OnSwitchGroupBy (switch_group_by → pure in-VM re-bucket of already-loaded entries, no refetch), OnApplySearchTemplateFilter (apply_search_template_filter → loading → GET /v1/makercheckers?actionName=&entityName=), OnOpenCommandPayload (open_command_payload → opens the payload inspect panel in the same master-detail surface), OnToggleEntrySelection (toggle_entry_selection → add/remove audit id from selectedIds), OnApproveEntry (approve_entry → submitting → outbox POST /v1/makercheckers/{auditId}?command=approve), OnRejectEntry (reject_entry → submitting → outbox POST /v1/makercheckers/{auditId}?command=reject), OnDeleteEntry (delete_entry → submitting → outbox DELETE /v1/makercheckers/{auditId}, confirm-gated), OnBulkApproveSelected (bulk_approve_selected → submitting → outbox POST /v1/batches, Independent, one approve per selected id), OnOpenNeedsAttention (open_needs_attention → navigate to needs-attention-inbox, read-only), OnRetryLoad (retry_load → error → loading, re-issues GET /v1/makercheckers; 403 routes to permission-drift refresh)
- **DI**: core/store, core/data, core/database, core/permissions, core-base/store, core-base/network

## API Endpoints (6)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_makercheckers | GET | actionName(String?), entityName(String?), makerCheckerable(Boolean?), makerDateTimeFrom(String?), offset(Int?), limit(Int?) | MakerCheckerEntryDto[] | 401, 403 | maker_checker_inbox |
| makerchecker_search_template | GET | — | MakerCheckerSearchTemplateDto | 401, 403 | maker_checker_search_template_cache |
| approve_entry | POST | auditId(Long), command(String) | CommandProcessingResultDto | 401, 403, 422 | draft_outbox |
| reject_entry | POST | auditId(Long), command(String) | CommandProcessingResultDto | 401, 403, 422 | draft_outbox |
| delete_entry | DELETE | auditId(Long) | CommandProcessingResultDto | 401, 403, 422 | draft_outbox |
| bulk_approve_entries | POST | enclosingTransaction(Boolean?) | BatchResponseDto[] | 401, 500 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves canCheck(action,entity) = CHECKER_SUPER_USER ∨ {ACTION}_{ENTITY}_CHECKER; drives per-control VISIBLE/DISABLED/HIDDEN, the hasAnyChecker nav gate, and the 403 permission-drift refresh |
| offline-sync-engine | uses | true | durable idempotency-key outbox (draft_outbox) + exactly-once /v1/batches replay so a command is never double-approved |
| needs-attention-inbox | navigate | true | read-only cross-link target + landing surface for rejected/permission-revoked replays (never silently dropped) |

## Navigation

- **Route**: m15-approvals-makerchecker
- **From**: nav_root (drawer/rail, gated on hasAnyChecker: ∃ *_CHECKER ∨ CHECKER_SUPER_USER), from_dashboard (M01 dashboard "Approvals Waiting" tile), from_needs_attention (an approval row drilled in from needs-attention-inbox)
- **To**: needs-attention-inbox

## App-Shell

- **Bottom navigation**: hidden (reached as a permission-gated nav root, not a bottom-tab; should_have/P2 module surfaced in the drawer/rail only when the checker holds ≥1 *_CHECKER ∨ CHECKER_SUPER_USER).
- **Top app bar**: visible (queue header + grouping/filter context).
- **FAB**: none (the checker does not author queued commands here — `fab_visible: false`).
- **Snackbar**: enabled (`snackbar_visible: true` — queued/exactly-once, permission-drift, retry toasts).
- **Safe-area**: standard top-app-bar inset; grouped list scrolls under the bar.

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Queue has entries | GET /v1/makercheckers returns ≥1 queued command the checker canCheck | content (grouped by entity/action/maker) | empty ("all caught up" + last-synced timestamp) | fold Store5 CACHE_FIRST_SWR stream → ScreenState |
| Cache vs error on fetch | queue fetch fails | no cache → error + Retry CTA (403 → permission-drift refresh, checker drops to nearest visible ancestor if access changed) | cache exists → serve cached queue instantly, revalidate in background | Store5 SourceOfTruth over Room |
| Group-by switch | groupBy changed (entity/action/maker) | re-bucket group headers in-VM from already-loaded entries, no refetch | — | pure in-ViewModel groupBy transform |
| Filter applied | action/entity filter chosen from GET /v1/makercheckers/searchtemplate options | loading → re-issue GET /v1/makercheckers?actionName=&entityName=, refresh Room + FetchedAt | clearing chips re-fetches the full self-scoped queue | Store5 SoT refetch narrowed by query params |
| Command submit | Approve / Reject / Delete (after confirm) / Bulk-approve chosen on canCheck-gated entries | submitting → content (enqueued to draft_outbox, row locked, exactly-once replay via POST /v1/batches) | submitting → error (hard local enqueue failure, not a server rejection) | offline outbox durable idempotency key |
| Replay authorization | canCheck(action,entity) re-checked in the domain layer at replay time | approve/reject/delete/bulk applies exactly-once, entry leaves the queue | BLOCKED_PERMISSION_REVOKED (permission revoked) or hard server rejection → surfaces in needs-attention-inbox, never dropped | per-command canCheck revalidation on replay |

## Testing (27 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_grouped_shimmer | Loading is the initial_state; a grouped shimmer placeholder list renders until entries resolve | medium |
| content_lists_queued_commands_grouped | Three demo entries render grouped by entity (Clients 1, Loans 2) with per-group counts, maker, made-on, and payload fields | high |
| empty_state_when_no_pending | An empty queue shows the "all caught up" illustration + last-synced timestamp, no controls | medium |
| submitting_state_locks_row_on_action | Approving audit_id 88202 shows a queued indicator and locks that row while the rest of the queue stays interactive | high |
| error_state_on_fetch_failure_no_cache | A 5xx with no Room cache shows the whole-screen error state + Retry CTA | medium |
| switch_group_by_rebuckets_without_refetch | Switching groupBy to maker/action re-buckets in-VM with zero network fetch | medium |
| filter_bar_narrows_queue_by_action_entity | Picking actionName=DISBURSE&entityName=LOAN re-issues the filtered GET, leaving only audit_id 88203 | medium |
| filter_bar_clear_chips_refetches_full_queue | Clearing all filter chips re-issues the unfiltered GET and restores all three entries | medium |
| open_command_payload_shows_full_details | Tapping audit_id 88201 opens the inspect panel with maker, action, entity, made-on, and the full CREATE CLIENT JSON body | high |
| toggle_entry_selection_adds_to_bulk_set | Checking 88202 and 88203 adds both to selectedIds and reveals the bulk-approve bar | high |
| approve_entry_enqueues_exactly_once | Approve on 88202 enqueues to draft_outbox with a durable idempotency key and replays exactly-once via POST /v1/batches | high |
| reject_entry_discards_command_notifies_maker | Reject on 88201 enqueues, notifies faith.njeri, and discards the underlying CREATE CLIENT command | high |
| delete_entry_requires_confirm_then_purges | Delete on 88203 only enqueues DELETE after an explicit confirm dialog; dismissing creates no outbox entry | high |
| bulk_approve_selected_independent_batch | Bulk-approving 88202+88203 sends one Independent BatchRequest per entry; one failure never aborts the other | high |
| open_needs_attention_navigates_without_mutation | The read-only cross-link navigates to needs-attention-inbox without mutating any inbox state | low |
| retry_load_transitions_back_to_loading | Retry re-issues GET /v1/makercheckers from the error state; a 403 routes to permission-drift refresh instead of looping | medium |
| list_makercheckers_self_scoped_permission_filtered | A checker holding only CREATE_CLIENT_CHECKER sees only audit_id 88201; 88202/88203 are absent | high |
| makerchecker_search_template_populates_filter_chips | The filter_bar options (CREATE/APPROVE/DISBURSE, CLIENT/LOAN) come from the searchtemplate response, not hardcoded | medium |
| search_template_miss_degrades_gracefully | A searchtemplate 5xx with no cache keeps the queue in content (unfiltered), never triggers the whole-screen error state | medium |
| list_makercheckers_preflight_badges_via_makercheckerable | GET ?makerCheckerable=true drives the pre-flight badge count on the M15 nav entry, removed when the queue empties | medium |
| approve_entry_prevents_double_approve_via_idempotency_lock | A second tap on an in-flight approve is blocked client-side; the durable idempotency_key guarantees exactly-once server-side too | high |
| bulk_approve_per_entry_failure_surfaces_individually | A per-entry batch failure (88202) surfaces individually in needs-attention-inbox without blocking the other entry's (88203) success | high |
| replay_blocked_on_permission_revoked | A permission revoked between enqueue and replay surfaces BLOCKED_PERMISSION_REVOKED, never silently auto-approved | high |
| rejected_replay_surfaces_in_needs_attention_inbox | A server-level rejection on replay surfaces in needs-attention-inbox with enough context to act on, never silently swallowed | medium |
| checker_sees_only_cancheck_entries_grouped_inspect_approve | Docs acceptance ref 1 — full journey: group by action, inspect, approve one, bulk-approve another, all canCheck-gated | high |
| all_mutations_offline_exactly_once_never_double_approved | Docs acceptance ref 2 — offline enqueue survives a connectivity drop and replays exactly-once on reconnect, never double-approved | high |
| preflight_badges_and_searchtemplate_filters_from_docs | Docs acceptance ref 3 — pre-flight badge from ?makerCheckerable=true and filter chips from the searchtemplate both verified end-to-end | medium |
