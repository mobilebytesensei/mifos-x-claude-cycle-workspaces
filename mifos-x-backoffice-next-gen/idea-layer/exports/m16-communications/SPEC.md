<!-- source: screens/m16-communications/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=f1148bbba204 docs=c7f5d613ecf1 flow=03d969aaa990 api=d45c14b36f17 -->
<!-- generated: 2026-07-21T18:23:27Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M16 · Communications -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m16-communications/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose master/detail communications module (campaigns · templates · email config · hooks tabs).
- **has_api** — Fineract smscampaigns / templates / hooks / externalservice endpoints.
- **has_offline_cache** — CACHE_FIRST_SWR Store5 reads + durable outbox for every lifecycle/management command.
- **has_form_input** — dynamic-template-forms create/edit campaign, template, hook and email-config forms.
- **has_flow** — list → detail → command-submit (create/activate/close · template/hook/email) → exactly-once replay.
- **requires_auth** — every read + command authenticated and gated on its exact {ACTION}_{ENTITY} permission.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| m16-communications | CommunicationsViewModel | Loading, Content, Empty, Submitting, Queued-offline, Pending-approval, Error, No-network | Marketing/admin communications module: SMS/email campaign list + per-campaign recipient delivery report, message-template library (business-rule + UGD document templates), outbound email/SMS gateway config, and registered webhooks (hooks). Author a template, create/activate/close a campaign, register a hook, edit the email config — every command flows through the durable outbox exactly-once. Archetype: master_detail. |

## State Model

### CommunicationsViewModel

- **State**: campaigns (List<SmsCampaign>), cachedCampaigns (List<SmsCampaign>), selectedTab (CommsTab), selectedCampaignId (String?), recipientReport (RecipientReport?), templates (List<Template>), hooks (List<Hook>), emailConfig (ExternalServiceConfig?), campaignStatus (CampaignStatus, optimistic pending-sync), queuedCount (Int)
- **ScreenState**: Loading, Content, Empty, Submitting, Queued-offline, Pending-approval, Error, No-network
- **Errors**: campaigns_read_failed (retry=true; keep last cache), recipient_report_read_failed (retry=true), templates_read_failed (retry=true), hooks_read_failed (retry=true), campaign_command_rejected (route → needs-attention-inbox), template_command_rejected (route → needs-attention-inbox), hook_command_rejected (route → needs-attention-inbox), email_config_command_rejected (route → needs-attention-inbox), template_fetch_failed (form template unavailable), offline_no_cache (no-network, no cached scope)
- **Events**: NavigateToDynamicTemplateForms(formSpec) → dynamic-template-forms, NavigateToNeedsAttention → needs-attention-inbox
- **Actions**: LoadCampaigns (campaignsStream CACHE_FIRST_SWR), SwitchCommsTab (in-VM re-projection, no refetch), OpenCampaignDetail (recipientReportStream), ActivateCampaign (outbox enqueue → activate, optimistic Active), CloseCampaign (outbox enqueue → close, optimistic Closed), ViewRecipientReport (messageByStatus CACHE_FIRST_SWR), CreateCampaign (outbox enqueue), CreateTemplate (outbox enqueue), RegisterHook (outbox enqueue), SaveEmailConfig (outbox enqueue; password secure-stored), RetryLoad (stream.refreshFresh), OpenNeedsAttention (emit NavigateToNeedsAttention)
- **DI**: core/model, core/network, core/database, core/store, core/data, core-base/store, core-base/network

## API Endpoints (15)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_sms_campaigns | GET | offset(Int), limit(Int) | SmsCampaign[] | 401, 403 | communications |
| get_campaign_template | GET | (none) | SmsCampaignTemplate | 401, 403 | communications |
| create_sms_campaign | POST | campaignName(String), campaignType(Int), triggerType(Int), runType(Int), providerId(Long), businessRuleId(Long), templateMessage(String), recipientCriteria(String), dateFormat(String), locale(String) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| update_sms_campaign | PUT | campaignId(Long) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| activate_sms_campaign | POST | campaignId(Long), activationDate(String), dateFormat(String), locale(String) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| close_sms_campaign | POST | campaignId(Long), closureDate(String), dateFormat(String), locale(String) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| get_campaign_recipient_report | GET | campaignId(Long), status(Int) | CampaignMessage[] | 401, 403 | communications |
| list_templates | GET | typeId(Int), entityId(Int) | Template[] | 401 | communications |
| get_template_form | GET | (none) | TemplateForm | 401 | communications |
| create_template | POST | name(String), entity(Int), type(Int), mappers([]), body(String) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| list_hooks | GET | (none) | Hook[] | 401, 403 | communications |
| get_hook_template | GET | (none) | HookTemplate | 401, 403 | communications |
| create_hook | POST | name(String), isActive(Boolean), events([]), config(Object) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| get_email_config | GET | (none) | ExternalServiceConfig | 401, 403 | communications |
| save_email_config | PUT | host(String), port(Int), username(String), password(String), useTLS(Boolean), fromAddress(String) | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_/CREATE_/UPDATE_/ACTIVATE_/CLOSE_SMSCAMPAIGN, CREATE_TEMPLATE, READ_/CREATE_HOOK, READ_/UPDATE_EXTERNALSERVICES; drives control VISIBLE/DISABLED/HIDDEN + 403 drift refresh |
| offline-sync-engine | uses | true | durable outbox + idempotency key + exactly-once replay for every create/activate/close/template/hook/email command (never double-activates) |
| dynamic-template-forms | uses | true | renders the create/edit campaign, create-template, register-hook and email-config forms from the Fineract GET templates (option lists never hardcoded) |
| needs-attention-inbox | navigate | true | triage of any server-rejected campaign lifecycle or management command (Retry / Edit / Discard) with the Fineract error |

## Navigation

- **Route**: m16-communications
- **From**: nav_root (permission-gated on READ_SMSCAMPAIGN or an umbrella; fail-closed HIDE without any *_SMSCAMPAIGN)
- **To**: dynamic-template-forms, needs-attention-inbox

## App-Shell

- **Bottom navigation**: visible (module root within the back-office shell).
- **Top app bar**: visible (module header + comms-tab context).
- **FAB**: "New campaign" — HIDDEN unless CREATE_SMSCAMPAIGN is held.
- **Snackbar**: enabled (queued-offline / retry / permission-drift toasts).
- **Safe-area**: standard top-app-bar inset; the campaign/template list scrolls under the bar.

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Has entries | list returns campaigns/templates/hooks rows | content | empty ("No campaigns yet" — offers New when CREATE_* held) | fold Store5 stream → ScreenState |
| Cache vs error | offline or list fetch failed | no-network (serve cached scope) | error + Retry (no cache) | CACHE_FIRST_SWR SourceOfTruth over Room |
| Submit path | network available at command submit | submitting → content (online applied) | submitting → queued-offline (durable outbox) | RoomSubmitOutbox enqueue + idempotency key vs online POST |
| Maker-checker | command returns commandId without resourceId | pending-approval ("Submitted for approval"; status NOT flipped) → M15 | optimistic status flip (Pending→Active / Active→Closed) pending-sync | tenant maker-checker gate |
| Permission drift | activate/close/create returns 403 NoAuthorization | route to permission-capability-engine refresh + re-resolve | continue | prune now-denied controls, keep surface alive |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| content_campaign_list_and_gated_bar | The module resolves the SMS/email campaign list with each campaign's template, run type and status, and renders the permission-gated command bar | high |
| campaign_controls_gated_fail_closed | A user with READ_SMSCAMPAIGN but no create/activate/close permission sees the read surface with every mutating control HIDDEN (fail-closed) | high |
| activate_campaign_queues_exactly_once | A campaign activation issued offline is written to the durable outbox and replays exactly once (no double-activate) even across an app kill/retry | high |
| close_campaign_gated_and_authoritative | Close is hidden without permission, and a close rejected by the server (409/400) surfaces the Fineract error into Needs-Attention | high |
| create_campaign_from_template_form | New-campaign opens the dynamic-template-forms create form sourced from GET /v1/smscampaigns/template and enqueues a durable create command | medium |
| create_template_and_recipient_report | A message template is authored via the template form and a campaign's recipient delivery report is read grouped by delivery status | medium |
| register_hook_and_edit_email_config | A webhook is registered and the outbound email (SMTP) gateway config is edited; the password field is masked and never logged | medium |
| offline_shows_cached_campaigns | Offline, the campaign list + templates + hooks show last cached values with an offline banner | medium |
