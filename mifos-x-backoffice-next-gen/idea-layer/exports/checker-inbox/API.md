<!-- source: screens/checker-inbox/api.yaml -->
<!-- source_hash: api=4cf3bfeadb4e -->
<!-- generated: 2026-07-31T03:19:28Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# Maker-Checker Inbox (self-scoped approvals queue) -- API Reference

> **Source of Truth**: `idea-layer/screens/checker-inbox/api.yaml` -> `endpoints[]`
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0

## Endpoints (2)

| Function | Method | Table | Auth | Params | Response | Cache |
|----------|--------|-------|------|--------|----------|-------|
| list_maker_checker_entries | GET | cached_maker_checkers | Yes | entityName(String)?, actionName(String)?, makerDateTimeFrom(String)?, offset(Integer)?, limit(Integer)? | MakerCheckerEntryDto[]: auditId, actionName, entityName, maker, madeOnDate, commandAsJson | CACHE_FIRST_SWR |
| decide_maker_checker_entry | POST | draft_outbox | Yes | auditId(Long), command(String: approve\|reject\|delete) | CommandProcessingResultDto: resourceId, commandId, changes{} | durable-outbox |

## Error Handling

All endpoints follow standard error mapping:
- 401 -> Auth error (redirect to signin)
- 403 -> NoAuthorization -> permission-capability-engine drift refresh + re-resolve (the checker gate; controls hidden on the row)
- 404 -> NotFound error (the entry was already resolved by another checker)
- 409 -> Optimistic-lock conflict -> row refreshed, decision re-offered
- 500 -> Server error (retryable; error state + Retry)

Notes:
- `list_maker_checker_entries` is a read — CACHE_FIRST_SWR: the self-scoped queue browses instantly from cache and a background revalidate refreshes it, so a checker works fully offline.
- `decide_maker_checker_entry` is enqueued through the offline outbox with a durable UUIDv4 Idempotency-Key and replayed all-or-nothing via `POST /v1/batches` — exactly-once, zero double-decision. Reject/Delete are confirm-gated and always audited.
- Decision controls are exposed per-row only where `canCheck(action,entity)` (or CHECKER_SUPER_USER) resolves true; the server is the authoritative checker gate.

## Full Contracts

See `idea-layer/screens/checker-inbox/api.yaml` -> `endpoints[]` for complete request/response schemas.
