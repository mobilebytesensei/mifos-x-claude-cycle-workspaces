<!-- source: screens/path-tracking/ (v4.0 siblings: ui, docs, flow, demo-data) -->
<!-- generated: 2026-08-01T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->

# Path Tracking — Implementation Specification

> **Source of Truth**: `idea-layer/screens/path-tracking/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: enriched

---

## Capabilities

- **has_ui** — Compose detail screen with maps-compose / MapKit polyline rendering (archetype: detail_screen).
- **has_flow** — idle → recording → paused → idle; idle → viewing; permission_required gate.
- **has_offline_cache** — All GPS session data in Room; no network dependency; fully offline.
- **has_local_preferences** — `multiplatform_settings` for `tracking_active_session_id` + `tracking_recording` (nav-shell badge + notification state).
- **has_system_integration** — KMP expect/actual `LocationProvider` (Android `LocationManager` / iOS `CLLocationManager`); persistent notification during active recording; Room database for waypoints.
- **requires_auth** — Post-login only; gated to field-officer personas via `READ_COLLECTIONSHEET`.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| path-tracking | PathTrackingViewModel | Idle, Recording, Paused, Viewing, Permission_Required, Error | GPS field-visit route trail. Reads completed sessions from Room (`path_tracking_sessions`) as a Flow for the history list. Recording appends GPS waypoints (~5s) to `path_tracking_points` via background `LocationProvider`. Renders live trail (recording/paused) or historical route (viewing) as a map polyline. Gated: Tier 1 hide-root (READ_COLLECTIONSHEET) at nav shell; Tier 3 gray-out-info-dialog on all recording actions. Fully offline — no Fineract API calls. Archetype: detail_screen. |

## State Model

### PathTrackingViewModel

- **State**: sessions (List<PathTrackingSession>), activeSession (PathTrackingSession?), elapsedSeconds (Long), distanceKm (Double), currentSpeedKmh (Double), activeRoutePoints (List<LatLng>), selectedSession (PathTrackingSession?), selectedRoutePoints (List<LatLng>), locationPermissionGranted (Boolean)
- **ScreenState**: Idle, Recording, Paused, Viewing, Permission_Required, Error
- **Errors**: location_permission_denied (Permission_Required state — "Location access required. Grant permission in device settings." + open_settings_button), gps_signal_lost (snackbar "GPS signal lost — route paused. Move to an open area and tap Retry." + session status=paused; session stays open in Room), storage_full (snackbar "Device storage full — older route sessions will be deleted to make room." + oldest sessions pruned automatically)
- **Events**: (in-screen state machine; no navigation events except back)
- **Actions**: OnStartTrackingClick (start_tracking → permission check → LocationProvider start → new PathTrackingSession in Room → tracking_active_session_id persisted), OnPauseTrackingClick (pause_tracking → LocationProvider pause → session status=paused in Room), OnResumeTrackingClick (resume_tracking → LocationProvider resume → status=recording), OnStopTrackingClick (stop_tracking → LocationProvider stop → session status=completed, ended_at=now → clear tracking_active_session_id → Idle), OnSessionRowClick (view_session → load selectedRoutePoints from Room → Viewing state), Internal.ReceiveLocationUpdate (append new PathTrackingPoint to Room + update activeRoutePoints + update elapsedSeconds/distanceKm/currentSpeedKmh), OnRetryClick (re-initialize LocationProvider; session stays open)
- **DI**: core/datastore

## API Endpoints

*(None — entirely offline GPS trail. No Fineract calls.)*

## Local Store Schema

| Table | Backend | Columns | Notes |
|-------|---------|---------|-------|
| path_tracking_sessions | Room | session_id, started_at, ended_at, status (recording/paused/completed), distance_km, duration_secs, waypoints_count | Ordered by started_at DESC. Shown in history list. Survives app kill (Room is durable). |
| path_tracking_points | Room | id, session_id, lat, lng, accuracy_m, speed_kmh, timestamp | Appended every ~5s by LocationProvider. Used to render the polyline. Pruned automatically when device storage is full (oldest sessions deleted). |
| app_settings (tracking keys) | multiplatform_settings | tracking_active_session_id, tracking_recording | Quick-access flags for nav-shell badge and OS notification. |

## Permission-Gating (§4.7)

| Gate Tier | Required Permission | Denied Behavior | Applied to |
|-----------|--------------------|-----------------|---------__|
| Tier 1 — nav root | READ_COLLECTIONSHEET | **hide-root** — path-tracking absent from nav roster for admin/super-user without field-ops code | nav-shell-assembler module roster |
| Tier 3 — action | READ_COLLECTIONSHEET | **gray-out + info dialog** ("You don't have permission to use Path Tracking. Contact your manager.") | session_row (view_session), start_button (start_tracking), pause_button (pause_tracking), resume_button (resume_tracking), stop_button (stop_tracking) |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| fineract-auth-session | requires | true | authenticated user context |
| permission-capability-engine | gates | true | READ_COLLECTIONSHEET evaluation for hide-root at nav-shell and gray-out at action level |
| nav-shell-assembler | consumed_by | true | nav root entry point gated by READ_COLLECTIONSHEET |

## Navigation

- **Route**: path-tracking
- **From**: nav-shell-assembler drawer root (loan-officer-maker / collections-field-officer personas with READ_COLLECTIONSHEET)
- **To**: self (state machine — idle ↔ recording ↔ paused ↔ viewing ↔ permission_required ↔ error); back navigation to the previous screen

## App-Shell

- **Bottom navigation**: hidden (field detail screen; not a bottom-tab module).
- **Top app bar**: visible (title "Path Tracking" + back arrow).
- **FAB**: none.
- **Snackbar**: enabled — gps_signal_lost / storage_full during recording.

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Location permission | OS location permission granted | idle (history list + Start button) | permission_required state (Open Settings button; Start hidden) | ActivityCompat.checkSelfPermission / CLLocationManager.authorizationStatus |
| Session history | Room sessions Flow emits ≥1 completed session | idle with history list | idle with empty history (no sessions yet; Start button still visible) | Room Flow<List<PathTrackingSession>> |
| Start tracking path | Start tapped with permission granted | new PathTrackingSession opened in Room; LocationProvider started; Recording state | (permission_required shown if not yet granted) | OnStartTrackingClick handler |
| GPS signal | LocationProvider emits updates without error | points appended to Room (~5s); polyline grows; elapsed/distance updated | gps_signal_lost snackbar; session status=paused; Error state + Retry | LocationProvider error callback |
| Stop tracking | stop_tracking tapped (recording or paused) | session.status=completed; ended_at=now; clear tracking_active_session_id; transition to Idle; route appears in history | — (one path only) | OnStopTrackingClick handler |

## Testing (5 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| record_and_stop_route | Field officer opens path-tracking → Start → location permission granted → records 47 min in Nairobi → Stop → session saved in Room → appears in history list | high |
| pause_and_resume | Recording → Pause (at Westlands Group Center) → session status=paused; map frozen → Resume → recording resumes from paused position → Stop → route saved | high |
| view_historical_route | Tap history row (pts-2026-07-31-001) → static polyline rendered for full Nairobi field day (17.4 km, 4697 waypoints, SW/NE bounds correct) | high |
| permission_denied_flow | Open path-tracking without location permission → permission_required state → Open Settings → grant permission → return → idle state shown | medium |
| admin_root_hidden | Super user (no READ_COLLECTIONSHEET field-ops code) → path-tracking absent from nav drawer (hide-root) | high |
