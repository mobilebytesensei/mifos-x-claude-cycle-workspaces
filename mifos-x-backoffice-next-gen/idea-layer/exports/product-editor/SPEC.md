<!-- source: screens/product-editor/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=c1ce2551d126 docs=badac140d422 flow=22a85c6427a2 api=d8e8ad49be1a -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Product Editor (loan/savings product create & edit) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/product-editor/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: draft

---

## Capabilities

- **has_ui** — Compose sectioned form: Details · Terms · Settings · Charges · Accounting, every field/bound/option driven by the Fineract product template.
- **has_offline_cache** — product + template read CACHE_FIRST_SWR through Store5; the form is editable/draftable offline.
- **has_flow** — drilled in from the M08 products list (edit) or the 'new product' affordance (create); loading → content/empty/error, save enqueues to the outbox.
- **requires_auth** — reads + save run under the authenticated Fineract session, gated by READ_LOANPRODUCT / (CREATE|UPDATE)_LOANPRODUCT.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| product-editor | ProductEditorViewModel | loading, content, empty, error | The loan/savings product create-and-edit surface drilled in from the M08 products & charges list. Loads GET /v1/loanproducts/{productId} (or /v1/savingsproducts/{id}) merged with GET /v1/loanproducts/template into a sectioned form — Details (name, short name, currency) · Terms (principal min/default/max, interest rate & method, repayment frequency) · Settings (amortisation, interest-calc period, arrears tolerance) · Charges · Accounting (rule + GL mappings). Every field, bound and option comes from the Fineract template, so the form always matches the server's product schema. Save posts PUT (edit) / POST (create) through the offline outbox with a durable idempotency key. Admin-only. Archetype: form. |

## State Model

### ProductEditorViewModel

- **State**: product (Product? — the loaded product being edited; null when creating), template (ProductTemplate? — Fineract template driving field options, bounds, accounting rules), form (ProductForm — captured values across Details/Terms/Settings/Charges/Accounting), expandedSection (ProductSection? — currently expanded section), canEdit (Boolean — PermissionSet holds CREATE/UPDATE product; else read-only), uiState (ProductEditorScreenState)
- **ScreenState**: loading, content, empty, error
- **Errors**: template_load_failed (retry=true; error state — template failed with no cache), product_load_failed (retry=true; error state — product failed with no cache), validation_failed (a field fails template validation; Save disabled until the whole form is valid), save_failed (retry=true; error state — save failed hard, form preserved for re-attempt)
- **Events**: (none — save resolves in-place; the product list reflects the change after the next sync)
- **Actions**: OnLoadTemplate (load product + template cache-first), OnFieldChange (field_change → in-VM value update + re-run template validators, gate Save), OnSectionExpand (expand_section → in-VM section toggle, no refetch), OnAddCharge (attach a charge from the template charge options), OnSaveProduct (save_product → PUT/POST enqueued to draft_outbox), OnRetry (retry → reload product + template / re-attempt save)
- **DI**: core/store, core/data, core/database, core/permissions, core/network

## API Endpoints (3)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| get_product | GET | productId(Long) | LoanProductDto | 401, 403 | cached_loan_products |
| get_product_template | GET | (none) | LoanProductTemplateDto | 401, 403 | cached_loan_product_templates |
| save_product | PUT | productId(Long), body(product form) | CommandProcessingResultDto | 401, 403, 409 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_LOANPRODUCT (read) + CREATE/UPDATE_LOANPRODUCT (Save visibility, read-only otherwise) |
| offline-sync-engine | uses | true | durable outbox + idempotency key + POST /v1/batches replay for exactly-once product save |
| fineract-auth-session | requires | true | authenticated session required for the product + template reads and save |
| dynamic-template-forms | uses | true | per-section fields render from the Fineract product template via the shared template-form engine |

## Navigation

- **Route**: product-editor(productId?)
- **From**: m08-products-charges (open_product_detail row, new-product affordance)
- **To**: (none — save resolves in-place; back returns to M08)

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Product resolves | the product + template resolve (cache-first) | content | (see empty/error) | Store5 CACHE_FIRST_SWR over GET /v1/loanproducts/{id} + template |
| Create mode | only the template loaded, no product | empty → content (sections default from template) | content (edit) | template-defaulted create form |
| Load hard-fail | product or template failed AND no cache | error (message + Retry) | content | GET failure with empty cache |
| Edit gate | PermissionSet holds CREATE/UPDATE product | editable + Save shown | read-only (disabled fields, no Save) | PermissionEvaluator gate |
| Save | Save tapped (edit permission held, form valid) | content — enqueued to draft_outbox | error (hard fail) | PUT/POST via outbox; list reflects change after sync |
| Retry | 'Retry' tapped in error | loading → reload / re-attempt save | error | reload product + template |

## Testing (7 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_sectioned_shimmer | The loading state shows a sectioned-form shimmer with no error | medium |
| content_shows_sections_from_template | Details/Terms/Settings/Charges/Accounting sections; every field/bound/option from the template; Save enabled only when valid | high |
| empty_create_mode_defaults_from_template | Create mode defaults all sections from the template with no product values pre-filled | low |
| error_when_load_or_save_fails | The error state is shown and Retry reloads / re-attempts save | medium |
| read_only_without_edit_permission | All fields render disabled and no Save action shows when the user lacks CREATE/UPDATE_LOANPRODUCT | high |
| save_enqueues_through_outbox | Save enqueues with a durable key; sync replays PUT (or POST for create); the product list reflects the change | high |
| template_validation_blocks_invalid_save | A principal default above the template max shows a validation error and keeps Save disabled | medium |
