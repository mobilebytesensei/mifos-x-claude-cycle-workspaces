<!-- source: screens/dynamic-template-forms/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=1d183707e72c docs=326ea05e63be flow=c66bd8333030 api=cb0287e91ae2 -->
<!-- generated: 2026-07-21T18:22:47Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Dynamic Template Forms (Fineract entity template + datatable renderer) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/dynamic-template-forms/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose form_screen that renders each field from the resolved server schema.
- **has_api** — Fineract GET /{entity}/template + GET /v1/datatables reads and POST /{entity} command submit.
- **has_offline_cache** — CACHE_FIRST_SWR template + datatable schemas cached in Room; forms render fully offline.
- **has_form_input** — per-field text / select / date capture with template-driven validators.
- **has_data_migration** — a back-office datatable/config change surfaces on the next sync with no app release.
- **requires_auth** — every read + submit is authenticated; datatable read gated by READ_DATATABLE.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| dynamic-template-forms | DynamicTemplateFormsViewModel | template-loading, content, validation-error, submitting, queued-offline, error | Foundation form engine: renders a create/edit form for ANY Fineract entity purely from GET /{entity}/template (field defaults + allowed-value option lists) joined with the entity's registered /v1/datatables schemas — no field hardcoded. Templates + datatable schemas cached in Room (CACHE_FIRST_SWR) so forms work offline; submits assemble a command JsonObject routed through the durable outbox for exactly-once batch replay. Archetype: form_screen. |

## State Model

### DynamicTemplateFormsViewModel

- **State**: entity (String), command (String), fields (List<TemplateField>), values (Map<String, JsonElement>), fieldErrors (Map<String, String>), fromCache (Boolean)
- **ScreenState**: template-loading, content, validation-error, submitting, queued-offline, error
- **Errors**: template_fetch_failed (retry=true; error state, no partial form), datatable_schema_fetch_failed (degrade — base template renders, datatable groups hidden + re-sync hint), field_validation_failed (validation-error, submit blocked until offending fields cleared), command_rejected (server 400/409 errors[] mapped back onto the offending field by parameterName)
- **Events**: NavigateToCreatedResource(resourceId) → caller screen (on 2xx CommandProcessingResult), NavigateToNeedsAttention → needs-attention-inbox (a FAILED outbox replay surfaces for triage)
- **Actions**: EditTextField (edit_text_field → update values[fieldCode], run declared validators, recompute canSubmit), SelectOption (select_option → set values[fieldCode] to chosen option id, re-derive cascading option lists), PickDate (pick_date → store date + dateFormat + locale, validate min/max), SubmitForm (submit_form → serialize command JsonObject, enqueue to durable outbox), RetryTemplateFetch (retry_template_fetch → re-fetch template + datatables, refresh cache, return to content)
- **DI**: core/network, core/database, core/data

## API Endpoints (3)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| get_entity_template | GET | clientId(Long)?, productId(Long)?, templateType(String)?, staffInSelectedOfficeOnly(Boolean)? | EntityTemplate | 401 | cached_templates |
| get_datatables | GET | apptable(String)? | DatatableSchema[] | 401, 403 | cached_datatable_schemas |
| submit_entity_command | POST | command(String)?, JsonObject(body) | CommandProcessingResult | 401, 400, 409 | framework_submit_drafts |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| offline-sync-engine | uses | true | durable outbox + idempotency key + batch replay for exactly-once command submit |
| permission-capability-engine | gates | true | resolves READ_DATATABLE for the datatable read; the concrete {ACTION}_{ENTITY} gate is inherited from the caller screen |
| needs-attention-inbox | navigate | true | a FAILED outbox replay surfaces here for triage (never silently dropped) |

## Navigation

- **Route**: dynamic-template-forms(entity, command)
- **From**: embedded — invoked by any create/edit surface (client onboarding M02, loan/savings wizards M04/M05, product editor M08, datatable rows M12); the caller passes the target entity + optional command and inherits the permission gate
- **To**: needs-attention-inbox

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Schema resolves | GET template + datatables returns (fresh fetch or offline cache hit) | content | error (no cached schema exists) | CACHE_FIRST_SWR SourceOfTruth over Room cached_templates |
| Datatable degrade | GET /v1/datatables fails but base template present | content — datatable groups hidden + re-sync hint | full form with datatable groups | partial-schema render, base template unaffected |
| Validation gate | fieldErrors non-empty at submit | validation-error (submit blocked, offending fields highlighted) | proceed to submit | client validators resolved from TemplateField.mandatory/length/range/regex |
| Submit path | connectivity available for an online-only entity | submitting → content on 2xx | queued-offline (durable outbox, idempotency key) | POST /{entity}?command={action} vs outbox enqueue for POST /v1/batches replay |
| Server outcome | submit returns CommandProcessingResult | content (commandId without resourceId = checker-queued badge) | error — server errors[] mapped to field by parameterName (400 validation / 409 conflict) | CommandProcessingResult + maker-checker handling |

## Testing (13 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| form_rendered_from_template | Base template fields + registered datatable groups render from the server schema | high |
| options_bound_and_cascade | Selecting an option updates state and re-derives cascading option lists | high |
| date_field_carries_format_and_locale | A date field emits dateFormat + locale per the Fineract mutation contract | medium |
| validation_blocks_submit | Client-side validation gates submit until fields are valid | high |
| submit_online_returns_command_result | A clean submit with connectivity posts the command and returns CommandProcessingResult | high |
| submit_offline_enqueues_to_outbox | Submitting offline queues the command with a durable idempotency key | high |
| config_change_appears_next_sync_no_release | A back-office datatable/config change surfaces without an app release | high |
| form_works_offline_from_cache | A previously cached template renders offline | high |
| error_template_fetch_failure_no_cache | Template fetch failure with no cache shows a recoverable error | medium |
| server_rejects_maps_error_to_field | A 400 validation body maps back onto the offending field | medium |
| numeric_range_validation_blocks_out_of_bounds | A numeric (Integer/Decimal) field enforces the template min/max value bound | medium |
| allowed_value_validation_on_select | A single-select field rejects a value absent from the template allowed-value list | medium |
| length_and_regex_validation_on_text | A text field enforces the template mandatory + length (and optional regex) rules | medium |
