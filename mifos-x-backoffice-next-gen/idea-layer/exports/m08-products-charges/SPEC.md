<!-- source: screens/m08-products-charges/ (ui+api+flow+docs+data-flow+tests) -->
<!-- source_hash: ui=2486941814cc docs=bfde2ee1cc8b flow=c4554bec5bef api=7f871a7a9e15 -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M08 · Products & Charges -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m08-products-charges/`
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| M08 · Products & Charges (`m08-products-charges`, archetype `master_detail`) | ProductsChargesViewModel | template-loading, content, empty, submitting, config-online-only, error | Tabbed product/charge control plane (Loan / Savings / FD / Share products, Charges, Funds, Tax, Floating rates, Delinquency buckets) → per-item detail → a create/edit surface rendered ENTIRELY by dynamic-template-forms from GET /{entity}/template. Catalog + templates read CACHE_FIRST_SWR (offline); every mutating command is permission-gated and ONLINE-ONLY + maker-checker. |

## Capabilities

### product_charge_catalog_read
`Store<PageKey,List<CatalogItem>>` over Room (`ProductChargeEntity`, unified by `entity_type`) + `asPagingScreenStream(CACHE_FIRST_SWR)`; GET /v1/loanproducts + /v1/savingsproducts + /v1/charges + /v1/funds + /v1/taxes/group; renders all ScreenState variants via `PagingScreenContent`; cached rows also supply loan/savings origination option lists offline. Permission `READ_LOANPRODUCT`. Errors: `product_catalog_failed`, `product_catalog_offline_no_cache`.

### entity_template_read
GET /v1/{loanproducts|savingsproducts|charges|taxes/group}/template — the largest template envelopes in the API; cached per `(entity, command)` in `cached_templates` (CACHE_FIRST_SWR); rendered by dynamic-template-forms. Permission `READ_LOANPRODUCT`. Errors: `product_template_failed`.

### product_create_edit
POST /v1/{loanproducts|savingsproducts} + PUT /v1/{...}/{id} — assembled `JsonObject` from dynamic-template-forms; ONLINE-ONLY (config never offline-queued) through `DraftSubmitHandler` with a durable idempotency key; maker-checker aware (queued commandId + empty changes → PENDING_APPROVAL); offline attempt → `config-online-only` blocked state. Permission `CREATE_LOANPRODUCT`. Errors: `config_command_rejected`, `config_online_only_blocked`, `permission_drift_403`, `command_pending_approval`.

### product_delete
DELETE /v1/{loanproducts|savingsproducts}/{id} — destructive confirm; server 409-rejects when accounts reference the product (surfaced verbatim); ONLINE-ONLY + maker-checker. Permission `DELETE_LOANPRODUCT`. Errors: `product_in_use_409`, `config_command_rejected`.

### charge_create_edit
POST /v1/charges + PUT /v1/charges/{id} (incl. active toggle) — assembled `JsonObject` (applies-to, calc type, amount, GL income account, tax group); ONLINE-ONLY + maker-checker; the created charge feeds product charge-assignment option lists. Permission `CREATE_CHARGE`. Errors: `config_command_rejected`, `config_online_only_blocked`.

### fund_create
POST /v1/funds — name + externalId; ONLINE-ONLY + maker-checker; feeds loan-product/loan-account fund option lists. Permission `CREATE_FUND`. Errors: `config_command_rejected`.

### tax_group_create
POST /v1/taxes/group — composed from GET /v1/taxes/group/template tax components (percentage → GL liability account, dated); ONLINE-ONLY + maker-checker; feeds charge tax-group option lists. Permission `CREATE_TAXGROUP`. Errors: `config_command_rejected`.

## State Model

### ProductsChargesViewModel

- **State**: `activeTab: CatalogTab`, `catalog: List<CatalogItem>` (in-memory filtered working set), `searchQuery: String`, `uiState: ProductsChargesUiState`
- **ScreenState**: Loading (`template-loading`), Success(`pagedCatalog`) (`content`), Empty (`empty`), Submitting (`submitting`), ConfigOnlineOnly (`config-online-only`), Error (`error`)
- **Errors**: `product_catalog_failed`, `product_catalog_offline_no_cache`, `product_template_failed`, `config_command_rejected`, `config_online_only_blocked`, `product_in_use_409`, `permission_drift_403`, `command_pending_approval`
- **Events**: `NavigateToProductEditor(entityType, id)`
- **Actions**: `LoadCatalog()` → catalogStream(CACHE_FIRST_SWR); `SwitchCatalogTab(tab)` → in-memory re-filter; `SearchCatalog(query)` → in-memory filter; `OpenProductDetail(entityType, id)` → NavigateToProductEditor; `CreateLoanProduct(payload)` / `CreateSavingsProduct(payload)` / `CreateCharge(payload)` / `CreateFund(payload)` / `CreateTaxGroup(payload)` → enqueueOnline + idempotencyKey; `EditProduct(entityType, id, payload)` / `EditCharge(id, payload)` / `ToggleChargeActive(id, active)` → enqueueOnline(UPDATE_*); `DeleteProduct(entityType, id)` → enqueueOnline(DELETE_*); `DismissConfigOfflineBanner()` → back to content; `RetryLoad()` → refreshFresh()
- **DI**: `ProductChargeRepository`, `ProductOutboxRepository`, `cmp-network-monitor`; required modules `core/store`, `core/data`, `core/network`, `core/database`, `core/permissions`

## API Endpoints (20)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_loan_products | GET | — | LoanProduct[] | 401 | products_charges |
| get_loan_product_template | GET | — | LoanProductTemplate | 401 | cached_templates |
| create_loan_product | POST | body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| update_loan_product | PUT | id(String), body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| delete_loan_product | DELETE | id(String) | CommandProcessingResult | 401, 409, 500 | framework_submit_drafts |
| list_savings_products | GET | — | SavingsProduct[] | 401 | products_charges |
| get_savings_product_template | GET | — | SavingsProductTemplate | 401 | cached_templates |
| create_savings_product | POST | body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| update_savings_product | PUT | id(String), body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| list_charges | GET | — | Charge[] | 401 | products_charges |
| get_charge_template | GET | — | ChargeTemplate | 401 | cached_templates |
| create_charge | POST | body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| update_charge | PUT | id(String), body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| list_funds | GET | — | Fund[] | 401 | products_charges |
| create_fund | POST | body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| list_tax_groups | GET | — | TaxGroup[] | 401 | products_charges |
| get_tax_group_template | GET | — | TaxGroupTemplate | 401 | cached_templates |
| create_tax_group | POST | body(JsonObject) | CommandProcessingResult | 401, 422, 500 | framework_submit_drafts |
| list_floating_rates | GET | — | FloatingRate[] | 401 | products_charges |
| list_delinquency_buckets | GET | — | DelinquencyBucket[] | 401 | products_charges |

## Dependencies (Tier 1 — foundation)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | feature | true | Gates every mutating control ({ACTION}_{ENTITY}); HIDE for create FABs, DISABLE-with-reason for makers; 403-drift routing |
| offline-sync-engine | feature | true | DraftSubmitHandler durable idempotency-key outbox (framework_submit_drafts) — online-only submit, never silent replay for config |
| dynamic-template-forms | feature | true | Renders every create/edit surface from GET /{entity}/template allowed-value lists; never hardcoded |
| needs-attention-inbox | feature | true | Rejected maker-checker config submissions surface here (Retry / Edit / Discard); never silently dropped |

## Navigation

- **Route**: m08-products-charges (nav root; permission-gated on the product/charge group)
- **From**: nav_root (module root — visible only with ≥1 of READ_LOANPRODUCT / READ_SAVINGSPRODUCT / READ_CHARGE / READ_FUND / READ_TAXGROUP)
- **To**: product-editor (per-item detail + template-driven create/edit surface)

## App-Shell

- **Bottom navigation**: visible (module reached from a permission-filtered nav root)
- **Top app bar**: visible (title `{strings.m08_title}`)
- **FAB**: present — one create FAB per family (loan-product / savings-product / charge / fund / tax-group), each HIDE-gated on its CREATE_ permission
- **Snackbar**: visible (submit / reject / offline-blocked feedback)

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| catalog resolve | GET catalog (+ open form template) succeeds or cache hit | template-loading → content (or empty if active tab has 0 items) | template-loading → error (no cache) | Store5 CACHE_FIRST_SWR stream |
| config write connectivity | connectivity available at submit time | content → submitting (POST/PUT/DELETE online) | content → config-online-only (blocked, never queued) | cmp-network-monitor gate; DraftSubmitHandler online-only |
| create-control permission | user holds tab's CREATE_ permission | FAB / empty CTA rendered | FAB / CTA HIDDEN (fail-closed) | permission-capability-engine on_denied=HIDE |
| edit/delete permission | user holds UPDATE_/DELETE_ action | control enabled | control DISABLED-with-reason | permission-capability-engine on_denied=DISABLE |
| submit outcome | server returns 2xx CommandProcessingResult | submitting → content (or Submitted-for-approval badge under maker-checker) | submitting → error (400/409 verbatim; reject → needs-attention-inbox) | maker-checker aware; 409 product-in-use surfaced verbatim |

## Testing (10 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| catalog_renders_offline_from_cache | The product/charge catalog renders CACHE_FIRST_SWR and feeds downstream form option lists offline | high |
| create_loan_product_from_large_template | The loan-product create form renders from the large GET /v1/loanproducts/template and submits online | high |
| create_charge_appears_in_product_lists | A newly created charge becomes selectable in product charge-assignment lists | high |
| create_gated_hidden_without_permission | Create FABs are fail-closed HIDDEN for a persona lacking the CREATE_ permission | high |
| edit_disabled_with_reason_for_maker | Edit/Delete on a visible detail are DISABLE-with-reason for a user lacking the action | high |
| config_edit_blocked_offline | A product/charge config write is BLOCKED offline (config is online-only, never queued) | high |
| delete_product_referenced_rejected | Deleting a product still referenced by accounts is 409-rejected and surfaced verbatim | medium |
| maker_checker_submit_routes_to_inbox_on_reject | A rejected maker-checker config submission surfaces in Needs-Attention, never silently dropped | medium |
| empty_tab_shows_gated_cta | An empty catalog tab shows a create CTA gated on the tab's CREATE_ permission | medium |
| error_retry_refetches_catalog | A catalog fetch failure shows a recoverable error and Retry refetches | medium |
