<!-- source: screens/loan-application-wizard/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=126f8d53340f docs=a2fb196147a3 flow=178afafa3594 api=65a5e31881c0 -->
<!-- generated: 2026-07-31T03:19:28Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Loan Application Wizard (product → terms → schedule → review) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/loan-application-wizard/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: designed

---

## Capabilities

- **has_ui** — Compose multi-step form: a Product → Charges → Schedule → Review stepper rendered entirely from the Fineract loan template.
- **has_offline_cache** — the loan template reads CACHE_FIRST_SWR through Store5; the application is draftable offline.
- **has_flow** — entered from M04 / client-detail-360; loading → content/empty/error, Submit lands on loan-detail.
- **requires_auth** — template read, schedule preview and submit run under the authenticated Fineract session, gated by CREATE_LOAN.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| loan-application-wizard | LoanWizardViewModel | loading, content, empty, error | The multi-step new-loan wizard entered from M04 'start application'/'disburse' and client-detail-360's Accounts tab. Step 1 loads the loan product template (GET /v1/loans/template) and captures product, principal, term, interest and disbursement date; step 2 attaches charges/collateral; step 3 previews the amortised schedule (POST /v1/loans?command=calculateLoanSchedule); step 4 reviews and submits (POST /v1/loans) through the offline outbox with a durable idempotency key. Field metadata comes entirely from the Fineract template, so a back-office product change appears next sync with no release. On success the wizard lands on the newly created loan's detail. Archetype: form. |

## State Model

### LoanWizardViewModel

- **State**: template (LoanTemplate? — allowed products, principal/term bounds, charges, defaults), form (LoanApplicationForm — product, principal, term, interest, disbursement date, charges, collateral), step (WizardStep — Product | Charges | SchedulePreview | Review), schedulePreview (List<RepaymentPeriod> — previewed amortised schedule), uiState (LoanWizardScreenState)
- **ScreenState**: loading, content, empty, error
- **Errors**: template_load_failed (retry=true; error state — template failed with no cache), validation_failed (a step fails template validation; Next disabled until corrected), schedule_calc_failed (retry=true; preview call failed hard), submit_failed (retry=true; submit failed hard, form preserved for re-attempt)
- **Events**: NavigateToLoanDetail(loanId) → loan-detail (Submit succeeds on the newly created loan)
- **Actions**: OnLoadTemplate (load loan product template cache-first), OnFieldChange (field_change → in-VM value update + re-run template validators, gate Next), OnNextStep (next_step → advance when the step validates), OnPrevStep (retreat a step), OnPreviewSchedule (preview_schedule → POST …?command=calculateLoanSchedule, render installments), OnSubmit (submit_application → POST /v1/loans enqueued to draft_outbox, navigate loan-detail), OnRetry (retry → reload template / re-attempt the failed call)
- **DI**: core/store, core/data, core/database, core/permissions, core/network

## API Endpoints (3)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| get_loan_template | GET | clientId(Integer)?, groupId(Integer)?, productId(Integer)?, templateType(String)? | LoanTemplateDto | 401, 403 | cached_loan_templates |
| calculate_loan_schedule | POST | command(String), body(captured terms) | LoanScheduleDto | 401, 403, 400 | (none — live preview) |
| submit_loan_application | POST | body(completed application) | CommandProcessingResultDto | 401, 403, 409 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves CREATE_LOAN for the wizard entry + the Review-step Submit |
| offline-sync-engine | uses | true | durable outbox + idempotency key + POST /v1/batches replay for exactly-once submit |
| fineract-auth-session | requires | true | authenticated session required for the template read, schedule preview and submit |
| dynamic-template-forms | uses | true | per-step fields render from the Fineract loan template via the shared template-form engine |

## Navigation

- **Route**: loan-application-wizard(clientId, productId?)
- **From**: m04-loan-portfolio (start-application / disburse actions), client-detail-360 (apply-for-loan affordance)
- **To**: loan-detail (Submit success)

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Template resolves | the loan product template resolves (cache-first) | content | (see empty/error) | Store5 CACHE_FIRST_SWR over GET /v1/loans/template |
| Ineligible | the client is ineligible for any product | empty (guidance panel) | content | no template products returned |
| Template hard-fail | template failed AND no cache available | error (message + Retry) | content | GET failure with empty cache |
| Step validation | current step passes template validation | Next enabled → advance | Next disabled | in-VM validators from template bounds |
| Preview | Preview schedule tapped | POST …?command=calculateLoanSchedule → installments render | content | live schedule calc, no idempotency |
| Submit | Submit tapped on Review (CREATE_LOAN held) | content — enqueued to draft_outbox → navigate loan-detail on success | error (hard fail) | POST /v1/loans via outbox |

## Testing (7 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_form_shimmer | The loading state shows a form shimmer with no error | medium |
| content_shows_stepper_and_template_fields | A Product/Charges/Schedule/Review stepper; fields + bounds come entirely from the template; Next enabled only when the step validates | high |
| empty_when_client_ineligible | A guidance panel explaining no eligible product when the client is ineligible | low |
| error_when_template_or_step_fails | The error state is shown and Retry reloads the template / re-attempts the failed call | medium |
| preview_schedule_before_submit | Preview schedule issues ?command=calculateLoanSchedule and renders installments/interest/fees for review | high |
| submit_enqueues_and_lands_on_loan_detail | Submit enqueues with a durable key; sync replays POST /v1/loans; on success it navigates to the new loan's detail | high |
| draftable_offline | The form renders from the cached template offline and the application can be drafted and queued for replay | medium |
