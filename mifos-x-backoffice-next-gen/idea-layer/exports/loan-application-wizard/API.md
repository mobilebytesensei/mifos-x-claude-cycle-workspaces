<!-- source: screens/loan-application-wizard/api.yaml -->
<!-- source_hash: api=bd324dda890a -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# Loan Application Wizard (product → terms → schedule → review) -- API Reference

> **Source of Truth**: `idea-layer/screens/loan-application-wizard/api.yaml` -> `endpoints[]`
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0

## Endpoints (3)

| Function | Method | Table | Auth | Params | Response | Cache |
|----------|--------|-------|------|--------|----------|-------|
| get_loan_template | GET | cached_loan_templates | Yes | clientId(Integer)?, groupId(Integer)?, productId(Integer)?, templateType(String: individual\|jlg)? | LoanTemplateDto: productOptions[], principal min/max, termBounds, interest defaults, chargeOptions[] | CACHE_FIRST_SWR |
| calculate_loan_schedule | POST | -- | Yes | command(String: calculateLoanSchedule), body(captured terms) | LoanScheduleDto: periods[] (dueDate, principalDue, interestDue, feeChargesDue, totalDue) | -- (live preview) |
| submit_loan_application | POST | draft_outbox | Yes | body(completed application) | CommandProcessingResultDto: resourceId (loanId), commandId, changes{} | durable-outbox |

## Error Handling

All endpoints follow standard error mapping:
- 401 -> Auth error (redirect to signin)
- 403 -> NoAuthorization -> permission-capability-engine drift refresh + re-resolve (CREATE_LOAN)
- 400 -> Validation error -> server errors[] mapped back onto the offending field by parameterName
- 409 -> Conflict -> application refreshed, submit re-offered
- 500 -> Server error (retryable; error state + Retry)

Notes:
- `get_loan_template` is a read — CACHE_FIRST_SWR: the entire wizard field set (products, bounds, charge options) comes from the template, so a back-office product change appears on the next sync with no app release, and the form drafts offline.
- `calculate_loan_schedule` is a live preview (no idempotency) issued before commit so the officer reviews installments, interest and fees.
- `submit_loan_application` is enqueued through the offline outbox with a durable UUIDv4 Idempotency-Key and replayed via the transactional batch endpoint — exactly-once; the returned loanId drives navigation to loan-detail.

## Full Contracts

See `idea-layer/screens/loan-application-wizard/api.yaml` -> `endpoints[]` for complete request/response schemas.
