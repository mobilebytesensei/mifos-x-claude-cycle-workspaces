<!-- source: screens/m11-tellers-cash/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=64e792bbb9d2 docs=df0d75b666e7 flow=b5e3615adf36 api=15a487d7ac70 -->
<!-- generated: 2026-07-21T17:40:38Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M11 · Tellers & Cash Management -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m11-tellers-cash/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

### list_tellers_with_cash
`GET /v1/tellers` → `StoreFactory.createStore` + Room `SourceOfTruth(tellers_cash)` + `asScreenStream(CACHE_FIRST_SWR)`; `TellerDto -> Teller` (office, vaultCashBalance, status); scoped to the office hierarchy server-side so the teller list + vault cash position render instantly offline. Permission `READ_TELLER`. Errors: `teller_read_failed`, `offline_no_cache`.

### list_cashiers_for_teller
`GET /v1/tellers/{id}/cashiers?fromDate&toDate` → cached per `teller_id` + date range; `CashierDto -> Cashier` (staffId, allocatedCash, startDate, endDate); re-projected by `change_cashier_date_range` over cached rows with no refetch. Permission `READ_TELLER`. Errors: `cashier_read_failed`.

### create_teller
`GET /v1/tellers/template` (office option list) → dynamic-template-forms → `POST /v1/tellers` command `JsonObject` (name, officeId, status, startDate + dateFormat + locale) enqueued via `RoomSubmitOutbox` with a durable `IdempotencyKey`. Permission `CREATE_TELLER`. Errors: `teller_command_rejected`, `template_fetch_failed`.

### assign_cashier
`GET /v1/tellers/{id}/cashiers/template` (staff option list) → `POST /v1/tellers/{id}/cashiers` command `JsonObject` (staffId, startDate, endDate, isFullDay + dateFormat + locale) enqueued via the durable outbox; exactly-once replay. Permission `ALLOCATECASHIER_TELLER`. Errors: `cash_command_rejected`.

### allocate_cash_to_cashier
`POST /v1/tellers/{id}/cashiers/{cashierId}/allocate` command `JsonObject` (txnAmount, txnDate, currencyCode + dateFormat + locale) → `RoomSubmitOutbox` with a DURABLE idempotency key; `BatchSubmitHandler.AllOrNothing` replay; optimistic vault decrement + cashier credit, pending-sync until replayed. Permission `ALLOCATECASHTOCASHIER_TELLER`. Errors: `cash_command_rejected`, `insufficient_vault_cash`.

### settle_cash_from_cashier
`POST /v1/tellers/{id}/cashiers/{cashierId}/settle` command `JsonObject` (txnAmount, txnDate, currencyCode + dateFormat + locale) → durable outbox exactly-once; client-side over-settle guard against allocated cash, server the final authority (409 → needs-attention). Permission `SETTLECASHFROMCASHIER_TELLER`. Errors: `cash_command_rejected`, `settle_exceeds_allocated`.

### view_teller_transactions
`GET /v1/tellers/{id}/transactions` → cached ledger (allocate/settle + cash-in/out with running totals); `CACHE_FIRST_SWR` read-only; renders offline. Permission `READ_TELLER`. Errors: `teller_txn_read_failed`.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| M11 · Tellers & Cash Management (`m11-tellers-cash`, archetype `master_detail`) | TellersCashViewModel | loading, content, empty, submitting, queued_offline, pending_approval, error, no_network | Teller list (office · vault cash balance · active-cashier count · status chip) → per-teller detail (vault cash header, cashiers assigned for a date range with allocated cash, teller-transaction ledger, permission-gated command bar). Create teller, assign cashier, allocate/settle cash — every cash movement rides the durable outbox exactly-once; reads ride CACHE_FIRST_SWR Store5 (offline). |

## State Model

### TellersCashViewModel

- **State**: `tellers: List<Teller>` (office, vaultCashBalance, status), `selectedTellerId: String?`, `cashiers: List<Cashier>` (allocatedCash, startDate, endDate), `dateRange: DateRange`, `tellerTransactions: List<TellerTransaction>` (ledger, running totals), `vaultCashBalance: Money` (optimistic, pending-sync)
- **ScreenState**: loading, content, empty, submitting, queued_offline, pending_approval, error, no_network
- **Errors**: `teller_read_failed` (keep last-known cache; Retry), `cashier_read_failed` (Retry), `teller_txn_read_failed` (Retry), `teller_command_rejected` (route → needs-attention-inbox), `cash_command_rejected` (route → needs-attention-inbox), `insufficient_vault_cash` (block before enqueue; guard message), `settle_exceeds_allocated` (block before enqueue; guard message), `offline_no_cache` (no_network banner)
- **Events**: `NavigateToNeedsAttention` → needs-attention-inbox, `NavigateToTemplateForm(command)` → dynamic-template-forms (create/edit teller · assign cashier · allocate · settle), `NavigateToApprovals` → m15-approvals (maker-checker pending)
- **Actions**: `LoadTellers()` → tellersStream(CACHE_FIRST_SWR); `OpenTellerDetail(tellerId)` → cashiersStream(tellerId, dateRange); `ChangeCashierDateRange(range)` → in-VM re-projection (no fetch); `AllocateCash(cashierId, amount)` → outbox enqueue + IdempotencyKey, optimistic vault decrement; `SettleCash(cashierId, amount)` → outbox enqueue + IdempotencyKey, optimistic vault credit; `AssignCashier(payload)` → outbox enqueue; `CreateTeller(payload)` → outbox enqueue; `ViewTellerTransactions(tellerId)` → ledgerStream(CACHE_FIRST_SWR); `RetryLoad()` → refreshFresh(); `OpenNeedsAttention()` → NavigateToNeedsAttention
- **DI**: `TellersRepository`, `CashiersRepository`, `TellerTransactionsRepository`, `RoomSubmitOutbox`, `cmp-network-monitor`; required modules `core/model`, `core/network`, `core/database`, `core/store`, `core/data`, `core-base/store`, `core-base/network`, `core/permissions`

## API Endpoints (10)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_tellers | GET | officeId(Int) | Teller[] | 401, 403 | tellers_cash |
| get_teller_template | GET | — | TellerTemplate | 401, 403 | cached_templates |
| create_teller | POST | body(JsonObject): name, officeId, status, startDate | CommandProcessingResult | 401, 403, 422 | draft_outbox |
| update_teller | PUT | tellerId(Int), body(JsonObject) | CommandProcessingResult | 401, 403, 422 | draft_outbox |
| list_cashiers | GET | tellerId(Int), fromDate(String), toDate(String) | Cashier[] | 401, 403 | tellers_cash |
| get_cashier_template | GET | tellerId(Int) | CashierTemplate | 401, 403 | cached_templates |
| assign_cashier | POST | tellerId(Int), body(JsonObject): staffId, startDate, endDate, isFullDay | CommandProcessingResult | 401, 403, 422 | draft_outbox |
| allocate_cash | POST | tellerId(Int), cashierId(Int), body(JsonObject): txnAmount, txnDate, currencyCode | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| settle_cash | POST | tellerId(Int), cashierId(Int), body(JsonObject): txnAmount, txnDate, currencyCode | CommandProcessingResult | 401, 403, 409, 422 | draft_outbox |
| list_teller_transactions | GET | tellerId(Int), dateRange(String) | TellerTransaction[] | 401, 403 | tellers_cash |

## Dependencies (Tier 1 — foundation)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_/CREATE_/UPDATE_/ALLOCATECASHIER_/ALLOCATECASHTOCASHIER_/SETTLECASHFROMCASHIER_TELLER; drives control VISIBLE/DISABLED/HIDDEN + 403-drift refresh; re-checked at outbox replay |
| offline-sync-engine | uses | true | durable RoomSubmitOutbox + idempotency key + exactly-once batch replay for every cash movement + management command (zero double-move across app kill) |
| dynamic-template-forms | uses | true | renders every create/edit-teller · assign-cashier · allocate · settle form from the Fineract GET template option lists (never hardcoded) |
| needs-attention-inbox | navigate | true | triage of a server-rejected allocate/settle/assign/create command (Retry / Edit / Discard) with the Fineract error; never silently dropped |

## Navigation

- **Route**: m11-tellers-cash (nav root; permission-gated on the cash_mgmt group)
- **From**: nav_root (module root — visible only when the PermissionSet resolves READ_TELLER or an umbrella; a persona with no `*_TELLER` permission never sees the entry — fail-closed HIDE)
- **To**: dynamic-template-forms (create/edit teller · assign cashier · allocate · settle), needs-attention-inbox (rejected command triage), m15-approvals (maker-checker pending)

## App-Shell

- **Bottom navigation**: visible (module reached from a permission-filtered nav root).
- **Top app bar**: visible (title `{strings.m11_title}` + teller/cash context).
- **FAB**: present — "New teller" FAB, HIDE-gated (onDenied HIDE) unless the user holds `CREATE_TELLER`.
- **Snackbar**: visible (queued-offline / pending-sync / permission-drift / guard feedback).
- **Safe-area**: top respected (top-app-bar inset), bottom respected, notch extend-and-pad; content list scrolls under the bar.

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| List/detail hydration | teller list (or selected teller's cashiers + cash position) loaded live or from cache | loading → content | loading → empty (no tellers in scope) / no_network (offline, no cache) / error (fetch failed, no cache) | fold CACHE_FIRST_SWR Store5 stream → ScreenState |
| Date-range re-projection | `change_cashier_date_range` on the selected teller | content → content (cashier assignments re-projected over cached rows) | — | pure in-ViewModel re-projection, no refetch, no network — works offline |
| Command connectivity | network available at create/assign/allocate/settle submit time | content → submitting (posted online) | content → queued_offline (durable outbox, idempotency key, replays exactly-once) | cmp-network-monitor gate; RoomSubmitOutbox.enqueue |
| Submit outcome | server returns 2xx CommandProcessingResult | submitting → content (2xx with resourceId — cash position / roster refreshed) | submitting → pending_approval (maker-checker: commandId, empty changes — cash NOT optimistically mutated) / error (400 validation / 409 optimistic-lock / 403 drift → needs-attention-inbox) | maker-checker aware; BatchSubmitHandler.AllOrNothing replay |
| Cash-movement guard | allocate ≤ vault balance / settle ≤ cashier's allocated cash | enqueue the movement (optimistic vault/cashier adjust, pending-sync) | block before enqueue; surface guard message (`insufficient_vault_cash` / `settle_exceeds_allocated`) | client-side guard; server the final authority (409 → needs-attention) |
| Control permission | user holds the control's exact `{ACTION}_{ENTITY}` code | control rendered/enabled (same binary = teller-officer surface) | HIDDEN (create/movement FABs, fail-closed) or DISABLED-with-reason (edit) | permission-capability-engine PermissionEvaluator.Gated |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| content_teller_list_and_cash_position | The module resolves the teller list with each teller's vault cash position and renders the permission-gated command bar | high |
| teller_management_gated | A READ_TELLER-only user sees the read surface with every mutating control HIDDEN (fail-closed, no role-name branching) | high |
| allocate_cash_queues_exactly_once | A cash allocation issued offline is written to the durable outbox and replays exactly once (no double-move) across an app kill/retry | high |
| settle_cash_gated_and_guarded | Settle is HIDDEN without permission; an over-settle beyond allocated cash is refused client-side then server-authoritative (409 → needs-attention) | high |
| create_teller_from_template_form | New-teller opens the dynamic-template-forms create form sourced from GET /v1/tellers/template and enqueues a durable create command | medium |
| assign_cashier_date_range | Assign-cashier binds a staff member for a date range; the date_range filter re-projects assignments over cached rows with no network call | medium |
| offline_shows_cached_cash_position | Offline, the teller list + cash position + ledger show last cached values with an offline banner and pending-sync markers | medium |
| error_command_rejected_routes_to_needs_attention | A server-rejected cash command surfaces the Fineract error and offers Review-failed into needs-attention-inbox; never double-moves cash | medium |
