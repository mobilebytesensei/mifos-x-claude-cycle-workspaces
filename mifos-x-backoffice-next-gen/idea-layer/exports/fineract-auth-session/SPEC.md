<!-- source: screens/fineract-auth-session/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=3b02b6fa6157 docs=ee03ec602a71 flow=cb69536d8c71 api=1d50cda2c102 -->
<!-- generated: 2026-07-22T01:40:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Fineract Sign in (tenant login → authenticated session) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/fineract-auth-session/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: designed

---

## Capabilities

- **has_ui** — Compose "Sign in" tenant login form (archetype: form_screen): base-URL, tenant identifier (default `default`), username, password, Sign in.
- **has_api** — Fineract `POST /fineract-provider/api/v1/authentication` (login) + `GET /userdetails` (session revalidate).
- **is_auth_feature** — the application entry gate; a real `AuthProvider` replaces `NoOpAuthProvider` and gates every authenticated surface.
- **native-capability** — `core/auth`: HTTP Basic auth (base64 `username:password`) + `Fineract-Platform-TenantId` header + dynamic base-URL; encrypted session persist/restore via secure `multiplatform-settings`; an authenticated Ktorfit/Ktor Fineract client over `core-base/network` — the transport every data feature rides.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| fineract-auth-session | FineractAuthViewModel | Restoring, Input, Authenticating, Error, Authenticated | On cold start restores a persisted encrypted session (offline-capable); otherwise shows the classic Fineract Basic-auth login (base-URL, tenant, username, password). On Sign in POSTs `/authentication`, builds the Basic token, persists the session encrypted, bootstraps the PermissionSet for `core/permissions`, and flips `AuthProvider.isConfigured=true`. A 401 anywhere drops the session back here. Archetype: form_screen. |

## State Model

### FineractAuthViewModel

- **State**: session (Session{baseUrl, tenant, basicToken, userId, permissions:List<String>}), formState (baseUrl, tenant='default', username, password + per-field validation), isConfigured (Boolean)
- **ScreenState**: Restoring, Input, Authenticating, Error, Authenticated
- **Errors**: invalid_credentials_401 (error banner; keep values; nothing persisted), invalid_base_url (highlight base-url field; blocked client-side), network_unreachable (error + Retry), two_factor_or_password_change_required (surface required next step; no full session), secure_store_read_failure (fall through to login), secure_store_write_failure (surface; require re-login), unauthorized_401_session_drop (clear session; route to login), client_not_configured (route to login)
- **Events**: NavigateToLogin → fineract-auth-session, NavigateToCapabilityBootstrap → permission-capability-engine
- **Actions**: RestoreSession (private — read encrypted session → Authenticated | Input), UpdateField (form state + validation), SignIn (POST /authentication → build Basic token → map AuthenticatedUserData → persist encrypted → bootstrap PermissionSet → isConfigured=true → route), RetrySignIn (re-run authenticate with retained inputs), Logout (clear secure store + AuthProvider.clearSession → Input), OnUnauthorized (401 drop: clear store → NavigateToLogin)
- **DI**: core/auth, core/permissions, core-base/network, core/datastore

## API Endpoints (2)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| authenticate | POST | tenantIdentifier(query, required), username(String, body), password(String, body) | AuthenticatedUserData: base64EncodedAuthenticationKey, userId, username, permissions[], roles[], officeId, staffId, shouldRenewPassword, isTwoFactorAuthenticationRequired | 401, invalid_base_url, network | (none — auth handshake) |
| get_authenticated_userdetails | GET | Fineract-Platform-TenantId(header) | AuthenticatedUserData (revalidate restored session) | 401 (session drop) | (none) |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | requires_features | yes | Login populates the `PermissionSet` (from `permissions[]`) that `core/permissions` PermissionEvaluator consumes; the login screen references the PermissionSet type and hands off to the capability bootstrap on success. |

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Session restore | a valid persisted session exists in secure settings | authenticated (no login form; offline-capable) | input (show login form) | RestoreSession reads encrypted store; no network call |
| Field validity | base-URL is http(s) URL AND tenant/username/password non-empty | Sign in enabled | Sign in disabled; highlight offending field | client-side validation; invalid_base_url before any request |
| Authenticate | POST /authentication returns 200 | authenticated (build Basic token, persist, bootstrap PermissionSet) | error (401 invalid creds / network) | base64('username:password'); nothing persisted on failure |
| Auth step required | shouldRenewPassword OR isTwoFactorAuthenticationRequired | error (auth_step_required — surface next step) | authenticated | no full session / capability surface built |
| Unauthorized drop | any authenticated request returns 401 | clear session + route to login | continue | AuthProvider clears store; isConfigured=false; never crash |
| Logout | operator invokes logout | clear session + return to login | continue | SecureSessionStore.clear() + AuthProvider.clearSession() |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| successful_login_establishes_session | Valid inputs authenticate, persist an encrypted session, and route to the capability bootstrap | high |
| basic_auth_and_tenant_header_applied | Every authenticated request carries Authorization: Basic base64(user:password) + Fineract-Platform-TenantId | high |
| invalid_credentials_shows_error | A 401 renders the invalid-credentials error without persisting anything | high |
| field_validation_blocks_sign_in | Empty fields or a malformed base-URL keep Sign in disabled and highlight the field | high |
| session_restored_on_cold_start | A persisted session rehydrates the AuthProvider on cold start with no network call | high |
| unauthorized_401_drops_session_routes_to_login | A 401 clears the session and returns to login | high |
| logout_clears_session | Logout clears the persisted session and returns to the login form | medium |
| password_change_or_2fa_required_blocks_surface | shouldRenewPassword / 2FA blocks building the capability surface | medium |
