<!-- source: screens/search-record/ (v4.0 siblings: ui, docs, flow, demo-data) -->
<!-- generated: 2026-08-01T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->

# Search Record — Implementation Specification

> **Source of Truth**: `idea-layer/screens/search-record/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: enriched

---

## Capabilities

- **has_ui** — Compose list screen with entity-type chips, swipe/icon delete, undo snackbar (archetype: list_screen).
- **has_flow** — loading → content / empty / error; delete + undo-window; clear-all confirm dialog; re-run search navigate.
- **has_offline_cache** — All reads from Room `search_history` table (ring buffer, max 50 rows); no Fineract API calls; updates live via Room `Flow`.
- **requires_auth** — Post-login only; gated to field-officer personas via `READ_CLIENT` (gray-out-info-dialog on deny).

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| search-record | SearchRecordViewModel | Loading, Content, Empty, Error | Offline recent-search history list. Collects Room `Flow<List<SearchHistoryRow>>` (ring buffer, max 50 rows, searched_at DESC). Entity types: client / loan / group / center. Row tap → entity detail navigation (READ_CLIENT gate). Row delete → 3s undo-snackbar window. Re-run search chip → M14 live search with pre-filled query. All reads offline; Room updated live by search screens. Archetype: list_screen. |

## State Model

### SearchRecordViewModel

- **State**: searchHistory (List<SearchHistoryRow>), pendingDeleteId (String? — composite key during the 3s undo window), historyEnabled (Boolean — search_history_enabled preference, defaults true)
- **ScreenState**: Loading, Content, Empty, Error
- **Errors**: history_empty (Empty state — "No recent searches yet. Your recently viewed clients, loans, groups, and centers will appear here."), history_disabled (Empty variant — "Search history is off — enable in Settings."), history_read_failed (Error state — "Could not load search history." + Retry)
- **Events**: NavigateToClientDetail(clientId) → client-detail-360, NavigateToLoanDetail(loanId) → loan-detail, NavigateToGroupCenter(entityId, entityType) → m03-groups-centers, NavigateToM14Search(queryText) → m14-reports-search-audit with pre-filled query
- **Actions**: OnHistoryRowClick (open_search_result → navigate to entity detail — READ_CLIENT gate), OnDeleteHistoryRowClick (delete_history_row → Room delete immediately; set pendingDeleteId; show 3s undo snackbar), OnUndoDelete (re-INSERT the pendingDeleteId row with original searched_at; clear pendingDeleteId), OnDeleteCommitted (3s expired; pendingDeleteId cleared; deletion final), OnClearAllClick (clear_all_history → confirm dialog → Room delete all rows → Empty state), OnSearchFromHistoryClick (re_run_search → NavigateToM14Search with pre-filled query — READ_CLIENT gate), Internal.ReceiveHistoryUpdates (collect Room Flow; list updates live when other screens insert new rows)
- **DI**: core/datastore

## API Endpoints

*(None — all reads from Room `search_history` table. No Fineract calls. Room is the sole data source.)*

## Local Store Schema

| Table | Backend | Columns | Notes |
|-------|---------|---------|-------|
| search_history | Room | id (UUID), entity_type (client/loan/group/center), entity_id (Long), entity_display_name (String), query_text (String), searched_at (Instant) | Max 50 rows — ring buffer enforced by a Room trigger: on INSERT when count > 50, DELETE the oldest row by searched_at. Ordered by searched_at DESC. Survives app kill. |
| search_history_prefs | multiplatform_settings | search_history_enabled (Boolean, default true) | User preference to disable history recording. When false: history_list is empty; an 'off' variant of the empty state shown. |

## SearchHistoryRow Schema

| Field | Type | Notes |
|-------|------|-------|
| id | String (UUID) | Composite key: "{entity_type}_{entity_id}" — prevents duplicate entries for the same entity. On re-search, updates searched_at (UPSERT). |
| entity_type | Enum: client/loan/group/center | Drives chip color (client=teal, loan=blue, group=orange, center=green) and navigation target. |
| entity_id | Long | Used to construct the navigation route (e.g. client_id → client-detail-360). |
| entity_display_name | String | Human-readable name (e.g. "Aisha Bello", "Loan #4471 · Active · KES 75,000"). |
| query_text | String | The search string the user typed that led to this result. Shown in italic below the entity name; used for re-run search. |
| searched_at | Instant | Timestamp of last search for this entity. Used for ordering + relative time display ("12 min ago", "Yesterday"). |

## Sample Content Data (from `demo-data.yaml`)

| Row | entity_type | entity_display_name | query_text | searched_at | relative |
|-----|-------------|--------------------|-----------|-----------|---------
| sr-001 | client | Aisha Bello | "Aisha Bello" | 2026-08-01T09:45+03:00 | 12 min ago |
| sr-002 | loan | Loan #4471 · Active · KES 75,000 | "4471" | 2026-08-01T09:30+03:00 | 27 min ago |
| sr-003 | client | Grace Wanjiru Mwangi | "grace wanjiru" | 2026-08-01T08:50+03:00 | 1 hr ago |
| sr-004 | group | Westlands Group Center · 8 members | "westlands group" | 2026-07-31T16:20+03:00 | Yesterday |
| sr-005 | center | Kibera Community Center · 12 groups | "kibera" | 2026-07-31T14:05+03:00 | Yesterday |
| sr-006 | client | Samuel Otieno Odhiambo | "samuel otieno" | 2026-07-30T11:00+03:00 | 2 days ago |

## Permission-Gating (§4.7)

| Action | Required Permission | Denied Behavior | Notes |
|--------|--------------------|-----------------|----|
| open_search_result (row tap) | READ_CLIENT | **gray-out + info dialog** ("Contact your manager") | Rows are visible; tapping denied shows info dialog, not navigation |
| re_run_search (re-run query chip) | READ_CLIENT | **gray-out + info dialog** | Same gate as row tap |
| delete_history_row | *(no permission gate)* | — | User owns their own history |
| clear_all_history | *(no permission gate)* | — | User owns their own history |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | READ_CLIENT evaluation for gray-out on row tap + re-run-search |
| fineract-auth-session | requires | true | authenticated user context |
| m14-reports-search-audit | consumed_by + navigate | true | primary entry point (M14 "Recent searches" tab); re-run search navigates back to M14 with pre-filled query |
| m02-clients | provides_to | true | client-list search inserts rows into search_history; row tap navigates to client-detail-360 |
| m04-loan-portfolio | provides_to | true | loan search inserts rows; row tap navigates to loan-detail |

## Navigation

- **Route**: search-record
- **From**: m14-reports-search-audit (tab or link), nav-shell-assembler drawer sub-item (field-officer personas)
- **To**: client-detail-360 (client row tap), loan-detail (loan row tap), m03-groups-centers (group/center row tap), m14-reports-search-audit (re-run search with pre-filled query)

## App-Shell

- **Bottom navigation**: visible (within M14 module stack).
- **Top app bar**: visible (title "Recent Searches" + back arrow + "Clear all" trailing text button in content state).
- **FAB**: none.
- **Snackbar**: enabled — 3s undo-delete snackbar with "Search removed. [Undo]".

## Flow Logic (4 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| History has rows | Room search_history Flow emits ≥1 row AND historyEnabled=true | Content state (row list) | historyEnabled=false → empty-off variant; rows=0 → standard empty state | Flow<List<SearchHistoryRow>> |
| Row delete undo | User taps [Undo] within 3s of delete | re-INSERT row with original searched_at; pendingDeleteId cleared | 3s expires; pendingDeleteId cleared; deletion final (committed) | 3s coroutine timer + pendingDeleteId state |
| Row tap permission | READ_CLIENT held | navigate to entity detail | gray-out info dialog ("Contact your manager") | PermissionEvaluator.hasCode(READ_CLIENT) |
| Clear all confirm | Confirm dialog accepted | DELETE all from search_history; flow emits empty → Empty state | Dialog dismissed; no deletion | AlertDialog.onConfirm |

## Testing (5 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| history_list_content | Field officer with 6 recent searches → content state shows 6 rows ordered by searched_at DESC with correct entity type chips and relative timestamps | high |
| navigate_to_entity | Tap "Aisha Bello" (client, sr-001) → navigate to client-detail-360 for client_id=10241 | high |
| delete_with_undo | Delete sr-001 → row removed from list → undo snackbar appears → tap Undo within 3s → row re-inserted with original searched_at | high |
| clear_all_confirm | Tap "Clear all" → confirm dialog → all rows deleted → Empty state shown | medium |
| permission_denied_row_tap | User without READ_CLIENT taps a history row → gray-out info dialog ("Contact your manager") shown; no navigation occurs | high |
