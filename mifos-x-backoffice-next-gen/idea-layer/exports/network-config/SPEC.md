<!-- source: screens/network-config/ (v4.0 siblings: ui, docs, flow, demo-data) -->
<!-- generated: 2026-08-01T00:00:00Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 1.0.0 -->
<!-- prior_version: — -->

# Network Config — Implementation Specification

> **Source of Truth**: `idea-layer/screens/network-config/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 1.0.0 | **Status**: enriched

---

## Capabilities

- **has_ui** — Compose form screen with two-tab connect-mode selector (archetype: form_screen).
- **has_api** — `GET /v1/serverinfo` (validation probe) + `POST /v1/authentication` (re-auth after save).
- **has_flow** — Demo one-tap → save → re-auth; Org custom → QR decode / manual entry → validate → save → re-auth.
- **has_local_preferences** — Encrypted `multiplatform-settings` store for `base_url`, `tenant`, `username`, `password_hash`; plain store for UI display labels.
- **has_system_integration** — Camera QR scan (`CameraCapture`) for config onboarding; `DynamicBaseUrlPlugin` swap on save.
- **requires_auth** — Used both pre-auth (sign-in screen) and post-auth (M04/M17); save triggers a forced re-auth.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| network-config | NetworkConfigViewModel | Idle, Scanning, Editing, Validating, Saving, Error | "Powered by Mifos Initiative" server connection editor. Two connect modes: Demo (one-tap fills mifos-bank-2.mifos.community / tenant mifos-bank-2 / mifos/password) or Org (custom Fineract base-URL + tenant + credentials, optionally via QR scan). Validates via GET /v1/serverinfo; on save, encrypts and persists to the multiplatform-settings store, swaps DynamicBaseUrlPlugin base-URL + Fineract-Platform-TenantId header, and forces POST /v1/authentication re-auth. Archetype: form_screen. |

## State Model

### NetworkConfigViewModel

- **State**: connectMode (ConnectMode: DEMO | ORG), baseUrl (String), tenant (String), username (String), password (String — transient, never persisted beyond editing), baseUrlError (String?), tenantError (String?), validationResult (ValidationResult?: VERIFIED | UNREACHABLE | WRONG_TENANT), activeConfig (NetworkConfigState), isSubmitting (Boolean), saveError (String?)
- **ScreenState**: Idle, Scanning, Editing, Validating, Saving, Error
- **Errors**: url_invalid (inline field error "Please enter a valid https:// URL" — blocks Validate + Save), tenant_empty (inline "Tenant identifier is required"), validation_failed_unreachable ("Could not reach {host} — check the URL and your connection."), validation_failed_tenant_unknown ("Tenant not found on this server — verify the Fineract-Platform-TenantId." + hint "this server uses 'mifos-bank-2'"), save_reauth_failed ("Config saved but sign-in to the new server failed — {server error}. Please try signing in manually." — config is persisted; retry offered), qr_decode_failed (snackbar "QR code not recognized")
- **Events**: NavigateToDashboard (on successful save + re-auth → capability bootstrap route)
- **Actions**: OnTabSelect (select_connect_mode → switch mode; auto-fill demo fields on DEMO tab), OnDemoConnect (apply_demo_config → fill PRIMARY defaults + save + re-auth), OnBaseUrlChange (update_base_url → inline URL validation), OnTenantChange (update_tenant), OnUsernameChange (update_username), OnPasswordChange (update_password — never logged), OnScanQRClick (start_qr_scan → camera; on decode auto-fill fields), OnValidateClick (validate_connection → GET /v1/serverinfo), OnSaveClick (save_config → persist + swap plugin + re-auth), OnResetToDemoClick (reset_to_demo → confirm dialog → PRIMARY demo config + re-auth), Internal.OnValidationResult, Internal.OnSaveResult
- **DI**: core-base/network, core/datastore, core/auth

## API Endpoints (2)

| Function | Method | Path | Params | Response | Errors |
|----------|--------|------|--------|----------|--------|
| validate_connection | GET | /v1/serverinfo | Fineract-Platform-TenantId header (entered tenant) | ServerInfo (Fineract version, readWrite mode) | 401 (wrong tenant — "Invalid tenant identifier supplied. Supplied value: {tenant}"), network errors (unreachable) |
| fineract_auth | POST | /v1/authentication | AuthRequest body (username, password) | AuthenticationResponse (base64-encoded auth token + userId + roles) | 401 (wrong credentials — "sign-in failed: wrong username or password") |

## Demo Defaults (PRIMARY — from `research/FINERACT_INSTANCES.md`)

| Field | Value |
|-------|-------|
| base_url | `https://mifos-bank-2.mifos.community/fineract-provider/api/v1` |
| tenant | `mifos-bank-2` (**NOT** `default` — returns 401 if wrong) |
| username | `mifos` |
| password | `password` |
| roles_count | 15 real roles (OFICIAL DE CAMPO, CAJERO, SUPERVISOR DE CREDITOS, Super user, and 11 more) |
| env_tag | `demo` |

Fallback (if PRIMARY unreachable): `https://sandbox.mifos.community/fineract-provider/api/v1` / tenant `default` / mifos / password — only 2 roles (Super user, Self Service User).

## Local Store Schema

| Table | Backend | Keys | Notes |
|-------|---------|------|-------|
| network_config | multiplatform_settings_encrypted | base_url, tenant, username, password_hash | Password stored as bcrypt hash (never plaintext). DynamicBaseUrlPlugin reads base_url + tenant on every Ktor call. |
| app_settings | multiplatform_settings | active_tenant_display, last_known_server_env | Non-sensitive; used for UI display (tenant label chip, env badge). |

## DTOs

| Name | Role | Notes |
|------|------|-------|
| ServerConfig | model | protocol + host + apiPath + port + tenant — validated by ServerConfigValidatorUseCase (field-officer app pattern: non-empty, https:// scheme, valid host). |
| AuthenticationResponse | response | POST /v1/authentication — confirms credentials valid on the new server. |
| NetworkConfigState | model | Current active config (base_url, tenant, username, env_label) shown read-only in the form. |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| fineract-auth-session | provides | true | receives re-auth trigger on save; manages the auth token from the AuthenticationResponse |
| permission-capability-engine | downstream | true | receives capability bootstrap trigger after re-auth success |
| m04-loan-portfolio | consumed_by | false | exposes "Network settings" tile entry point for this screen |
| m17-sync-settings | consumed_by | false | exposes "Network configuration" row entry point |

## Navigation

- **Route**: network-config
- **From**: fineract-auth-session (Scan/Configure buttons on sign-in screen), m04-loan-portfolio (Network settings tile), m17-sync-settings (Network configuration row), nav-shell-assembler drawer footer (wifi icon — all authenticated users)
- **To**: fineract-auth-session capability bootstrap (on successful save + re-auth)

## App-Shell

- **Bottom navigation**: hidden (modal over the shell).
- **Top app bar**: visible (title "Network Configuration" + back arrow).
- **FAB**: none.
- **Snackbar**: enabled — qr_decode_failed, camera_permission_denied, save_success.

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Connect mode | Demo tab selected | auto-fill PRIMARY mifos-bank-2 defaults; skip validate; Save enabled | ORG tab: user enters fields; Save disabled until validate returns VERIFIED | state.connectMode == DEMO |
| QR decode | Camera captures a valid Mifos config QR payload | auto-fill base_url + tenant + username from QR; transition to editing; password field focused | snackbar "QR not recognized"; camera closes | QR payload parse success/failure |
| Validation probe | GET /v1/serverinfo returns HTTP 200 with entered config | "Connection verified" chip shown; save_button enabled | error state with server error detail (unreachable / wrong tenant) | ServerConfigValidatorUseCase + HTTP probe |
| Save path | network available + validation passed | persist config; swap DynamicBaseUrlPlugin; trigger re-auth (POST /v1/authentication) | (no offline save — config requires a verified server) | encrypted persist + plugin swap + re-auth |
| Re-auth result | POST /v1/authentication returns 200 | route to capability bootstrap (app fully operational on new server) | error state: "Config saved but sign-in failed" (config retained; retry available) | AuthSessionManager |

## Testing (6 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| demo_one_tap | Tap 'Use Mifos community demo' → fields filled with mifos-bank-2 defaults → save → re-auth succeeds → Super user demo back-office loaded | high |
| qr_autofill | Scan a valid Mifos config QR → base_url + tenant + username auto-filled → validate → save → re-auth | high |
| org_custom_server | Enter communitybank.co.ke URL + tenant communitybank + credentials → validate → save → re-auth → org back-office loaded | high |
| inline_url_error | Enter URL without https scheme → inline "Please enter a valid https:// URL" → Validate + Save buttons disabled | high |
| wrong_tenant_error | Enter valid host but wrong tenant → validation_failed_tenant_unknown error shown with hint | medium |
| reset_to_demo_confirm | Tap Reset to demo while on org config → confirm dialog → demo config applied → re-auth | medium |
