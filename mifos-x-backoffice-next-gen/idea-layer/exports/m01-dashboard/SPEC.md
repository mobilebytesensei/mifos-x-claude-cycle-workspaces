<!-- source: screens/m01-dashboard/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=ca9fd11291f5 docs=9dfc40cb4035 flow=1106f9191b1f api=be368d02a877 -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M01 · Role-adaptive Dashboard -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m01-dashboard/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose IndependentCardLayout home surface of permission-filtered tiles.
- **has_api** — Fineract runreports / makercheckers / collectionsheet / notifications reads.
- **has_offline_cache** — per-tile CACHE_FIRST_SWR so each tile renders its last cached value offline.
- **requires_auth** — every tile fetch authenticated; each tile permission-gated via PermissionEvaluator.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| M01 · Role-adaptive Dashboard (`m01-dashboard`, archetype `detail_screen`) | DashboardViewModel | loading, content, empty, partial-tile-error, no-network, error | The canonical "same binary renders per permission" home surface. Every KPI card, quick-action tile, and activity row is resolved through `core/permissions` PermissionEvaluator — present ONLY if the user holds its gating permission (no role branching). Each tile hydrates independently CACHE_FIRST_SWR, so one failing tile shows an inline error placeholder and never blanks the rest. |

## Capability Contracts

### resolve_tile_roster
`PermissionEvaluator.observe(tileId): Flow<CapabilityState>` per fixed tile; VISIBLE tiles form the roster; unmapped/denied → HIDDEN (fail-closed); `ALL_FUNCTIONS_READ` never yields a mutating quick-action tile. Permission `none`. Errors: `tile_roster_resolve_failed`, `retained_permission_set_absent`.

### hydrate_kpi_tiles
GET /v1/runreports/{reportName} (R_officeId) → `StoreFactory.createStore` + Room SourceOfTruth + `asScreenStream(CACHE_FIRST_SWR)` per tile_id; `ReportResult` → headline KPI value (PAR / active-clients). Permission `REPORTING_SUPER_USER`. Errors: `tile_load_failed`, `report_unavailable`.

### hydrate_collections_tile
GET /v1/collectionsheet?staffId&transactionDate → cached `CollectionSheet` summary (due amount + count) for the field officer's route. Permission `READ_COLLECTIONSHEET`. Errors: `tile_load_failed`.

### hydrate_approvals_tile
GET /v1/makercheckers?makerCheckerable → pending-approval count self-scoped to entries the user can approve; present iff `hasAnyChecker`. Permission `CHECKER_SUPER_USER`. Errors: `tile_load_failed`.

### hydrate_activity_feed
GET /v1/notifications (poll + mark-read) → recent-activity rows with entity deep-links; no special permission required. Permission `none`. Errors: `tile_load_failed`.

## State Model

### DashboardViewModel

- **State**: `uiState: DashboardUiState`, `tiles: List<DashboardTile>` (resolved roster), `failedTileIds: Set<String>` (isolated per-card failures), `cachedTiles: List<DashboardTile>` (last cached values for offline), retained encrypted `PermissionSet`
- **ScreenState**: Loading (`loading`), Success(`tiles`) (`content`), Empty (`empty`), PartialTileError(`tiles`, `failedTileIds`) (`partial-tile-error`), NoNetwork(`cachedTiles`) (`no-network`), Error (`error`)
- **Errors**: `tile_roster_resolve_failed` (retry=true; retain encrypted set), `retained_permission_set_absent` (route to re-authenticate), `tile_load_failed` (retry=true; isolated to the one card), `report_unavailable` (inline error placeholder on that KPI card)
- **Events**: `NavigateTo(target)` (tile / quick-action deep-link), `NavigateToAccessPermissions` → permission-capability-engine
- **Actions**: `LoadDashboard()` → resolveTileRoster() + per-permitted-tile StoreFactory.stream(tileId).collect; `RetryFailedTile(tileId)` → DashboardTileRepository.refetch(tileId) (that card only); `RetryDashboard()` → re-resolve roster from retained encrypted PermissionSet; `OpenTileTarget(tileId)` → sendEvent(NavigateTo(tile.target)); `ViewAccess()` → sendEvent(NavigateToAccessPermissions)
- **DI**: `core/permissions`, `core/store`, `core/network`, `core/database`

## API Endpoints (4)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| run_dashboard_reports | GET | reportName(String), R_officeId(Integer) | ReportResult | 401, 403, 500 | dashboard_tile_cache |
| get_makerchecker_count | GET | makerCheckerable(Boolean) | MakerCheckerEntry[] | 401, 403 | dashboard_tile_cache |
| get_collectionsheet_today | GET | transactionDate(String), staffId(Integer) | CollectionSheet | 401, 403 | dashboard_tile_cache |
| get_notifications | GET | isRead(Boolean) | Notification[] | 401 | dashboard_tile_cache |

## Dependencies (Tier 1 — foundation)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | `PermissionEvaluator.observe(tileId)` resolves each tile VISIBLE/HIDDEN; every KPI / quick-action / activity tile present only if its gating permission is held; drives 403-drift refresh + re-resolve |
| m14-reports-search-audit | navigate | false | report-runner drill-down from the Portfolio-at-Risk KPI card (present only when canRunReport) |
| m02-clients | navigate | false | Clients list from the active-clients KPI card + new-client quick-action (READ_CLIENT / CREATE_CLIENT) |
| m06-collections | navigate | false | today's collection sheet from the collections-due KPI card (READ_COLLECTIONSHEET) |
| m15-approvals-makerchecker | navigate | false | checker inbox from the pending-approvals KPI card (hasAnyChecker) |
| m04-loan-portfolio | navigate | false | loan repayment capture from the take-repayment quick-action (REPAYMENT_LOAN) |
| needs-attention-inbox | navigate | false | entity deep-link / failed-sync routing from a recent-activity row |

## Navigation

- **Route**: m01-dashboard (Home nav root)
- **From**: post_login (landing after login-permission-bootstrap once the PermissionSet is resolved), nav_root (Home tab — re-entered on return from any module)
- **To**: report-runner, client-list, collection-sheet, checker-inbox, loan-detail, needs-attention-inbox, permission-capability-engine

## App-Shell

- **Bottom navigation**: visible (Home is the primary permission-filtered bottom-nav root).
- **Top app bar**: visible (title `{strings.m01_title}`).
- **FAB**: none (quick-actions are in-grid tiles, not a floating action button).
- **Snackbar**: enabled (offline / retry / permission-drift feedback).
- **Safe-area**: standard top-app-bar inset; the tile grid scrolls under the bar; offline banner pins below the bar in no-network.

## Flow Logic (5 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Roster resolve | PermissionEvaluator resolves the permitted tile roster | loading → content / empty / partial-tile-error / no-network | loading → error (full-screen Retry; retained encrypted set kept, app stays usable) | resolveTileRoster() over the retained encrypted PermissionSet |
| Any tile permitted | roster has ≥1 VISIBLE tile | content (permitted tiles rendered as IndependentCardLayout) | empty ("no dashboard tiles available for your access" + View-my-access link) | fail-closed tile visibility — pure data, no role branching |
| Per-tile hydration | every permitted tile's fetch succeeds (live or cache) | content | partial-tile-error (inline error + Retry on the failing card only; the rest render) | per-tile StoreFactory.stream(tileId) CACHE_FIRST_SWR; single-tile failure isolated |
| Connectivity | offline at load | no-network (each permitted tile shows its last cached value + offline banner; tapping still navigates) | content (live tile values) | cmp-network-monitor; CACHE_FIRST_SWR SourceOfTruth serves cache |
| Retry path | retry_failed_tile re-fetch succeeds | partial-tile-error → content (every tile now has a value) | partial-tile-error (that tile still failing — inline placeholder kept) | DashboardTileRepository.refetch(tileId) re-issues only the failed tile's Fineract call |

## Testing (7 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| content_permitted_tiles_rendered | Dashboard resolves the permitted tile roster and renders each permitted tile with its value | high |
| tiles_are_permission_intersection | The same binary renders a different tile set per persona purely from permissions (no role branching) | high |
| read_only_persona_has_no_mutating_quick_actions | An ALL_FUNCTIONS_READ auditor sees every read KPI tile but zero mutating quick-action tiles | high |
| one_failing_tile_does_not_blank_rest | A single tile source failing shows an inline error on that card while the rest render | high |
| offline_tiles_show_cached | Offline, every permitted tile shows its last cached value with an offline banner | medium |
| empty_no_tile_permissions | A freshly-provisioned user with no tile permissions sees the empty state, not a blank screen | medium |
| error_roster_resolve_fails_offers_retry | If the tile roster cannot be resolved, a full-screen Retry rebuilds from the retained set | medium |
