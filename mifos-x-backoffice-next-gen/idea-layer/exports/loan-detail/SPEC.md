<!-- source: screens/loan-detail/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=2ad7eecddfa4 docs=e02733143e0d flow=206d3b801d41 api=99b44c2ebf36 -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 1.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# Loan Account Detail (summary · schedule · ledger · actions) -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/loan-detail/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0 | **Status**: draft

---

## Capabilities

- **has_ui** — Compose detail screen: a loan summary header + tabbed body (Schedule · Transactions · Details) + a permission-gated lifecycle action bar.
- **has_offline_cache** — the loan aggregate reads CACHE_FIRST_SWR through Store5; the loan opens fully offline with a stale-refresh badge.
- **has_flow** — entered from M01 repayment tile / M04 list; loading → content/empty/error, the client link drills into client-detail-360.
- **requires_auth** — reads + every mutation run under the authenticated Fineract session, gated by READ_LOAN.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| loan-detail | LoanDetailViewModel | loading, content, empty, error | The single-loan account detail entered from the M01 dashboard 'quick take repayment' tile and the M04 loan-portfolio list. Loads GET /v1/loans/{loanId}?associations=all into a summary header (client, product, principal, status, outstanding, arrears, next installment) over a tabbed body — Repayment schedule · Transactions ledger · Loan details — and a permission-gated action bar mapping to the Fineract loan command endpoints (approve/disburse/repayment/undoapproval/foreclosure/writeoff). Every mutating action is enqueued to the offline outbox (durable key → POST /v1/batches on next sync), so a field-captured repayment replays exactly-once. Amounts, arrears and schedule read cache-first — opens offline. Archetype: detail. |

## State Model

### LoanDetailViewModel

- **State**: loan (LoanAccount? — client, product, principal, status, outstanding, arrears, next installment), schedule (List<RepaymentPeriod> — due date, principal, interest, fees, outstanding), transactions (List<LoanTransaction> — disbursement, repayment, waiver, fee, accrual), selectedTab (LoanDetailTab — Schedule | Transactions | Details), permittedActions (Set<LoanAction> — valid for the status AND held by the PermissionSet), uiState (LoanDetailScreenState)
- **ScreenState**: loading, content, empty, error
- **Errors**: loan_not_found (empty state — loanId resolved to no account; not-found panel + back), loan_load_failed (retry=true; error state — no usable cache, Retry re-reads aggregate), action_not_permitted (403 — action hidden; permission drift refresh + re-resolve), repayment_post_failed (surfaced inline; stays queued in the outbox for replay)
- **Events**: NavigateToClientDetail(clientId) → client-detail-360 (client link tapped in the summary header)
- **Actions**: OnLoad (read loan aggregate cache-first, resolve permitted actions), OnTabSelected (select_tab → in-VM tab switch, no refetch), OnMakeRepayment (make_repayment → POST …/transactions?command=repayment enqueued to draft_outbox with a fresh key), OnApproveLoan (approve_loan → POST …?command=approve enqueued; maker step, checker may follow), OnDisburseLoan (disburse_loan → POST …?command=disburse enqueued), OnUndoApproval (…?command=undoapproval), OnForeclose (…?command=foreclosure), OnOpenClient (open_client → navigate client-detail-360), OnRetry (retry → re-read aggregate)
- **DI**: core/store, core/data, core/database, core/permissions, core/network

## API Endpoints (3)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| get_loan | GET | loanId(Long), associations(String)?, exclude(String)? | LoanAccountDto | 401, 403 | cached_loans |
| post_repayment | POST | loanId(Long), command(String) | CommandProcessingResultDto | 401, 403, 409 | draft_outbox |
| loan_lifecycle_command | POST | loanId(Long), command(String) | CommandProcessingResultDto | 401, 403, 409 | draft_outbox |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_LOAN (read) + per-command gates (REPAYMENT_LOAN, APPROVE_LOAN, DISBURSE_LOAN, …) against the status for the action bar |
| offline-sync-engine | uses | true | durable outbox + fresh idempotency key per action + POST /v1/batches replay for exactly-once mutations |
| fineract-auth-session | requires | true | authenticated session required for the aggregate read and every lifecycle command |

## Navigation

- **Route**: loan-detail(loanId)
- **From**: m04-loan-portfolio (open_loan_detail row), m01-dashboard (quick-take-repayment tile, Schedule-tab deep link), needs-attention-inbox (loan-approved / repayment-due drill-in), client-detail-360 (account row)
- **To**: client-detail-360 (client link)

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Loan resolves | the loan aggregate resolves with associations (cache-first) | content | (see empty/error) | Store5 CACHE_FIRST_SWR over GET /v1/loans/{loanId} |
| Not found | the loanId resolves to no account | empty (not-found panel + back) | content | server 404 / deleted loan |
| Load hard-fail | loan failed AND no cache available | error (message + Retry) | content | GET failure with empty cache |
| Action gate | action valid for status AND held by PermissionSet | shown in action bar | hidden | per-command PermissionEvaluator gate |
| Mutation | a permitted action is tapped (confirm-gated) | content — enqueued to draft_outbox, summary/schedule update in place | error/inline | POST via outbox with a fresh durable key |
| Client link | the summary-header client link is tapped | navigate client-detail-360 | content | — |

## Testing (8 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| loading_shows_summary_schedule_shimmer | The loading state shows a summary + schedule shimmer with no error | medium |
| content_shows_summary_tabs_and_actions | Header shows client/product/status/outstanding/arrears/next-due; Schedule/Transactions/Details tabs; only permitted-and-held actions shown | high |
| empty_when_loan_not_found | A not-found panel with a back affordance when the loanId resolves to no account | low |
| error_when_load_fails_no_cache | The error state is shown and Retry re-reads the aggregate | medium |
| get_loan_reads_cache_first | Offline, the loan renders from cache (CACHE_FIRST_SWR) with a background-refresh badge when stale | high |
| repayment_enqueues_durable_idempotency | Make repayment enqueues with a FRESH durable key; sync replays via POST /v1/batches; schedule + outstanding update | high |
| approve_disburse_gated_by_status_and_permission | Approve shows only when APPROVE_LOAN is held; Disburse only after approval and when DISBURSE_LOAN is held | high |
| client_link_drills_to_360 | Tapping the client link navigates to client-detail-360 for the owning client | medium |
