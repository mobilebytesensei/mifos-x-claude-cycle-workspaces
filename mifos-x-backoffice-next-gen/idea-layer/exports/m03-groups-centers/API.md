<!-- source: screens/m03-groups-centers/api.yaml -->
<!-- source_hash: api=f630379596d0 -->
<!-- generated: 2026-07-21T18:15:55Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->

# M03 · Groups & Centers -- API Reference

> **Source of Truth**: `idea-layer/screens/m03-groups-centers/api.yaml` -> `endpoints[]`
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0

## Endpoints (13)

| Function | Method | Table | Auth | Params | Response | Cache |
|----------|--------|-------|------|--------|----------|-------|
| list_centers | GET | groups_centers | Yes | offset(Int), limit(Int), officeId(Long), staffId(Long), underHierarchy(String) | CenterPage: totalFilteredRecords, pageItems[] | CACHE_FIRST_SWR |
| list_groups | GET | groups_centers | Yes | offset(Int), limit(Int), officeId(Long), staffId(Long), status(String) | GroupPage: totalFilteredRecords, pageItems[] | CACHE_FIRST_SWR |
| get_group_detail | GET | groups_centers | Yes | groupId(Long), associations=all | GroupDetail: members, roles, GLIM loans, calendar | CACHE_FIRST_SWR |
| get_center_detail | GET | groups_centers | Yes | centerId(Long), associations=groupMembers | CenterDetail: groups, collection calendar | CACHE_FIRST_SWR |
| list_meetings | GET | groups_centers | Yes | groupId(Long) | Calendar[]: calendars, upcoming meetings | CACHE_FIRST_SWR |
| group_center_template | GET | cached_templates | Yes | officeId(Long), staffInSelectedOfficeOnly(Boolean) | GroupTemplate: offices, staff, roles | CACHE_FIRST_SWR |
| create_group | POST | framework_submit_drafts | Yes | externalId(String), name(String), officeId(Long) | CommandProcessingResult: commandId, resourceId | durable-outbox |
| activate_group_center | POST | framework_submit_drafts | Yes | groupId(Long), activationDate(String), dateFormat(String), locale(String) | CommandProcessingResult: commandId, resourceId | durable-outbox |
| assign_staff | POST | framework_submit_drafts | Yes | groupId(Long), staffId(Long) | CommandProcessingResult: commandId, resourceId | durable-outbox |
| manage_members | POST | framework_submit_drafts | Yes | groupId(Long), clientMembers([]) | CommandProcessingResult: commandId, resourceId | durable-outbox |
| associate_groups_to_center | POST | framework_submit_drafts | Yes | centerId(Long), groupMembers([]) | CommandProcessingResult: commandId, resourceId | durable-outbox |
| save_attendance | POST | framework_submit_drafts | Yes | meetingId(Long), clientsAttendance([]), meetingDate(String), dateFormat(String), locale(String) | CommandProcessingResult: commandId, resourceId | durable-outbox |
| save_collection_sheet | POST | framework_submit_drafts | Yes | centerId(Long), bulkRepaymentTransactions([]), bulkDisbursementTransactions([]), transactionDate(String), dateFormat(String), locale(String) | CommandProcessingResult: commandId, resourceId | durable-outbox |

## Error Handling

All endpoints follow standard error mapping:
- 401 -> Auth error (redirect to signin)
- 403 -> NoAuthorization -> permission-capability-engine drift refresh + re-resolve (not a crash)
- 404 -> NotFound error
- 422 -> Validation error
- 500 -> Server error (retryable; error state + Retry)

Notes:
- Reads (`list_*`, `get_*_detail`, `list_meetings`, `group_center_template`) are office-scoped and CACHE_FIRST_SWR so lists, members, and attendance sheets render offline from Room.
- Mutations are all `?command=` state transitions returning a `CommandProcessingResult`; they are enqueued through the offline outbox with a durable idempotency key and replayed as an atomic batch (`POST /v1/batches`) — exactly-once, zero double-post.
- Mutations are maker-checker aware: a `commandId` without a `resourceId` = submitted-for-approval (badged, not applied optimistically).
- `create_group` uses a client-generated `externalId` so group/center creation works fully offline; `manage_members` reads the m02-clients roster but never mutates a client record.

## Full Contracts

See `idea-layer/screens/m03-groups-centers/api.yaml` -> `endpoints[]` for complete request/response schemas.
