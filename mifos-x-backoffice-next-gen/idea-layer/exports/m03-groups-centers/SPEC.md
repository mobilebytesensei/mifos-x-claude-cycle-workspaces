<!-- source: screens/m03-groups-centers/ (v4.0 siblings: ui, docs, flow, api) -->
<!-- source_hash: ui=657dffc6bca7 docs=bb8835b74bb2 flow=5d5389f35725 api=f630379596d0 -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: 2.0.0 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M03 · Groups & Centers -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m03-groups-centers/` (v4.0 siblings)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: 2.0.0 | **Contract version**: 2.0.0 | **Status**: approved

---

## Capabilities

- **has_ui** — Compose master/detail groups-&-centers screen (list ⇄ detail with members, GLIM loans, calendar, attendance).
- **has_api** — Fineract groups/centers/meetings command-pattern endpoints (reads + `?command=` mutations).
- **has_offline_cache** — CACHE_FIRST_SWR reads of centers/groups/members/meetings + durable outbox for every mutation.
- **has_form_input** — per-member attendance toggles, collection-sheet capture, and template-driven create/edit + staff picker.
- **has_flow** — list → scope-toggle → detail → attendance/membership/lifecycle commands (offline-queued, gated).
- **requires_auth** — every read and command is authenticated and permission-gated per `*_GROUP` / `*_CENTER` / `*_MEETING` codes.

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| m03-groups-centers | GroupsCentersViewModel | Loading, Content, Empty, No-network, Error | Group/center hierarchy management for group lending: office-scoped list of centers (with nested groups) ⇄ a group/center detail (members, GLIM group loans, meeting calendar, attendance). Create/edit + staff picker route through dynamic-template-forms; every mutating command enqueues to the offline outbox with a durable idempotency key; every action renders VISIBLE / DISABLED / HIDDEN purely from permission-capability-engine. Archetype: master_detail. |

## State Model

### GroupsCentersViewModel

- **State**: selectedScope (GroupScope — centers | groups), selectedGroupCenter (GroupCenterDetail?), selectedMeetingId (Long?), attendanceDraft (Map<Long, AttendanceStatus>), attendanceDraftDirty (Boolean), collectionSheetDraft (CollectionSheetDraft?), collectionSheetDraftDirty (Boolean), listFetchedAt (Instant)
- **ScreenState**: Loading, Content, Empty, No-network, Error
- **Errors**: group_list_failed (retry=true; cache present → serve cached rows + soft error toast), attendance_queued_offline (queued in outbox, never dropped), command_rejected (rejected activate/assignStaff/associateClients replay → needs-attention-inbox), permission_drift_403 (permission-capability-engine refresh + re-resolve; prune now-denied actions), maker_checker_pending (commandId without resourceId → "Submitted for approval" badge, not applied optimistically)
- **Events**: NavigateToClient360(clientId) → m02-clients, NavigateToLoanAccount(loanId) → m04-loan-portfolio
- **Actions**: OnScopeTabClick (switch_scope_tab → in-VM selectedScope, no fetch), OnGroupCenterRowClick (open_group_center_detail → master-detail load + permission resolve), OnMemberRowClick (open_member_client → NavigateToClient360), OnGroupLoanRowClick (open_group_loan → NavigateToLoanAccount), OnSelectMeeting (select_meeting → re-derive attendance draft, no fetch), OnSaveCollectionSheetClick (save_collection_sheet → outbox), OnAttendanceToggle (toggle_member_attendance → in-VM draft dirty), OnSaveAttendanceClick (save_attendance → outbox), OnActivateClick (activate_group_center → outbox, optimistic activating-queued), OnAssignStaffClick (assign_staff → dynamic-template-forms picker → outbox), OnManageMembersClick (manage_members → associateClients/disassociateClients → outbox), OnCreateClick (start_group_center_creation → template create form → outbox), OnRetryClick (retry_load → re-fetch through Store5; 403 → permission-drift), Internal.ReceiveList, Internal.ReceiveNetworkStatus
- **DI**: core/store, core/data, core/database, core/permissions, core-base/store, core-base/network

## API Endpoints (13)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_centers | GET | offset(Int), limit(Int), officeId(Long), staffId(Long), underHierarchy(String) | CenterPage | 401, 403 | groups_centers |
| list_groups | GET | offset(Int), limit(Int), officeId(Long), staffId(Long), status(String) | GroupPage | 401, 403 | groups_centers |
| get_group_detail | GET | groupId(Long), associations(String) | GroupDetail | 401, 403, 404 | groups_centers |
| get_center_detail | GET | centerId(Long), associations(String) | CenterDetail | 401, 403, 404 | groups_centers |
| list_meetings | GET | groupId(Long) | Calendar[] | 401, 403 | groups_centers |
| group_center_template | GET | officeId(Long), staffInSelectedOfficeOnly(Boolean) | GroupTemplate | 401, 403 | cached_templates |
| create_group | POST | externalId(String), name(String), officeId(Long) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |
| activate_group_center | POST | groupId(Long), activationDate(String), dateFormat(String), locale(String) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |
| assign_staff | POST | groupId(Long), staffId(Long) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |
| manage_members | POST | groupId(Long), clientMembers([]) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |
| associate_groups_to_center | POST | centerId(Long), groupMembers([]) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |
| save_attendance | POST | meetingId(Long), clientsAttendance([]), meetingDate(String), dateFormat(String), locale(String) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |
| save_collection_sheet | POST | centerId(Long), bulkRepaymentTransactions([]), bulkDisbursementTransactions([]), transactionDate(String), dateFormat(String), locale(String) | CommandProcessingResult | 401, 403, 422 | framework_submit_drafts |

## Dependencies (Tier 2)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| permission-capability-engine | gates | true | resolves READ_/CREATE_/ACTIVATE_/ASSIGNSTAFF_/ASSOCIATECLIENTS_/TRANSFERCLIENTS_/CLOSE_GROUP + *_CENTER + *_MEETING + SAVEORUPDATEATTENDANCE_MEETING + SAVECOLLECTIONSHEET_CENTER/_GROUP; drives control VISIBLE/DISABLED/HIDDEN + 403 drift refresh |
| offline-sync-engine | uses | true | durable outbox + idempotency key + /v1/batches replay for activate / assignStaff / associateClients / saveOrUpdateAttendance / saveCollectionSheet (exactly-once) |
| dynamic-template-forms | uses | true | template-driven create/edit form + staff picker (GET /v1/groups|centers/template — office/staff/role option lists) |
| m02-clients | read_only | true | member roster source (associated-clients association) + Client 360 cross-link; never mutates a client record |
| m04-loan-portfolio | navigate | true | GLIM group-loan account 360 deep-link (read-only cross-link) |
| needs-attention-inbox | navigate | true | rejected activate/assignStaff/associateClients replay triage with the Fineract server error |

## Navigation

- **Route**: m03-groups-centers
- **From**: nav_root (drawer/rail gated on READ_GROUP / READ_CENTER), from_dashboard (group/center or collections field-officer tile)
- **To**: m02-clients, m04-loan-portfolio, dynamic-template-forms, needs-attention-inbox

## App-Shell

- **Bottom navigation**: hidden (reached as a permission-gated nav root, not a bottom-tab).
- **Top app bar**: visible (list header / detail header + scope tabs).
- **FAB**: visible (New group/center — `create_group_center_fab`, gated CREATE_GROUP / CREATE_CENTER).
- **Snackbar**: enabled (queued-offline / retry / permission-drift / command-rejected toasts).
- **Safe-area**: standard top-app-bar inset; content list scrolls under the bar.

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| List has rows | list load returns centers/groups | content | empty ("no groups or centers in office scope") | fold Store5 stream → ScreenState |
| Cache vs error | offline or list/detail fetch failed | no-network (serve cached rows + offline banner) | error + Retry (no cache) | CACHE_FIRST_SWR SourceOfTruth over Room |
| Command submit path | network available at submit | command applied online (batch) | queued-offline (durable outbox, "will sync when online" chip) | outbox enqueue + POST /v1/batches replay |
| Maker-checker | mutating command returns commandId without resourceId | "Submitted for approval" badge (not applied) | optimistic content refresh | tenant maker-checker gate |
| Permission drift | any read/command returns 403 NoAuthorization | route to permission-capability-engine refresh + re-resolve; prune now-denied actions | continue | surface stays alive, no crash |
| Rejected replay | a queued command is rejected on replay | route to needs-attention-inbox with the server error | accepted commands stay posted | outbox → needs-attention triage (never silently dropped) |

## Testing (14 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| centers_groups_list_offline | Cached centers+groups render offline via CACHE_FIRST_SWR with the offline banner | high |
| switch_scope_tab_centers_groups | Scope tab toggles the Centers ⇄ Groups view with no network call | medium |
| open_group_detail_shows_members_and_calendar | Opening a group shows members, GLIM loans, and the meeting calendar/attendance | high |
| attendance_queues_offline | Marked attendance queues to the outbox with a durable idempotency key, replays exactly-once | high |
| attendance_toggle_hidden_without_permission | Attendance toggles + Save gated by SAVEORUPDATEATTENDANCE_MEETING | high |
| open_group_loan_cross_links_to_m04 | Tapping a GLIM group-loan row opens the loan account in m04-loan-portfolio, no mutation | medium |
| select_meeting_rederives_attendance_sheet | Selecting a meeting re-derives the attendance draft for that date with no fetch | medium |
| collection_sheet_queues_offline | Collection-sheet capture enqueues saveCollectionSheet to the outbox offline, replays via /v1/batches | high |
| collection_sheet_hidden_without_permission | The collection-sheet action is gated by SAVECOLLECTIONSHEET_CENTER/_GROUP | medium |
| activate_gated_and_queued | Activate is gated by ACTIVATE_GROUP and enqueued offline (status chip: activating · queued) | high |
| manage_members_associates_clients | Membership edit enqueues associateClients from the m02-clients roster, no client mutated | medium |
| create_fab_hidden_without_permission | The New group/center FAB is absent without CREATE_GROUP / CREATE_CENTER | medium |
| error_group_list_failure_retry | A failed list fetch with no cache shows the error state + Retry | medium |
| rejected_command_routes_to_needs_attention | A server-rejected replay lands in needs-attention-inbox, never silently dropped | medium |
