<!-- source: screens/m05-savings-deposits-shares/ui.yaml -->
<!-- source_hash: ui=7b29e56d075a docs=e45681cd8617 flow=3abe2fb1da48 api=b5e488dda9c6 -->
<!-- generated: 2026-07-26T04:07:02Z -->
<!-- generated_from_feature_version: v2 -->
<!-- generated_from_contract_version: 2.0.0 -->
<!-- prior_version: — -->

# M05 · Savings, Deposits & Shares -- Implementation Specification

> **Source of Truth**: `idea-layer/screens/m05-savings-deposits-shares/` (ui + api + flow + docs + data-flow + tests)
> **Auto-generated by**: `/idea export --spec` -- Do not edit manually.
> **Feature version**: v2 | **Contract version**: 2.0.0 | **Status**: approved

---

## Screens (1)

| Screen | ViewModel | States | Description |
|--------|-----------|--------|-------------|
| M05 · Savings, Deposits & Shares | M05SavingsDepositsSharesViewModel | Loading, Content, Empty, Submitting, QueuedOffline, PendingApproval, Error, NoNetwork | Master-detail deposit & shares module: a filterable list of savings/FD/RD/share accounts and a per-account detail (balance, transactions, charges, on-hold funds) with the full Fineract command lifecycle. Reads ride CACHE_FIRST_SWR Store5; every money-movement command flows through the durable outbox with an exactly-once idempotency key; every control renders through PermissionEvaluator. |

## Capabilities

### has_ui
Compose master-detail screen (account list grouped by type + per-account detail with a permission-gated command bar).

### has_api
Consumes the live Fineract deposit/shares surface (savings/FD/RD/share accounts, transfers, standing instructions) — no server layer is built for this project.

### has_offline_cache
Account reads ride CACHE_FIRST_SWR Store5 over a Room `deposit_accounts` cache; every command is written to the durable RoomSubmitOutbox and replayed exactly-once via `POST /v1/batches`.

### has_form_input
Create/edit and every money-movement command are rendered by `dynamic-template-forms` from the entity `/template` (field defaults + product-driven option lists come from the server payload).

### has_flow
Multi-state command-submit lifecycle (submitting → content / pending_approval / queued_offline / error) plus error/drift recovery routes into needs-attention-inbox and permission-capability-engine.

### requires_auth
Every endpoint requires auth; list visibility gated by `READ_*` codes and each control by its exact `{ACTION}_{ENTITY}` command code.

## State Model

### M05SavingsDepositsSharesViewModel

- **ScreenState**: Loading, Content, Empty, Submitting, QueuedOffline, PendingApproval, Error, NoNetwork
  - **Loading** — fetching the account list or a selected account's detail (no cache yet); Store5 Loading (default/initial state).
  - **Content** — account list (savings/FD/RD/share grouped by type with balance + status chips) OR a selected account detail (balance header, transactions, charges, on-hold funds, permission-gated command bar); Store5 Content served instantly from cache, revalidated per FreshnessBands.
  - **Empty** — no accounts in the current office/client/product scope; offers "Open an account".
  - **Submitting** — a command is being posted ONLINE; command bar disabled with a per-command PENDING→SUBMITTED indicator.
  - **QueuedOffline** — command written to the durable RoomSubmitOutbox with its idempotency key; "N queued · will sync when online" banner; optimistic row marked pending-sync.
  - **PendingApproval** — a maker-checker tenant queued the command (2xx with commandId, empty changes); "Submitted for approval"; account NOT optimistically mutated; badge routes to M15.
  - **Error** — list/detail fetch failed with no cache, OR a command rejected (400 validation / 409 optimistic-lock / 403 permission-drift); surfaces the Fineract server error with a "Review failed" CTA and a Retry CTA.
  - **NoNetwork** — offline with no cached account data for the requested scope; captive-portal aware; cached reads (if any) still served.
- **Errors**: deposit_list_failed (fallback_cache, Retry), command_rejected (navigate → needs-attention-inbox), insufficient_balance (inline validation, submit blocked), maker_checker.queued (silent → pending_approval), permission.drift (navigate → permission-capability-engine, refreshPermissions), network.offline (fallback_cache / queued_offline)
- **Events**: NavigateToTemplateForm(entity, mode, clientId/productId/accountId), NavigateToNeedsAttention, NavigateToPermissionEngine, NavigateToM15Approvals
- **Actions**: OnSwitchAccountType (switch_account_type, transform_state), OnAccountRowClick (open_account_detail, navigate), OnOpenAccountFormClick / OnEditAccountFormClick (navigate → dynamic-template-forms), OnApproveClick / OnActivateClick / OnCloseAccountClick (call_api), OnRecordDepositClick / OnRecordWithdrawalClick (call_api), OnHoldAmountClick / OnReleaseAmountClick / OnPostInterestClick (call_api), OnPrematureCloseClick (call_api, FD/RD), OnSubscribeSharesClick / OnRedeemSharesClick (call_api, shares), OnAccountTransferClick / OnStandingInstructionClick (call_api), OnReviewFailedClick (open_needs_attention, navigate), OnRetryClick (retry_load → refreshFresh), Internal.ReceiveAccounts, Internal.ReceiveCommandResult, Internal.ReceiveNetworkStatus
- **DI**: core/model, core/network, core/database, core/store, core/data, core-base/store, core-base/network, cmp-network-monitor, PermissionEvaluator, RoomSubmitOutbox (offline-sync-engine), Store5 (org.mobilenativefoundation.store.store5)

## App-Shell

- **Bottom navigation**: visible — M05 tab shown only when the user holds ≥1 `READ_*` deposit-group code (fail-closed).
- **Top app bar**: visible.
- **FAB**: "Open account" — HIDDEN unless `CREATE_SAVINGSACCOUNT` / `CREATE_SHAREACCOUNT`.
- **Snackbar**: visible (offline-queue + command-result surfacing).

## API Endpoints (12)

| Function | Method | Params | Response | Errors | Table |
|----------|--------|--------|----------|--------|-------|
| list_savings | GET | offset(int), limit(int), clientId(long), sqlSearch(string) | SavingsAccountDto (list) | 401, 404, 500 | deposit_accounts |
| savings_template | GET | clientId(long), productId(long) | SavingsAccountTemplateDto | 401, 404, 500 | deposit_accounts |
| savings_lifecycle | POST | accountId(path), command(query), SavingsCommandRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |
| savings_transaction | POST | accountId(path), command(query), SavingsTransactionRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |
| list_fixed_deposits | GET | offset(int), limit(int), clientId(long) | FixedDepositAccountDto (list) | 401, 404, 500 | deposit_accounts |
| fixed_deposit_lifecycle | POST | accountId(path), command(query), DepositCommandRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |
| list_recurring_deposits | GET | offset(int), limit(int), clientId(long) | RecurringDepositAccountDto (list) | 401, 404, 500 | deposit_accounts |
| recurring_deposit_transaction | POST | accountId(path), command(query), SavingsTransactionRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |
| list_share_accounts | GET | offset(int), limit(int), clientId(long) | ShareAccountDto (list) | 401, 404, 500 | deposit_accounts |
| share_lifecycle | POST | accountId(path), command(query), ShareCommandRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |
| account_transfer | POST | AccountTransferRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |
| standing_instruction | POST | StandingInstructionRequest(body) | CommandProcessingResult | 400, 403, 409, 500 | draft_outbox |

## Dependencies (Tier 1)

| Feature | Type | Required | Check |
|---------|------|----------|-------|
| dynamic-template-forms | library | true | Renders create/edit + every money-movement command form from the entity `/template`. |
| offline-sync-engine | library | true | Owns the shared RoomSubmitOutbox (`draft_outbox`, Room v11) + OfflineSubmitSyncer replay via `POST /v1/batches`. |
| permission-capability-engine | library | true | PermissionEvaluator resolves each control against its exact `{ACTION}_{ENTITY}` code; refreshPermissions() on drift. |

## Navigation

- **Route**: m05-savings-deposits-shares
- **From**: nav_root (M05 tab, gated on ≥1 `READ_*` deposit code), from_client_360 (M02 client account row), from_sync (offline-sync-engine / needs-attention-inbox deep-link)
- **To**: dynamic-template-forms, offline-sync-engine, needs-attention-inbox, permission-capability-engine, m15-approvals-makerchecker

## Flow Logic (6 decisions)

| Decision | Condition | True Path | False Path | Impl |
|----------|-----------|-----------|------------|------|
| Control visibility | `canDo({ACTION}, {ENTITY})` holds | Control VISIBLE-enabled | Control HIDDEN (fail-closed) | PermissionEvaluator (permission-capability-engine) |
| Command dispatch | Network available at submit | submitting (online POST) | queued_offline (durable outbox, replays exactly-once) | OfflineSubmitSyncer / RoomSubmitOutbox |
| Command result | 200 with resourceId | content (optimistic row confirmed) | commandId-without-resourceId → pending_approval; 4xx/5xx → error → needs-attention | Internal.ReceiveCommandResult |
| Maker-checker | command is maker-checkerable | pending_approval (badge → M15, no optimistic mutation) | content (optimistic confirm) | Internal.ReceiveCommandResult |
| Withdrawal guard | amount ≤ available balance | enqueue withdrawal | insufficient_balance inline block, no outbox row | client-side available-balance guard (server final authority) |
| Permission drift | command returns 403 NoAuthorization | route permission-capability-engine → refreshPermissions() → prune control | continue | data-flow error_paths (permission.drift) |

## Testing (16 scenarios)

| ID | Scenario | Priority |
|----|----------|----------|
| list_loads_grouped_by_type | Account list loads grouped by type with balance + status chips | high |
| empty_scope_offers_open | Empty scope renders with a gated "Open an account" CTA | medium |
| deposit_gated_offline | Deposit VISIBLE-enabled, withdrawal HIDDEN offline (fail-closed grant) | high |
| deposit_queues_exactly_once | Offline deposit → durable outbox → kill/restart → replays exactly-once | high |
| withdrawal_insufficient_balance | Over-balance withdrawal blocked inline, no outbox row | medium |
| approve_then_activate_lifecycle | Approve then activate lifecycle transitions | high |
| maker_checker_deposit_pending_approval | Maker-checker deposit → pending_approval, no optimistic mutation | high |
| hold_then_release_amount | Hold amount then release from the on-hold funds row | medium |
| release_amount_gated | Release control HIDDEN without RELEASEAMOUNT grant | medium |
| post_interest_capitalizes | postInterestAsOn capitalizes interest + appends ledger row | medium |
| fixed_deposit_premature_close | FD premature-close preview then command through the outbox | medium |
| share_redeem_exactly_once | Share redeem reuses idempotency key, applied exactly-once | high |
| transfer_and_si_gated | Transfer HIDDEN without grant; SI follows its own code | medium |
| rejected_command_routes_to_needs_attention | Rejected command surfaces error + routes to needs-attention-inbox | high |
| permission_drift_on_command | 403 drift routes to permission-capability-engine, control pruned | high |
| offline_no_cache_shows_no_network | Cold offline scope renders no_network with Retry | medium |
