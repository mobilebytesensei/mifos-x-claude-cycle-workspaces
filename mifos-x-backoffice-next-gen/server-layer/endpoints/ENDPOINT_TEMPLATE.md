# template_meta
# template_version: "2.86.0"
# template_path: "templates/blueprints/workspace-project/server-layer/endpoints/ENDPOINT_TEMPLATE.md"
# last_modified: "2026-03-19"

# ${CATEGORY_NAME} Endpoints

> **Category**: ${CATEGORY_NAME}
> **Endpoints**: ${ENDPOINT_COUNT}
> **Service**: `${SERVICE_NAMES}`

---

## POST /authentication

**Purpose**: Login with username and password

**Service**: `AuthenticationService.authenticate()`

**Request**:
```json
{
    "username": "string",
    "password": "string"
}
```

**Response**:
```json
{
    "userId": 123,
    "username": "john_doe",
    "isAuthenticated": true,
    "base64EncodedAuthenticationKey": "encoded_key",
    "officeName": "Head Office"
}
```

**DTO**: `User`

**Errors**:
| Code | Message | Handling |
|------|---------|----------|
| 401 | Unauthorized | Show login error |
| 403 | Forbidden | Show permission error |
| 500 | Server error | Show retry option |

---

## Related

- Design Layer: [${FEATURE}/API.md](../../idea-layer/exports/${FEATURE}/API.md)
- Patterns: [CLIENT_PATTERNS.md](../CLIENT_PATTERNS.md)
- Errors: [ERROR_HANDLING.md](../ERROR_HANDLING.md)

---

## Template Instructions

When generating endpoint documentation, include for EACH endpoint:

1. **Header**: `## ${METHOD} ${PATH}`
2. **Purpose**: One-line description
3. **Service**: `ServiceName.methodName()`
4. **Query Parameters** (if applicable):
   ```markdown
   | Parameter | Type | Required | Values |
   |-----------|------|:--------:|--------|
   | `param` | String | Yes | "value1", "value2" |
   ```
5. **Request** (for POST/PUT/PATCH):
   ```json
   { "field": "value" }
   ```
6. **Response**: JSON sample with all fields
7. **DTO**: Kotlin/TypeScript class name
8. **Errors**: Common error codes and handling

---

**Generated by**: /project-verify or /server command
