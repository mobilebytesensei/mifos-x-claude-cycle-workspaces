# App Store Connect export-compliance answers — MifosSave (promote 1:1)
platform: ios
uses_encryption: true
# Only standard encryption (HTTPS/TLS) for authentication and data sync — no proprietary crypto.
exempt: true
exemption_category: "Standard encryption (TLS/HTTPS) — qualifies for exemption under CCATS 5D992"
uses_proprietary_encryption: false
available_on_french_store: true
# ITSAppUsesNonExemptEncryption should be set to NO in Info.plist.
info_plist_key: "ITSAppUsesNonExemptEncryption"
info_plist_value: false
