# ============================================================
# CAPABILITY_STATE.yaml — Project-Side Idea Capability Tracker
# ============================================================
# Generated by: /idea init (Phase 1 backfill) and updated by each
#               /idea {capability} runtime on success (Phase 3+).
# Read by:      /idea dashboard (Layer Sanity row + Capabilities grid)
#               PROJECT_MIGRATION (drift detection between framework
#               registry version and per-capability artifact_template_version_at_run)
# Spec:         core/registries/IDEA_CAPABILITY_REGISTRY.yaml (framework-side)
# Plan:         PLAN-fw-260506-idea-capability-registry
#
# DO NOT hand-edit unless you know what you're doing — this is
# auto-maintained. To force re-init: delete the file and run /idea init.
# ============================================================

schema_version: "1.0.0"
project: "mifos-x-mifos-x-group-banking"
generated: "2026-05-06"
generator: "/idea init (Phase 1 backfill)"
framework_version_at_init: "2.86.0"

# ============================================================
# Capabilities used (rows added by detection or per-run writers)
# ============================================================

capabilities_used:

  enrich:
    last_run: "inferred-2026-05-06"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "Glob idea-layer/screens/*.yaml → ≥1 file"
    artifact_count: 25

  sync:
    last_run: "inferred-2026-05-06"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "idea-layer/flows/*.yaml ≥1 OR prototype/index.html exists OR APP_FLOW.mmd exists"
    artifact_count: 9

  analytics:
    last_run: "inferred-2026-05-06"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "idea-layer/TAG_REGISTRY.yaml exists AND events: array has ≥1 entry"
    artifact_count: 1

  plan:
    last_run: "inferred-2026-05-06"
    framework_version_at_run: "<2.86.0"
    capability_version_at_run: "unknown"
    artifact_template_version_at_run: "unknown"
    backfilled: true
    detection_rule: "idea-layer/idea-plan.yaml exists AND quality: field is set"
    artifact_count: 1

  completeness:
    last_run: "2026-05-07T10:15:00Z"
    framework_version_at_run: "2.86.0"
    capability_version_at_run: "1.0.0"
    artifact_template_version_at_run: "v1"
    backfilled: false
    detection_rule: "Glob idea-layer/COMPLETENESS_LOG_*.md → ≥1 file"
    runs: 3
    gaps_found_run2: 9
    gaps_fixed_run2: 9
    wiring_fixes_run2: 6
    flow_fixes_run2: 2
    docs_fixes_run2: 2
    gaps_found_run3: 35
    gaps_fixed_run3: 35
    click_intents_typed_run3: 35
    screens_patched_run3: 12

# ============================================================
# Init metadata (audit trail)
# ============================================================

init:
  ran_at: "2026-05-06"
  detected_count: 4
  not_detected_count: 10
  skipped_due_to_error: []

# ============================================================
# Phase 3+ fields (placeholder — populated when feature ships)
# ============================================================

drift_check:
  last_run: null
  drifted_capabilities: []

per_capability_writes_enabled: false
