# BEHAVIOR_VERDICTS.yaml — on-device behavior verification results
# Written by the device-test / behavior-verification runtime (RULE-IMPL-BEHAVIOR-EXECUTED-001,
# IA3-7 device-truth). Consumed by .claude-runtime/lib/idea-graph-audit/behavior-signals.ts
# (evalBehavior signal B). Each verdict is a FRESH on-device capture taken AFTER
# `adb shell am force-stop`, with device-APK md5 == built-APK md5, read back and confirmed.
#
# Build under test:
#   apk:        cmp-android/build/outputs/apk/demo/debug/cmp-android-demo-debug.apk
#   applicationId (installed): org.mifos.groupbanking   (fork overrides the .demo/.debug suffix)
#   built_md5:  c6cebed7756a67559407110c8025ca44
#   device_md5: c6cebed7756a67559407110c8025ca44   (emulator-5554 / Pixel_10_Pro — MATCH)
#   launcher:   cmp.android.app.MainActivity  (launched clean, no FATAL in logcat)
#   verified_at_run: 2026-07-31T14:22:54Z

verdicts:
  - feature: login-signup
    verdict: real-behavior
    verified_at: "2026-07-31T14:22:54Z"
    evidence: |
      FRESH on-device capture after `am force-stop org.mifos.groupbanking`; device-APK
      md5 == built md5 (c6cebed7756a67559407110c8025ca44), read back and confirmed.
      Rendered Content state on launch: "Welcome to MifosX", Sign In / Sign Up tabs,
      "Email, Phone or Username" + "Password" fields, "Forgot password?" link, "Sign In" button
      (screenshot 01-launch.png).
      on_click behaviors executed on-device (real state changes, not stubs):
        - Tab switch (Sign In <-> Sign Up): tapping "Sign Up" transformed the form to the
          signup layout — a11y nodes "Full name input" + "Email or phone number input" +
          "Password input" appeared, tab label "Sign Up" (screenshot 02-signup-tab.png).
          Real transform_state / mode switch fired.
        - Login button (OnLoginClick -> call_api): filled identifier + password; the button was
          gated (enabled only when the fields validate), tap drove Loading (spinner rendered,
          screenshot 03-filled/04-after-login.png), the Fineract auth API was invoked, and the
          result was mapped to a proper Error state with retry copy "Something went wrong on our
          end. Please retry." + content-desc="Error" (screenshot 05-login-result.png). The full
          validate -> loading -> call_api -> error-mapping path is wired and executes; the auth
          failure is a backend/credentials condition (demo.openmf.org rejected demo/demo and
          mifos/password), NOT a handler stub.
      Verdict: handlers fire with real effects and real state transitions -> real-behavior.

  # ── The following 3 batch features could NOT be device-verified this run ──
  # They are reachable ONLY AFTER a successful backend login (their screens live behind the
  # authenticated NavHost). The demo Fineract backend (demo.openmf.org) returned a server error
  # for every credential pair tried (demo/demo, mifos/password), so navigation never reached
  # them. This is an EXTERNAL-DEPENDENCY block (backend auth/seed unavailable from this
  # emulator), truthfully surfaced — not a handler defect and not a stub. Recorded as
  # pending-device-verify (non-pass) so no phantom-truth green is claimed.
  - feature: group-list
    verdict: pending-device-verify
    verified_at: "2026-07-31T14:22:54Z"
    evidence: |
      Blocked pre-nav: requires successful backend login to reach the authenticated group-list
      screen. demo.openmf.org returned "Something went wrong on our end" for all creds tried.
      pending-device-verify (external-dep: backend auth/seed). Re-verify once a reachable/seeded
      Fineract tenant is available.
  - feature: member-list
    verdict: pending-device-verify
    verified_at: "2026-07-31T14:22:54Z"
    evidence: |
      Blocked pre-nav: group-scoped member-list is reachable only via group-list -> group-dashboard
      after login. Backend auth unavailable this run (see group-list). pending-device-verify
      (external-dep: backend auth/seed).
  - feature: field-officer-dashboard
    verdict: pending-device-verify
    verified_at: "2026-07-31T14:22:54Z"
    evidence: |
      Blocked pre-nav on TWO gates: (1) successful backend login (unavailable this run), and
      (2) the documented reachability gap — the screen is reachable ONLY via organizer-dashboard's
      Field-Officers quick-nav tile, which is visible_when fieldOfficerEnabled (default false), so
      it needs an ORGANIZER account + companion fieldOfficerEnabled=true seed. pending-device-verify
      (external-dep: backend auth/seed + fieldOfficerEnabled flag). See the screen's
      maestro/field-officer-dashboard.assertions.md "Hard gap" note.
