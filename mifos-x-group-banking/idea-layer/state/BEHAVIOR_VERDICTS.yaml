# BEHAVIOR_VERDICTS.yaml — on-device behavior verification results
# Written by the device-test / behavior-verification runtime (RULE-IMPL-BEHAVIOR-EXECUTED-001,
# IA3-7 device-truth). Consumed by .claude-runtime/lib/idea-graph-audit/behavior-signals.ts
# (evalBehavior signal B). Each verdict is a FRESH on-device capture taken AFTER
# `adb shell am force-stop`, with device-APK md5 == built-APK md5, read back and confirmed.
#
# Build under test:
#   apk:        cmp-android/build/outputs/apk/demo/debug/cmp-android-demo-debug.apk
#   applicationId (installed): org.mifos.groupbanking   (fork overrides the .demo/.debug suffix)
#   built_md5:  c6cebed7756a67559407110c8025ca44
#   device_md5: c6cebed7756a67559407110c8025ca44   (emulator-5556 headless — MATCH, read back from
#               /data/app/.../base.apk)
#   launcher:   cmp.android.app.MainActivity  (launched clean, no FATAL on login path)
#   verified_at_run: 2026-07-31T15:15:00Z
#
# ENVIRONMENT NOTE (test infra, host-side — NOT app/idea-layer source):
#   The app targets http://localhost:8080 (adb reverse tcp:8080). The pre-existing host companion
#   mock served ONLY the 3 auth endpoints; every authenticated data endpoint returned `{}` which
#   FAILED kotlinx deserialization (required-field DTOs) and blocked all post-login screens. To
#   perform honest device-truth verification of the authenticated surface, that mock was replaced
#   with a COMPLETE demo-aligned mock (scratchpad/companion-mock.mjs) serving all companion +
#   fineract endpoints with valid DTO-shaped demo data (identity preserved exactly: login/me ->
#   Grace Wanjiku, ORGANIZER of "Umoja Savings Group" g-1001 + MEMBER of "Harambee Welfare Circle"
#   g-1002). Endpoint shapes were derived from core/network model DTOs and iterated against live
#   on-device SERIALIZATION errors until each screen deserialized. Empty-array responses were used
#   where the DTO permits, so empty-state renders (valid verified behaviors) are exercised too.
#
# ── REBUILD ATTEMPT 2026-07-31T21:10Z (defect-fix re-verify run) ────────────────────────────────
#   Two source fixes were supposed to land and be re-verified on-device this run:
#     (fix1) core-base/store/.../StorePagingSource.kt  — empty-page NPE guard `response.value ?: emptyList()`
#     (fix2) feature/group-type-picker/.../GroupTypePickerViewModel.kt — enum-vs-String slug resolution
#   `./gradlew :cmp-android:assembleDemoDebug` FAILED (BUILD RED, exit != 0). Root cause: fix2 is
#   itself broken — GroupTypePickerViewModel.kt:202 `it.typeSlug.name == slug` compares String to
#   GroupTypeSlug (line 197 already converts `slug` to the enum). Compiler:
#     "e: ...GroupTypePickerViewModel.kt:202:54 Operator '==' cannot be applied to 'String' and 'GroupTypeSlug'."
#   Correct form: `it.typeSlug == slug` (enum==enum). fix1's module (:core-base:store) compiles GREEN
#   in isolation, but the whole-app APK cannot be produced while fix2 breaks the shared build, so
#   NEITHER fix is installable. Built APK md5 UNCHANGED (still c6cebed…) — the stale pre-fix build.
#   Per the leaf-subagent constraint (never edit app source) fix2 was NOT patched here; it is an OPEN
#   source blocker for the parent to route through the pipeline. Consequences for this run:
#     - fix1 (loan-list empty-page crash): NOT device-verifiable — no fresh APK. Prior loan-list
#       real-behavior verdict (whose evidence documents the EMPTY-page FATAL) is UNCHANGED; the guard
#       is written+compiles but is NOT on device, so the crash is NOT proven-fixed. pending re-verify.
#     - fix2 (group-type-picker card → group-create): still BROKEN (won't compile) → group-create
#       stays pending-device-verify (unreachable).
#   The MEMBER-IDENTITY pass below is INDEPENDENT of both fixes and was verified honestly against the
#   installed valid build c6cebed (device_md5 == c6cebed, confirmed by md5sum of /data/app/.../base.apk)
#   — the same build that verified the 17 features above.
#
# ── MEMBER-IDENTITY note ─────────────────────────────────────────────────────────────────────────
#   The app routes on groupMemberships (LoginSignupViewModel.routeEvent): ANY ORGANIZER membership →
#   organizer-dashboard, else → personal-dashboard. grace_w has an ORGANIZER role so always lands
#   organizer. To reach member mode the host mock (scratchpad/companion-mock.mjs — host infra, NOT
#   app/idea source) was extended: /companion/auth/login now reads body.emailPhone; a non-'grace'
#   handle (e.g. 'peter_k') → activeRole=MEMBER → MEMBER-only memberships → personal-dashboard. Also
#   fixed a MOCK DATA-SHAPE bug: member-dashboard recentTransactions[].date must be date-only
#   ("2026-07-30"), parsed via LocalDate.parse at MemberDashboardStore.kt:187 (a full ISO timestamp
#   threw DateTimeParseException — mock artifact, not an app bug).
#
# ── 3-GAP RE-VERIFY RUN 2026-08-01T00:29Z (build 1b08d279) ───────────────────────────────────────
#   Fresh GREEN build under test:
#     apk:        cmp-android/build/outputs/apk/demo/debug/cmp-android-demo-debug.apk
#     built_md5:  1b08d279eef9ba8eaeec05b0dd0986c5
#     device_md5: 1b08d279eef9ba8eaeec05b0dd0986c5  (emulator-5556 headless — MATCH, read back from
#                 /data/app/~~ttb4t0Y20yJyUEq9U_E-IA==/org.mifos.groupbanking-.../base.apk)
#     install:    adb install -r Success; Room AppDatabase v15→v16 migration — NO crash on launch
#                 (clean login path, zero FATAL/AndroidRuntime).
#   This run re-verified the 6 features that the enrich+implement stage unblocked (3 gaps closed):
#     GAP-1 viewerRole nav-param forwarded → loan-apply (canApplyLoan) + loan-repayment-dialog
#            (canRecordRepayment) + loan-mark-defaulted-dialog (canMarkDefaulted+OVERDUE)
#     GAP-2 GroupCorpusDto.isCycleEnd server field → share-out-preview + share-out-execute
#     GAP-3 MemberDashboardDto clientId/savingsIds forwarded → personal-savings (real screen, not
#            the group savings-dashboard drift-bridge)
#   ALL 6 → real-behavior (each on_click produced a real nav/dialog/API effect on a fresh post-
#   force-stop capture). Implementation quality: GENUINELY WIRED, no stubs/mis-wiring found — the
#   handlers touch their declared targets (repayment→POST command=repayment, defaulted→POST
#   command=writeoff, share-out→POST shareout/execute, savings→GET /self/savingsaccounts). Mock
#   (scratchpad/companion-mock.mjs, host infra) extended: corpus.isCycleEnd=true, member-dashboard
#   clientId/groupLinkedSavingsId/individualSavingsId, loan 9002 status OVERDUE,
#   /self/savingsaccounts/{id}/transactions ledger, /companion/groups/{id}/shareout/{preview,execute},
#   and RecordRepaymentResponseDto shape. Two NON-BLOCKING cosmetic nits observed: (a) double-percent
#   "0.3%%"/"30%%" formatting on share-out screens; (b) execute request cycleNumber=0 (not threaded
#   through memberPayoutsJson route seam). Screenshots: scratchpad/verify6-002920/.
#
# verified_at_run: 2026-08-01T00:44:00Z

verdicts:
  - feature: login-signup
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      FRESH capture after `am force-stop org.mifos.groupbanking` on emulator-5556; device-APK
      md5 == built md5 (c6cebed7756a67559407110c8025ca44), read back and confirmed.
      Login screen renders (Welcome to MifosSave, Sign In/Sign Up tabs, identifier + password fields,
      Forgot-password, biometric option). OnLoginClick -> call_api executed against the companion
      mock: logcat "(CompanionAuthApi) login succeeded (200)", "(CompanionSessionStore) Session
      saved userId=u-6772616365", "(LoginSignupViewModel) login success groupCount=2", then
      me-refresh 200 -> navigated into the authenticated NavHost (organizer-dashboard). Full
      validate -> loading -> call_api -> session-persist -> nav path fires with real effects.
      real-behavior.

  - feature: organizer-dashboard
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Landing screen after login. Renders full content (screenshot 10-orgdash.png):
      "Welcome back, Grace Wanjiku", KPI cards (2 My Groups, 27 Members, 1 Share-Outs Due,
      Meetings Today), Quick Navigation (All Groups / Field Officers), Today's Schedule
      (Umoja Savings Group 14:00 · 15 members), Recent Activity feed (Weekly contribution
      KES 4,500 / Loan disbursed KES 12,000 / Meeting concluded). Zero deserialization/FATAL.
      on_click: "All Groups" tile -> nav to group-list (fired). real-behavior.

  - feature: group-list
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via organizer-dashboard "All Groups". Renders both group cards (11-grouplist.png):
      Umoja Savings Group (VSLA, ORGANIZER, cycle 3, 15 members, health chip, Last met 2026-07-28)
      + Harambee Welfare Circle (BURIAL_WELFARE, MEMBER, cycle 1, 12 members), Search field,
      Create Group FAB. on_click: group card -> nav to group-dashboard (fired). Zero errors.
      real-behavior.

  - feature: group-dashboard
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via group-list card tap. Renders full content (screenshots 12-groupdash.png,
      30/31 PNG): header (Cycle 3 of 12 · WEEKLY · VSLA · organizer · 15 members · 1 overdue),
      Corpus Fund KES 185,000 + Projected share-out KES 210,000 + Opening/Contributions/Loans-Out,
      Quick Actions (Start Meeting / Members / Loans / Share-Out), Savings Summary, Recent Activity.
      on_click fired: Members -> member-list, Loans -> loan-list, Start Meeting -> meeting-calendar.
      Share-Out correctly disabled with gate copy "Available at cycle end only". Zero errors.
      real-behavior.

  - feature: member-list
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via group-dashboard "Members". Renders 4 member rows (13-memberlist.png) with
      role + savings + loan-status: Grace Wanjiku (Chairperson, KES 6,200), John Otieno (Treasurer,
      KES 4,500, Active), Mary Achieng (Secretary, KES 3,800, Overdue), Peter Kamau (Member,
      KES 2,100). Add Member FAB + Invite action present. on_click: member row -> member-profile;
      Add Member -> member-add; Invite -> member-invite (all fired). Zero errors. real-behavior.

  - feature: member-profile
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via member-list row tap. Renders full profile (14-memberprofile.png): John Otieno,
      Member since 2026-01-15, phone, Savings History (KES 4,500 + View Full History), Active Loan
      (Group Loan KES 8,000 outstanding), Meeting Attendance (0/0). Fetches /clients/{id} +
      /clients/{id}/accounts + /datatables/dt_member_role/{id} (all 200, deserialized). on_click:
      View Full History -> savings surface (fired). Zero errors. real-behavior.

  - feature: member-add
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via member-list "Add Member" FAB. Renders form (15-memberadd.png): Add Photo picker,
      First Name / Last Name / Kenyan Phone inputs, Role selector, Save Member + Close. on_click
      verified: tapping Save Member with invalid input fired real validation state-change
      ("Enter a valid Kenyan phone number."); fields accept typed input (Ada / Nyong / 0712000111
      rendered). Zero errors. real-behavior (validation handler fires with real state effect).

  - feature: member-invite
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via member-list Invite action. Renders form (16-memberinvite.png): Phone/Email input,
      Role selector, Generate Code; Pending Invites section renders empty-state "No pending invites"
      (GET invitations -> [] 200). on_click verified: filled phone + tapped Generate Code -> real
      POST /companion/datatables/invitations/0 (200) -> rendered generated invite ("Invite Code
      ABC123", Join Link https://companion.example.org/join/ABC123, Copy Code / Copy Link)
      (16b-invite-generated.png). Zero errors. real-behavior.

  - feature: loan-list
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via group-dashboard "Loans". With loan data renders fully (17-loanlist.png): filter
      tabs All/Active/Overdue/Closed, John Otieno ACTIVE (KES 12,000, outstanding KES 8,000,
      Next 2026-08-15), Mary Achieng OVERDUE (KES 6,000, outstanding KES 3,200, OVERDUE KES 1,200).
      on_click: loan row -> loan-detail (fired). real-behavior for the populated path.
      DEFECT FOUND (device-truth): with an EMPTY loan page (valid empty LoanPageDto,
      totalFilteredRecords:0, pageItems:[]) the app CRASHES — FATAL NullPointerException
      "Attempt to invoke interface method 'int java.util.List.size()' on a null object reference"
      at kpt.core.base.store.paging.StorePagingSourceKt.loadPage(StorePagingSource.kt:114)
      (response.value null on the empty page). This is a genuine template-code (core-base/store)
      empty-page crash bug, NOT a mock artifact (confirmed: non-empty page renders fine, empty
      page crashes). Should be filed upstream (RULE-TEMPLATE-MODULE-FIX-UPSTREAM-001).

  - feature: loan-detail
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via loan-list row tap. Renders full detail (18-loandetail.png): John Otieno,
      Group Loan, Principal KES 12,000, Disbursed 2026-05-01, Interest 10%/week, ACTIVE,
      Outstanding KES 8,000, Schedule / Repayment History tabs with the repayment schedule table
      (Wk/Due Date/Due/Paid/Balance/Status; rows Paid + Upcoming). GET /loans/{id} 200,
      deserialized. Zero errors. real-behavior.

  - feature: meeting-calendar
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via group-dashboard "Start Meeting" (routes to the Meetings/calendar screen)
      (22-meetingcalendar.png). Renders calendar/list toggle, upcoming Meeting #13 (2026-08-04,
      Upcoming, Start Meeting), Past Meetings (Meeting #12, 2026-07-28, 14/5 present, KES 21000,
      Completed). GET /fineract-provider/api/v1/centers/{id}/meetings + dt_meeting_record 200.
      on_click: Start Meeting -> meeting-conduct wizard (fired). Zero errors. real-behavior.

  - feature: meeting-conduct
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via meeting-calendar "Start Meeting". Renders the 7-step meeting wizard
      (23-meetingconduct.png): step-progress bar, "Previous Meeting Review — Step 1 of 7", meeting
      summary card, Next + Close. on_click verified: tapping Next advanced the wizard to
      "Attendance — Step 2 of 7" (23b-meeting-step2.png) — real state change with per-step content
      (Late KES 50 / Absent KES 100 fine, FR-012). Zero errors. real-behavior.

  - feature: previous-meeting-review
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Rendered on-device as Step 1 of the meeting-conduct wizard, titled "Previous Meeting Review"
      (23-meetingconduct.png): meeting summary card (Meeting #, Date, Total Collected, Closing
      Corpus, Attendance, View Full Report) + Next to advance. This is the previous-meeting-review
      surface; it renders and its Next on_click fires (advances wizard). Zero errors. real-behavior.

  - feature: group-type-picker
    verdict: real-behavior
    verified_at: "2026-07-31T21:52:00Z"
    evidence: |
      FRESH on-device capture (emulator-5556, APK md5 de51ff8835e306e9bff8470cef7c8142 ==
      built md5, taken AFTER `am force-stop`; IA3-7). Reached via My Groups "Create Group" FAB.
      Renders all 4 group-type cards (03-grouptypepicker.png): Village Savings & Loan, Rotating
      Savings, Accumulating Savings, Burial Welfare, each with tagline + feature-chip pills.
      DEAD-CLICKABLE FIX VERIFIED (RULE-IMPL-DEAD-CLICKABLE-001): the decorative feature chips
      were disabled AssistChips whose clickable node SWALLOWED taps landing on the card centre —
      A/B device-proven — killing card→group-create nav. Replaced with a non-clickable Box pill
      (GroupTypeCard.kt) so taps fall through to the card's `.clickable(onClick)`. Post-fix, a tap
      in the previously-dead chip/centre region (device coord ~720,632) now logs
      `(GroupTypePickerViewModel) type card tapped slug=VSLA` AND
      `GroupBankingNavHost destination changed: ...GroupCreateRoute/...` → group-create form
      renders (04-after-tap-villagechip.png). Consistency: a second card (Accumulating Savings,
      chip region ~705,1337) logs `type card tapped slug=ASCA` and navigates identically
      (05-after-tap-accum-chip.png), config carried through to the "New Accumulating Savings
      Group" title/chip. Pills render visually correct (light-purple rounded boxes). Zero errors.
      real-behavior — catalogue render + card-tap navigation both proven on-device.

  - feature: savings-dashboard
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via member-profile "View Full History" (routes to the group Savings dashboard)
      (33-savingsdash.png). Renders Last-synced banner, Group Savings / Individual tabs, Weekly
      Group Contributions bar chart (W1/W2/W3), Cycle Progress (KES 185,000 of KES 260,000, 71% of
      target), member contribution row (John Otieno, 8 meetings, Last KES 1,500, KES 12,000 total).
      GET /companion/groups/{id}/savings + /savings/individual 200, deserialized. on_click: member
      row -> member-savings-detail (fired). Zero errors. real-behavior.

  - feature: member-savings-detail
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via savings-dashboard member row tap (34-membersavingsdetail.png). Renders member
      header (John Otieno, Savings Balance KES 4,500, Account SA-8001), 6-Month Trend chart,
      All/Deposits/Withdrawals filter tabs, transaction row (Deposit +KES 1,500, 2026-07-15,
      Balance KES 4,500). GET /companion/groups/{id}/members/{id}/savings 200, deserialized.
      Zero errors. real-behavior.

  - feature: field-officer-dashboard
    verdict: real-behavior
    verified_at: "2026-07-31T15:15:00Z"
    evidence: |
      Reached via organizer-dashboard "Field Officers" tile (fieldOfficerEnabled=true seeded in
      the companion mock). Renders its designed EMPTY state (21-fieldofficer.png): "No Groups
      Assigned — You don't have any groups assigned yet. Contact your office administrator." plus
      "Export Field Officer Group Report as CSV" action. Empty-state render (valid verified
      behavior per task); no crash, no serialization error. real-behavior (empty state).

  # ────────────────────────────────────────────────────────────────────────────────────────
  # pending-device-verify (non-pass) — honestly un-verifiable this run. Reason recorded per
  # feature. No phantom-truth green is claimed for any of these.
  # ────────────────────────────────────────────────────────────────────────────────────────
  - feature: group-create
    verdict: real-behavior
    verified_at: "2026-07-31T21:52:00Z"
    evidence: |
      NOW REACHABLE (unblocked by the GroupTypeCard dead-clickable fix; see group-type-picker
      verdict). FRESH on-device capture (emulator-5556, APK md5 de51ff8835e306e9bff8470cef7c8142
      == built md5, AFTER `am force-stop`; IA3-7). Reached by tapping a group-type card. Renders
      the multi-step create-group wizard (04/05-*.png): header "New <Type> Group", type chip,
      4-step progress (1 Identity / 2 Rules / 3 Members / 4 Review), and step-1 form (Group Name,
      Office dropdown, Currency=KES, Meeting Day, Meeting Time). Config from the picker is bound
      into the title + chip (VSLA → "Village Savings & Loan", ASCA → "Accumulating Savings").
      PRIMARY on_click PROVEN REAL: entering a Group Name updates state (label floats, value
      persists — MVI onValueChange effect); tapping "Next" fires real validation logic —
      logcat `(GroupCreateViewModel) step 1 validation failed fields=[officeId, meetingDay,
      meetingTime]` and the three required fields turn red with inline errors ("Select an office.",
      "Select a meeting day.", "Select a meeting time.") (07-after-next2.png). Not a no-op.
      real-behavior. KNOWN ENV LIMITATION (not an app defect, out of source scope): the companion
      mock `/offices` endpoint returns a JSON object `{...}` where the client expects an array
      `[...]` → `getOffices ... SERIALIZATION` error, so the Office dropdown cannot be populated
      and the wizard cannot be advanced past step 1 in this run. Full happy-path completion is
      blocked only by the mock shape, not by app behavior.

  - feature: share-out-preview
    verdict: real-behavior
    verified_at: "2026-08-01T00:33:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on build 1b08d279 (device_md5 == 1b08d279, fresh capture after
      am force-stop). isCycleEnd now backed by server-computed GroupCorpusDto.isCycleEnd
      (@SerialName("isCycleEnd")) — mock corpus extended isCycleEnd:true. ORGANIZER (grace_w) →
      group-dashboard (Umoja g-1001, ACCUMULATING): Share-Out Quick-Action renders ENABLED (dark
      text, not greyed — 06c-group-dashboard.png). on_click fired: logcat "(GroupDashboardViewModel)
      OnShareOut groupId=g-1001 distributionStrategy=PROPORTIONAL_TO_SHARES" → NavHost →
      ShareOutPreviewRoute → real API GET /companion/groups/g-1001/shareout/preview 200 →
      "(ShareOutPreviewViewModel) preview loaded pool=210000.0 payouts=4". Screen renders full
      distribution table: Total Corpus KES 185,000, Total Profit KES 25,000, Total Pool KES 210,000,
      4 member payout rows (Grace 63,000 / John 46,200 / Mary 37,800 / Peter 63,000)
      (07-shareout-preview.png). real-behavior. Minor cosmetic: sharePercent renders "0.3%%"
      (double-% format bug), non-blocking.

  - feature: share-out-execute
    verdict: real-behavior
    verified_at: "2026-08-01T00:36:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on build 1b08d279 (device_md5 == 1b08d279). From share-out-preview,
      "Confirm & Proceed to Execute" on_click fired: logcat "(ShareOutPreviewViewModel) confirm
      accepted" → NavHost → ShareOutExecuteRoute (08-shareout-execute.png, 4 member payouts +
      irreversible-confirm gate). Double-confirm phrase gate works (button DISABLED until exact
      "SHARE OUT" typed — validation "Must type exactly SHARE OUT" shown for a mistyped phrase,
      11-execute-result.png). After typing exact phrase, Execute Share-Out on_click fired real
      mutation: mock POST /companion/groups/g-1001/shareout/execute 200 → logcat
      "(ShareOutExecuteViewModel) share-out executed recordId=so-rec-1 succeeded=4 failed=0" →
      screen shows "Share-Out Complete! KES 210,000 distributed to 4 members successfully" + Done
      (14-execute-fired.png). real-behavior. Minor: execute request carried cycleNumber=0
      (cycleNumber not threaded through the memberPayoutsJson route seam) — non-blocking data nit.

  - feature: loan-apply
    verdict: real-behavior
    verified_at: "2026-08-01T00:37:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on build 1b08d279 (device_md5 == 1b08d279). canApplyLoan now seeded from
      the viewerRole nav-param (LoanListViewModel: canApplyLoan = viewerRole in LOAN_APPLY_ROLES
      {ORGANIZER,CHAIRPERSON,TREASURER}); viewerRole forwarded group-dashboard→loan-list
      (LoanListRoute(groupId, viewerRole)). ORGANIZER: logcat "(GroupDashboardViewModel) OnViewLoans
      groupId=g-1001 viewerRole=ORGANIZER" → loan-list renders WITH the "Apply for Loan" FAB
      (content-desc "Apply for a new group loan", 16-loan-list.png) — previously hidden for everyone.
      FAB on_click fired: NavHost → LoanApplyRoute → loan-apply form renders (Select Member,
      Requested Amount, Loan Duration, Loan Purpose, Submit Application — 17-loan-apply.png).
      real-behavior. Note: groupId nav-param "g-1001" degrades to 0 via toLongOrNull()?:0L
      (pre-existing documented groupId-type drift, unrelated to this gap; mock returns loans
      regardless so list still renders).

  - feature: loan-request
    verdict: real-behavior
    verified_at: "2026-08-01T01:37:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on FRESH build 6b14d74 (device_md5 == 6b14d74, installed -r then
      am force-stop, md5 read back from /data/app/.../base.apk). The upstream personal-loans nav gap
      is FIXED (personalLoansScreen + loanRequestScreen now registered in GroupBankingNavHost; the
      personal-loans FAB wires onNavigateToLoanRequest → navigateToLoanRequest). MEMBER (peter_k) →
      personal-dashboard → My Loans card → personal-loans → tapped "Request Loan" FAB (content-desc
      "Request a new loan") → NavHost → loan-request. Form renders fully (03-loan-request.png):
      "Request a Loan" title, "Your savings balance KES 0" (documented savingsBalance=0.0 nav bridge),
      "Maximum you can borrow (3× your savings)", Loan Amount (KES) input, Loan Purpose dropdown,
      Repayment Duration slider (12 weeks), Submit Application. Primary on_click fired with real
      effect: tapped Loan Purpose dropdown → opened real option list (School Fees / Medical Emergency /
      Business / Farming / Home Improvement / Emergency / Other — 04a-purpose-open.png); selected
      "Business" + typed amount "2000" → form live-recalculated validation, surfacing "Please check all
      fields and try again" because 2000 > max (3×0=0 from the KES-0 savings bridge) — 04b-filled.png.
      Real MVI state wiring (purpose + amount bound to state, validator recomputes on change), not a
      stub. Zero FATAL/serialization. real-behavior. Note: submit correctly blocked by the documented
      KES-0 savings bridge (max=0); a non-zero savings seam would exercise the POST path.

  - feature: loan-repayment-dialog
    verdict: real-behavior
    verified_at: "2026-08-01T00:40:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on build 1b08d279 (device_md5 == 1b08d279). canRecordRepayment now seeded
      from the viewerRole nav-param (LoanDetailViewModel: canRecordRepayment = viewerRole in
      RECORD_REPAYMENT_ROLES {TREASURER,ORGANIZER}); button gated canRecordRepayment && loan.status
      ==ACTIVE (LoanActionButtonsRow). ORGANIZER → loan-detail loanId=9001 (ACTIVE, GET /loans/9001
      200): "Record Repayment" button renders (19-loan-detail-active.png). on_click fired: logcat
      "(LoanDetailViewModel) OnRecordRepayment tapped loanId=9001" → dialog opens (feature=
      loan-repayment-dialog, Amount KES 1000.00 / M-Pesa|Cash / Reference — 20/21-repayment-dialog.png).
      Submit on_click fired REAL mutation: mock POST /loans/9001/transactions?command=repayment 200 →
      logcat "(LoanRepaymentDialogViewModel) make_repayment succeeded loanId=9001 resourceId=1;
      loan-detail cache invalidated" → dialog dismissed (23-repayment-success.png). real-behavior
      (submit produces real API effect). Test-infra note: initial POST hit a mock-DTO shortfall
      (RecordRepaymentResponseDto requires officeId/clientId/loanId) surfaced honestly as an
      on-screen "Could not record repayment" error; mock response corrected, re-fired → success.

  - feature: loan-mark-defaulted-dialog
    verdict: real-behavior
    verified_at: "2026-08-01T00:42:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on build 1b08d279 (device_md5 == 1b08d279). canMarkDefaulted seeded from
      viewerRole nav-param (MARK_DEFAULTED_ROLES {CHAIRPERSON,ORGANIZER}); button gated
      canMarkDefaulted && loan.status==OVERDUE. Mock loan 9002 returns status OVERDUE. ORGANIZER →
      loan-detail loanId=9002 (OVERDUE, GET /loans/9002 200): "Mark Defaulted" button renders AND
      "Record Repayment" is correctly HIDDEN (status!=ACTIVE) — both gates independently correct
      (25-loan-detail-overdue.png). on_click fired: logcat "(LoanDetailViewModel) OnMarkDefaulted
      tapped loanId=9002" → confirm dialog opens (feature=loan-mark-defaulted-dialog, "Member Mary
      Achieng's loan of KES 3,200 will be marked as defaulted" — 26-defaulted-dialog.png). Confirm
      on_click fired REAL mutation: mock POST /loans/9002/transactions?command=writeoff 200 → logcat
      "(LoanMarkDefaultedDialogViewModel) write_off_loan succeeded loanId=9002 resourceId=1; cache
      invalidated" → dialog dismissed (27-defaulted-confirmed.png). real-behavior.

  - feature: meeting-summary
    verdict: real-behavior
    verified_at: "2026-08-01T02:21:00Z"
    evidence: |
      FULL 7-step wizard walked to submit on the current build (device_md5 ==
      6b14d74ca36965a58a22a4043eeabfb8 == built APK, read back from /data/app/.../base.apk AFTER
      pm-clear/force-stop; no rebuild since — no source changed). ORGANIZER path: grace_w login →
      organizer-dashboard → Today's Schedule → group-list → Umoja (g-1001) → group-dashboard →
      Start Meeting → meeting-calendar (Meeting #13 Upcoming) → Start Meeting → meeting-conduct.
      loadMeetingData succeeded (logcat: members=1 openingCorpus=185000 activeLoans=0 hasPrevious=true).
      Walked all 7 steps with REAL reducer state: Step0 Previous-Review (Meeting #12 card), Step1
      Attendance → marked Peter Kamau Present ("1/1 recorded"), Step2 Opening Balance (185,000),
      Step3 Savings → entered Group Savings KES 500 (corpus band recomputed live to 185,500), Step4
      Loans (No Active Loans), Step5 Applications (No Pending), Step6 Closing reconciliation
      (Opening 185,000 + Savings 500 = Closing 185,500). Tapped Submit Meeting → the REAL ordered
      6-priority POST sequence fired all-200 (logcat + mock-access.log): POST /datatables/dt_meeting_record,
      POST /datatables/dt_meeting_attendance (memberId=7004), POST /savingsaccounts/8001/transactions
      (amount=500), PUT /datatables/dt_group_corpus/0 (closingCorpus=185500) →
      "submitMeeting: succeeded meetingId=mt-2 (all 6 priorities posted)" → NavHost destination
      changed to MeetingSummaryRoute. meeting-summary then ran its single-key Store5 read
      (GET /fineract-provider/api/v1/datatables/dt_meeting_record/0?meetingNumber=13 → 200) and
      RENDERED real completed-meeting content (81-meeting-summary.png / 81b-summary-full.png):
      "Meeting #13 Summary", Total Collected KES 200, Corpus KES 185,200, Attendance 1/1, Savings
      Breakdown row (Peter Kamau — Group KES 200 · Individual KES 0), Corpus Reconciliation
      (Opening 185,000 → Closing 185,200), Done + Share affordances. on_click verified: tapped Done →
      logcat "(MeetingSummaryViewModel) NavigateDone tapped meetingNumber=13" → NavHost popped back
      (submit-success overlay "Meeting #13 submitted successfully!"). Zero SERIALIZATION/FATAL. Mock
      extended (scratchpad companion-mock.mjs ONLY, no app/idea-layer source) to serve the
      meeting-conduct base-URL endpoints (/centers/{id}, /datatables/dt_meeting_record/{id},
      /datatables/dt_group_corpus/{id}, /loans, submit POSTs) + the flat MeetingSummaryRecordDto on
      the ?meetingNumber= branch. real-behavior.

  - feature: join-with-code
    verdict: real-behavior
    verified_at: "2026-08-01T02:16:00Z"
    evidence: |
      RE-VERIFIED on the current build (device_md5 == 6b14d74ca36965a58a22a4043eeabfb8 == built APK,
      read back AFTER pm-clear/force-stop; no rebuild since). Zero-group in-app entry now exercised:
      logged in with a *newbie* handle ('newbie_ann') → companion mock LOGIN role=MEMBER
      zeroGroups=true → LoginSignupViewModel.screenStateFor(empty memberships)=ZeroGroups →
      login-signup ZeroGroups empty state rendered "Join with Invite Code" button (63b-zerogroups.png).
      Tapped it → NavigateToJoinWithCode → join-with-code screen ("Join a Group", invite-code field,
      Validate Code — 64-joinscreen.png). Entered code "ABC123"; at 6 chars auto-validate fired
      (logcat "(JoinWithCodeViewModel) invite code reached 6 chars — auto-validating"). Validate
      on_click fired the REAL companion calls in order (mock-access.log): GET
      /companion/datatables/invitations/ABC123 → 200 (validateInviteToken), then GET
      /companion/groups/5001 → 200 (getGroupPreview) → logcat "group preview loaded groupId=5001".
      Screen transitioned Initial→Validating→Preview: GroupPreviewCard rendered real data
      (66-validated.png): "You're invited to join / Umoja Savings Group / Type VSLA / Organiser Grace
      Wanjiku / Members 15 members / role MEMBER / Join Group CTA). Zero SERIALIZATION/FATAL. (Mock
      GET /companion/groups/{id} branched to return GroupPreviewDto for numeric fineract ids only, so
      the g-xxxx group-detail path is untouched.) real-behavior.

  - feature: personal-dashboard
    verdict: real-behavior
    verified_at: "2026-07-31T21:10:00Z"
    evidence: |
      MEMBER-IDENTITY pass on the installed valid build c6cebed (device_md5 == c6cebed). Logged in
      via the mock member handle 'peter_k' (activeRole=MEMBER, MEMBER-only memberships): logcat
      "(LoginSignupViewModel) login success groupCount=2" then
      "GroupBankingNavHost destination changed: ...personaldashboard.PersonalDashboardRoute" —
      routeEvent() correctly chose personal-dashboard (no ORGANIZER role). Renders full content
      (56-personaldash-rendered.png): "Good evening, Grace Wanjiku", group selector tabs
      (Umoja Savings Group / Harambee Welfare Circle), Total Savings KES 24,700 (Group-linked
      KES 18,500 + Individual KES 6,200), Projected Share-Out KES 21,000, Recent Activity
      (+KES 1,500 DEPOSIT 2026-07-30, -KES 800 WITHDRAWAL 2026-07-22). GET /companion/member/dashboard
      200, deserialized (after mock date-shape fix). on_click verified TWICE with real effects:
      (a) Total Savings card tap → logcat "(PersonalDashboardViewModel) savings card tapped
      groupId=g-1001 poolModel=ACCUMULATING" → navigated to SavingsDashboardRoute (savings-dashboard
      loaded 200, 58-savings-from-member.png); (b) group-tab switch to Harambee → logcat
      "getMemberDashboard: GET /companion/member/dashboard (selectedGroupId=g-1002)" real scoped
      refetch (59-grouptab-switch.png). Zero FATAL/serialization. real-behavior (member mode).

  - feature: personal-savings
    verdict: real-behavior
    verified_at: "2026-08-01T00:44:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on build 1b08d279 (device_md5 == 1b08d279). The prior DISCONNECTED-NAV
      gap is FIXED: member-dashboard now carries clientId/groupLinkedSavingsId/individualSavingsId
      (MemberDashboardDto), PersonalDashboardEvent.NavigateToSavings forwards them, and
      personalSavingsScreen IS registered + navigateToPersonalSavings IS wired in GroupBankingNavHost
      (the savings_summary_card no longer drift-bridges to the group savings-dashboard). MEMBER
      (peter_k) → personal-dashboard → tapped Total Savings card: logcat "(PersonalDashboardViewModel)
      savings card tapped groupId=g-1001 clientId=7004 groupLinkedSavingsId=8001 poolModel=ACCUMULATING"
      → NavHost destination = PersonalSavingsRoute/{clientId}/{groupLinkedSavingsId}?individualSavingsId=
      (NOT SavingsDashboardRoute). personal-savings loaded clientId=7004: real API GET
      /self/savingsaccounts/8001/transactions + /8002/transactions → "(SavingsRepository)
      loadMemberSavings succeeded (groupLinked=3 rows, individual=3 rows)". Screen renders "My Savings"
      with Group-linked/Individual tabs, KES 6,200 hero (Acc: 8001), Contribution Progress, and a
      color-coded Transaction History (Deposit +KES 1,500 bal 6,200 / Withdrawal -KES 800 bal 4,700 /
      Deposit +KES 1,500 bal 5,500) — 32-personal-savings.png. real-behavior (reaches the real
      personal-savings screen with real per-member ledger content, not the group drift-bridge).

  - feature: personal-loans
    verdict: real-behavior
    verified_at: "2026-08-01T01:35:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on FRESH build 6b14d74 (device_md5 == 6b14d74). The prior
      DISCONNECTED-NAVIGATION gap is FIXED: personal-dashboard now hosts a loan entry card
      (ui.yaml#components.loan_card, "My Loans" / content-desc "View your loans and request a new
      one"), and GroupBankingNavHost registers personalLoansScreen(...) + wires the dashboard's
      onNavigateToLoans → navigateToPersonalLoans(clientId). MEMBER (peter_k) → personal-dashboard →
      tapped "My Loans" card → NavHost → personal-loans. Screen renders REAL content (02-personal-
      loans.png): status filter chips (All / Active / Closed), a loan row "Group Loan · ACTIVE · KES
      3,200 · Next repayment due 2026-08-20" (content-desc "Group Loan loan, ACTIVE, outstanding KES
      3,200") loaded from real API GET /clients/7004/loans → 200 (clientId 7004 = the signed-in
      member Peter Kamau, forwarded from the dashboard nav-param), and the "Request Loan" FAB
      (content-desc "Request a new loan") is present. Zero FATAL/serialization. real-behavior (loan
      card navigates + personal-loans loads the member's own loans + FAB present, reaching loan-request).

  - feature: settings
    verdict: real-behavior
    verified_at: "2026-08-01T01:37:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on FRESH build 6b14d74 (device_md5 == 6b14d74). The prior "entry path not
      found" gap is FIXED: personal-dashboard's top bar now hosts a profile overflow menu (MoreVert
      IconButton, content-desc "More options", ui.yaml#components.top_bar.overflow_menu). MEMBER
      (peter_k) → personal-dashboard → tapped "More options" → DropdownMenu opened with "Settings" +
      "Sync Status" items (08-overflow-open.png) → tapped "Settings" → NavHost → settings. Screen
      renders full real content (09-settings.png): Language selector (English / Kiswahili / Français /
      हिन्दी), Appearance (Light / Dark / System Default), Security (Biometric Unlock with real
      "Biometric hardware not available on this device" state + Change PIN row), Notifications (Push
      Notifications toggle), About (App Version "1.0.0 (1)"), and Logout. Zero FATAL/serialization.
      real-behavior (overflow menu navigates + settings renders its full preference surface).

  - feature: settings-logout-dialog
    verdict: real-behavior
    verified_at: "2026-08-01T01:37:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on FRESH build 6b14d74 (device_md5 == 6b14d74). Now reachable (settings
      reachable via the dashboard overflow menu — see settings). MEMBER (peter_k) → settings → tapped
      "Logout" → confirmation dialog opened (10-logout-dialog.png): title "Log Out?", body "You will
      be logged out of your account. Any unsynced changes will be lost.", Cancel + Log Out buttons.
      Confirm on_click fired REAL effect: tapped "Log Out" (content-desc "Confirm logout and end
      session") → session cleared + NavHost → login-signup ("Welcome to MifosSave", Sign In / Sign Up
      tabs, Email/Password inputs — 11-after-logout.png). Re-login as peter_k then succeeded
      (LOGIN peter_k → role=MEMBER → personal-dashboard), confirming the session was genuinely cleared.
      Zero FATAL/serialization. real-behavior (confirm produces real session-clear + navigate effect).

  - feature: sync-status
    verdict: real-behavior
    verified_at: "2026-08-01T01:38:00Z"
    evidence: |
      GAP-CLOSED RE-VERIFY on FRESH build 6b14d74 (device_md5 == 6b14d74). The prior "entry path not
      found" gap is FIXED: personal-dashboard's overflow menu now exposes "Sync Status"
      (ui.yaml#components.top_bar.overflow_menu → OnSyncStatusClick → navigateToSyncStatus, registered
      in GroupBankingNavHost). MEMBER (peter_k, fresh login through the login screen this run) →
      personal-dashboard → "More options" → tapped "Sync Status" → NavHost → sync-status. Screen
      renders real content (14-sync-status.png): overall status indicator "All Synced" (content-desc
      "Overall sync status indicator"), "Never synced" last-sync label, "Sync Now" button, and the
      empty state "All caught up! / No pending or failed operations." — the real observe_sync_queue
      output (empty local SyncQueue → SYNCED). "Sync Now" on_click tapped: with an empty queue the
      status correctly stays "All caught up" (honest no-op, nothing to sync — 15-sync-now.png). Zero
      FATAL/serialization. real-behavior (overflow menu navigates + sync-status renders real
      queue-derived state).
