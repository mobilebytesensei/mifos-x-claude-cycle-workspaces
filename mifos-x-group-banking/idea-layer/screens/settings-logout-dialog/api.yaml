sibling: api
schema_version: "4.0"
feature_id: settings-logout-dialog

# Settings logout dialog is a local action — clears session from local storage, no API call.
# POST /authentication/logout does not exist in Fineract; logout is client-side only.
api: []
