# Screens — CommonPurse

> 30 screens defined in `idea-plan.yaml` §screens (line 1168, quality 92%, approved 2026-05-04).
> Per-screen YAML files generated via `/idea sync` or `/idea-extend-api`.
> This directory is a placeholder — populate via:
>   - `/idea sync` — auto-generate skeleton YAMLs from §screens for all features
>   - `/idea-enrich screens/{name}` — enrich an individual screen with full state machine, components, navigation
>
> Until populated, the canonical screens definition lives in `../idea-plan.yaml` §screens.

## Source

| Section | File:Line |
|---------|-----------|
| §screens (canonical) | `../idea-plan.yaml:1168` |
| §features (cross-ref) | `../idea-plan.yaml:870` |
| §flows (cross-ref) | `../idea-plan.yaml:1340` |
| FEATURES.md projection | `../FEATURES.md` |
