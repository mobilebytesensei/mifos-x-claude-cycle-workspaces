sibling: tests
schema_version: "4.0"
feature_id: group-create
generated_at: "2026-07-21T16:00:00Z"
generated_by: "/idea-test-generate"

testing:
  coverage_targets:
    ViewModel: "90%+"
    Screen: "80%+"
    Repository: "85%+"

  scenarios:
    # ---- state coverage (ui.yaml#states: content[step1-4], submitting, success, error) ----
    - id: TC-GC-001
      scenario: "On mount, typeConfig nav_arg is stored and Step 1 identity form initialises in Content state"
      type: ViewModel
      priority: P0
      given: "nav_params: typeConfig={group_type:VSLA, pool_model:ACCUMULATING, contribution_model:SHARE_BASED_VARIABLE}"
      when: "GroupCreateScreen enters composition"
      then: "typeConfig stored; groupTypeName='VSLA'; currentStep=1; isSubmitting=false; GroupCreateScreenState=Content"

    - id: TC-GC-002
      scenario: "Submitting state renders review_card with submit_button in loading spinner and fields disabled"
      type: Screen
      priority: P1
      given: "isSubmitting=true; currentStep=4"
      when: "GroupCreateScreen renders"
      then: "submit_button shows loading spinner; review_card visible; back_step_button hidden while submitting"

    - id: TC-GC-003
      scenario: "Error state shows inline error on review step with retry via back/edit"
      type: Screen
      priority: P1
      given: "error=GroupCreateError.Server; currentStep=4"
      when: "GroupCreateScreen renders"
      then: "review_card + submit_button + back_step_button visible; inline error message shown"

    # ---- Step 2 type-adaptive visibility (ui.yaml group_rules_form visible expressions) ----
    - id: TC-GC-004
      scenario: "FIXED_AMOUNT contribution model shows contribution_amount_field, hides share fields"
      type: Screen
      priority: P0
      given: "typeConfig.contribution_model=FIXED_AMOUNT; currentStep=2"
      when: "group_rules_form renders"
      then: "contribution_amount_field visible; share_value_field/share_min_field/share_max_field hidden"

    - id: TC-GC-005
      scenario: "SHARE_BASED_VARIABLE contribution model shows share_value/share_min/share_max, hides contribution field"
      type: Screen
      priority: P0
      given: "typeConfig.contribution_model=SHARE_BASED_VARIABLE; currentStep=2"
      when: "group_rules_form renders"
      then: "share_value_field/share_min_field/share_max_field visible; contribution_amount_field hidden"

    - id: TC-GC-006
      scenario: "ROTATING_PAYOUT pool model additionally shows payout_order_dropdown"
      type: Screen
      priority: P1
      given: "typeConfig.pool_model=ROTATING_PAYOUT; currentStep=2"
      when: "group_rules_form renders"
      then: "payout_order_dropdown visible with options FIXED_ORDER/LOTTERY/AUCTION/NEED_BASED"

    - id: TC-GC-007
      scenario: "socialFundEnabled toggle reveals social_fund_percent_field"
      type: Screen
      priority: P2
      given: "socialFundEnabled=false; currentStep=2"
      when: "OnSocialFundToggle(enabled=true) fires"
      then: "socialFundEnabled=true; social_fund_percent_field becomes visible with default '5'"

    # ---- interaction tests (ui.yaml on_click action_contracts) ----
    - id: TC-GC-008
      scenario: "OnNextStep with incomplete Step 1 writes per-field errors to validationErrors and blocks advance"
      type: ViewModel
      priority: P0
      given: "currentStep=1; groupName empty; officeId=null; meetingTime empty"
      when: "OnNextStep fires"
      then: "validationErrors populated (group_name_field, office_dropdown, meeting_time_picker); currentStep stays 1; no API call"

    - id: TC-GC-009
      scenario: "OnNextStep with valid Step 1 increments currentStep"
      type: ViewModel
      priority: P0
      given: "currentStep=1; groupName set; officeId selected; meetingDay + meetingTime set"
      when: "OnNextStep fires"
      then: "validationErrors empty; currentStep=2"

    - id: TC-GC-010
      scenario: "OnPreviousStep decrements currentStep without validation or data loss"
      type: ViewModel
      priority: P1
      given: "currentStep=3; groupName + rules fields populated"
      when: "OnPreviousStep fires"
      then: "currentStep=2; all field values retained; no validationErrors written"

    - id: TC-GC-011
      scenario: "OnBack emits NavigateBack and discards wizard state"
      type: ViewModel
      priority: P1
      given: "wizard partially filled on any step"
      when: "OnBack fires (top-bar close icon)"
      then: "NavigateBack event emitted; no data persisted"

    # ---- API integration (api.yaml operations get_offices, create_group_orchestrate) ----
    - id: TC-GC-012
      scenario: "on_mount fetches offices via GET /offices and populates officeList (stale-while-revalidate)"
      type: ViewModel
      priority: P0
      given: "network online; offices_cache empty"
      when: "GroupCreateScreen mounts"
      then: "get_offices called; officeList populated; offices_cache upserted"

    - id: TC-GC-013
      scenario: "get_offices 401 redirects to login-signup"
      type: ViewModel
      priority: P1
      given: "session expired on mount"
      when: "get_offices returns 401"
      then: "GroupCreateError type=Auth; navigation to login-signup"

    - id: TC-GC-014
      scenario: "get_offices offline serves cached offices from offices_cache"
      type: ViewModel
      priority: P1
      given: "network offline; offices_cache populated"
      when: "GroupCreateScreen mounts"
      then: "officeList served from offices_cache; wizard renderable offline"

    - id: TC-GC-015
      scenario: "OnSubmit online POSTs to /companion/groups and navigates to group-dashboard"
      type: ViewModel
      priority: P0
      given: "currentStep=4; all required fields valid; network online"
      when: "OnSubmit fires"
      then: "isSubmitting=true; create_group_orchestrate POST called with body+typeConfig; on success groups_cache + group_type_config_cache written; NavigateToGroupDashboard(groupId) emitted"

    - id: TC-GC-016
      scenario: "OnSubmit 400 populates validationErrors per-field"
      type: ViewModel
      priority: P1
      given: "server returns 400 invalid typeConfig"
      when: "OnSubmit fires"
      then: "validationErrors populated; isSubmitting=false; GroupCreateError type=Validation; form stays open"

    - id: TC-GC-017
      scenario: "OnSubmit 409 shows group-name-taken snackbar"
      type: ViewModel
      priority: P1
      given: "group name already exists in office"
      when: "OnSubmit returns 409"
      then: "ShowSnackbar(error_group_name_taken) emitted; isSubmitting=false"

    - id: TC-GC-018
      scenario: "OnSubmit 401 sets Auth error and redirects to login"
      type: ViewModel
      priority: P0
      given: "session expired at submit"
      when: "OnSubmit returns 401"
      then: "GroupCreateError type=Auth; redirect to login"

    - id: TC-GC-019
      scenario: "OnSubmit 500 sets retry-able Server error"
      type: ViewModel
      priority: P1
      given: "companion API 5xx"
      when: "OnSubmit returns 500"
      then: "GroupCreateError type=Server retry=true; isSubmitting=false"

    - id: TC-GC-020
      scenario: "OnSubmit offline enqueues CREATE_GROUP_ORCHESTRATE to sync_queue and shows offline banner"
      type: ViewModel
      priority: P0
      given: "network offline at Step 4"
      when: "OnSubmit fires"
      then: "sync_queue row written (entity_type=CREATE_GROUP_ORCHESTRATE, idempotency name+officeId+userId); offline_notice_banner shown; ShowOfflineSyncDialog available"

    # ---- acceptance criteria ----
    - id: TC-GC-021
      scenario: "Config-driven wizard: Step 2 fields adapt to GroupTypeConfig passed from group-type-picker"
      type: ViewModel
      priority: P0
      given: "typeConfig forwarded as nav_arg"
      when: "wizard advances through steps"
      then: "visible/required Step 2 fields match typeConfig.contribution_model and pool_model rules"

    - id: TC-GC-022
      scenario: "review_card on Step 4 reflects entered identity, rules and members values"
      type: Screen
      priority: P1
      given: "currentStep=4; all steps filled"
      when: "review_card renders"
      then: "identity/rules/members sections show entered values; type-conditional rows shown only when applicable"
