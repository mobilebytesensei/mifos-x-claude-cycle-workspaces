sibling: tests
schema_version: "4.0"
feature_id: loan-list
generated_at: "2026-07-21T16:15:00Z"
generated_by: "/idea-test-generate"

testing:
  coverage_targets:
    ViewModel: "90%+"
    Screen: "80%+"
    Repository: "85%+"

  scenarios:
    # ---- state coverage (ui.yaml#states: loading, content, empty, error) ----
    - id: TC-LL-001
      scenario: "Loading state fetches first page then transitions to Content"
      type: ViewModel
      priority: P0
      given: "groupId=1; network online; loans cache empty"
      when: "LoanListScreen mounts"
      then: "LoanRepository.getGroupLoans(groupId=1, limit=20, offset=0) called; shimmer_list shown; LoanListScreenState transitions Loading -> Content; isLoading=false"

    - id: TC-LL-002
      scenario: "Empty state shown when no loans match the group"
      type: Screen
      priority: P1
      given: "load succeeds with loans.isEmpty()"
      when: "content evaluation runs"
      then: "LoanListScreenState=Empty; empty_state shown; apply_loan_fab still visible when canApplyLoan=true"

    - id: TC-LL-003
      scenario: "Error state renders error_state with retry CTA"
      type: Screen
      priority: P1
      given: "get_group_loans returns network/server error"
      when: "load completes"
      then: "LoanListScreenState=Error; error_state visible with body {{error.message}} and Retry CTA"

    # ---- interaction tests (ui.yaml on_click action_contracts) ----
    - id: TC-LL-004
      scenario: "Filter chips recompute filteredLoans client-side (ALL/ACTIVE/OVERDUE/CLOSED)"
      type: ViewModel
      priority: P0
      given: "loans=[ACTIVE x2, OVERDUE x1, CLOSED x1, PENDING x1]"
      when: "OnFilterChange(LoanStatusFilter.ACTIVE) fires"
      then: "selectedFilter=ACTIVE; filteredLoans contains only the 2 ACTIVE loans; no network call (transform_state)"

    - id: TC-LL-005
      scenario: "OVERDUE filter chip surfaces only overdue loans"
      type: ViewModel
      priority: P1
      given: "loans span all statuses"
      when: "OnFilterChange(LoanStatusFilter.OVERDUE) fires"
      then: "selectedFilter=OVERDUE; filteredLoans = loans where status==OVERDUE; overdue chip selected (errorContainer)"

    - id: TC-LL-006
      scenario: "Tapping a loan_card emits NavigateToLoanDetail with loanId"
      type: Screen
      priority: P0
      given: "Content state; loan_card for loanId=5001 visible"
      when: "OnLoanClick(5001) fires"
      then: "NavigateToLoanDetail(loanId=5001) emitted; navigates to loan-detail"

    - id: TC-LL-007
      scenario: "FAB navigates to loan-apply when canApplyLoan=true"
      type: Screen
      priority: P0
      given: "canApplyLoan=true (chairperson/treasurer); Content state"
      when: "OnApplyLoan fires"
      then: "apply_loan_fab visible; NavigateToLoanApply(groupId=1) emitted"

    - id: TC-LL-008
      scenario: "FAB hidden when canApplyLoan=false"
      type: Screen
      priority: P1
      given: "canApplyLoan=false (member role)"
      when: "LoanListScreen renders"
      then: "apply_loan_fab not visible; OnApplyLoan not triggerable"

    - id: TC-LL-009
      scenario: "OnLoadNextPage appends the next 20-row page"
      type: ViewModel
      priority: P1
      given: "first page of 20 loans loaded; server has more pages"
      when: "OnLoadNextPage fires (scroll to end)"
      then: "get_group_loans called with offset=20; new rows appended to loans list and upserted to loans cache (atomic replacePage)"

    - id: TC-LL-010
      scenario: "OnLoadNextPage halts appending when server returns empty pageItems"
      type: ViewModel
      priority: P2
      given: "all pages already loaded"
      when: "OnLoadNextPage fires"
      then: "empty pageItems returned; no rows appended; further page loads suppressed"

    - id: TC-LL-011
      scenario: "Pull-to-refresh resets to offset 0 and re-fetches (network_first)"
      type: ViewModel
      priority: P1
      given: "Content state; multiple pages loaded"
      when: "OnRefresh fires"
      then: "isRefreshing=true; get_group_loans(offset=0) network_first called; loans reset to first page; loans cache upserted; isRefreshing=false"

    - id: TC-LL-012
      scenario: "Retry from error state re-triggers Store5 stream with fresh=true"
      type: ViewModel
      priority: P1
      given: "LoanListScreenState=Error"
      when: "Retry fires"
      then: "get_group_loans re-called network_first; Error cleared; transitions Loading -> Content on success"

    # ---- API integration (api.yaml get_group_loans errors 401/403/404/500) ----
    - id: TC-LL-013
      scenario: "Auth 401 redirects to login-signup"
      type: ViewModel
      priority: P0
      given: "session expired"
      when: "get_group_loans returns 401"
      then: "LoanListError type=Auth redirect=login; navigation to login-signup"

    - id: TC-LL-014
      scenario: "403 forbidden surfaces insufficient-permissions error"
      type: ViewModel
      priority: P1
      given: "user lacks group access"
      when: "get_group_loans returns 403"
      then: "error_insufficient_permissions shown; LoanListScreenState=Error"

    - id: TC-LL-015
      scenario: "404 group-not-found shows empty state"
      type: ViewModel
      priority: P2
      given: "invalid groupId"
      when: "get_group_loans returns 404"
      then: "show_empty behavior; LoanListScreenState=Empty (error_group_not_found)"

    - id: TC-LL-016
      scenario: "500 server error shows retry"
      type: ViewModel
      priority: P1
      given: "companion 5xx"
      when: "get_group_loans returns 500"
      then: "LoanListError type=Server retry=true; error_state retry CTA shown"

    - id: TC-LL-017
      scenario: "Offline stale-while-revalidate serves cached loans"
      type: ViewModel
      priority: P0
      given: "network offline; loans cache populated (TTL 180s)"
      when: "LoanListScreen mounts"
      then: "cached LoanSummary rows shown via cmp-network-monitor fallback_cache (error_offline_cached); Content reachable offline"

    # ---- acceptance criteria ----
    - id: TC-LL-018
      scenario: "loan_card conditional rows: next-repayment shown for ACTIVE, overdue indicator for isOverdue"
      type: Screen
      priority: P1
      given: "loans include an ACTIVE loan and an isOverdue loan"
      when: "loan_card renders"
      then: "next_repayment_text visible only when status==ACTIVE; overdue_indicator visible only when loan.isOverdue"
