sibling: api
schema_version: "4.0"
feature_id: client-type-selector

# Client-type selector is a local routing screen — no API calls.
# Reads cached auth token + dt_member_role from local storage to determine client type.
api: []
