# Settings — API Contract

> **Source of Truth**: `idea-layer/screens/settings/api.yaml`
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 2.0.0 (contract) | **Contract version**: 2.0.0

## Endpoints (1)

| ID | Method | Endpoint | Auth | Writable | Cache |
|---|---|---|---|---|---|
| `change_pin` | PUT | `/fineract-provider/api/v1/self/user/updatePassword` | BasicAuth | yes | — |

Change PIN is the ONLY network call on this screen. All other preferences (language, theme,
biometric, notifications) are persisted locally via DataStore (`multiplatform-settings`).

## Request / Response Details

### PUT /self/user/updatePassword (Change PIN)

Mapped to the Fineract self-service password update. BasicAuth.

**Body** (`ChangePinRequest`)

| Field | Type | Description |
|---|---|---|
| password | String | New PIN |
| repeatPassword | String | New PIN (confirmation) |

**Response** (`ChangePinResponse`)

| Field | Type | Description |
|---|---|---|
| resourceId | Long | Updated user resource id |

## DTOs

### ChangePinRequest  { password: String, repeatPassword: String }
### ChangePinResponse { resourceId: Long }

### AppLanguage (local preference)
```
values: ENGLISH, SWAHILI, FRENCH, HINDI
locale: ENGLISH→en, SWAHILI→sw, FRENCH→fr, HINDI→hi
```
### AppTheme (local preference) = { LIGHT, DARK, SYSTEM }

## Local Preferences (DataStore — no network)

| Key | Type | Written by |
|---|---|---|
| selectedLanguage | AppLanguage | `OnLanguageSelected` |
| selectedTheme | AppTheme | `OnThemeSelected` |
| isBiometricEnabled | Boolean | `OnBiometricToggled` (only when hardware available) |
| isNotificationsEnabled | Boolean | `OnNotificationsToggled` |

## Error Type Map (change_pin)

| Error | Behaviour |
|---|---|
| `400` | `PinChangeFailed` — invalid current PIN or weak new PIN (inline error) |
| `401` | `SessionExpired` — clear session, navigate to login-signup |
| `500` | Generic error snackbar |

## Full Contracts

See `idea-layer/screens/settings/api.yaml` for complete request/response schemas.
