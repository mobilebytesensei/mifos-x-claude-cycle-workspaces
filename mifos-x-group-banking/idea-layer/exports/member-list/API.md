# Member List — API Contract

> **Source of Truth**: `idea-layer/screens/member-list/api.yaml`
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 1.0.0 | **Contract version**: 2.0.0

## Endpoints (1)

| ID | Method | Endpoint | Auth | Writable | Cache |
|---|---|---|---|---|---|
| `get_group_members` | GET | `/groups/{groupId}/clients` | Bearer | no | 120 s SWR (offline show_cached) |

## Request / Response Details

### GET /groups/{groupId}/clients (list — offset paginated)

**Path Params**

| Param | Type | Required | Source | Description |
|---|---|---|---|---|
| groupId | Long | yes | nav_params | Fineract group id |

**Query Params**

| Param | Type | Required | Default | Description |
|---|---|---|---|---|
| limit | Int | no | `20` | Page size |
| offset | Int | no | `0` | Pagination offset |

**Response** (`object`)

| Field | Type | Description |
|---|---|---|
| totalFilteredRecords | Int | Total members in group (drives `hasMorePages`) |
| pageItems | List<ClientItem> | Page of member rows |

**ClientItem (server shape)**

| Field | Type | Description |
|---|---|---|
| id | Long | Fineract client id |
| displayName | String | Member name |
| status | { id: Int, value: String } | Client status |
| mobileNo | String | Phone |
| imagePresent | Boolean | Whether a photo exists |
| savingsAccountBalance | Double | Savings balance |
| loanStatus | String | ACTIVE / NONE / OVERDUE |

## DTOs

### Member (domain)
```
id: String
fineractClientId: Long
displayName: String
photoUri: String?
role: MemberRole              // CHAIRPERSON | TREASURER | SECRETARY | MEMBER
savingsBalance: Double
loanStatus: LoanStatus        // ACTIVE | NONE | OVERDUE
```

### Enums
```
MemberRole = { CHAIRPERSON, TREASURER, SECRETARY, MEMBER }
LoanStatus = { ACTIVE, NONE, OVERDUE }
```

## Pagination Contract

- Type: `offset`; `page_size = 20`
- Append page to local `members` (SQLDelight) on each fetch
- `hasMorePages` = `totalFilteredRecords > loaded.size`
- Pull-to-refresh: invalidate cache + reload offset=0

## Offline Behaviour

`members` table serves cached rows (120 s stale-while-revalidate) when
`cmp-network-monitor` reports offline; a "Showing cached members" message is surfaced.
Revalidation runs in the background when connectivity returns.

## Error Type Map

| Error | Behaviour |
|---|---|
| `network.offline` | Serve cached members + "Showing cached members" (retryable) |
| `401 Unauthorized` | `MemberListError.Auth` → redirect to login |
| `403 Forbidden` | Error banner (retryable) |
| `404 Group not found` | Error banner |
| `500 Server` | `MemberListError.Server` → error banner + retry |

## Full Contracts

See `idea-layer/screens/member-list/api.yaml` for complete request/response schemas.
