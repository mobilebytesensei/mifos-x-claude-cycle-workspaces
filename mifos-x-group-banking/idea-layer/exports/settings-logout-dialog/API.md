# Settings Logout Dialog — API Contract

> **Source of Truth**: `idea-layer/screens/settings-logout-dialog/api.yaml`
> **Auto-generated by**: `/idea export --spec` — Do not edit manually.
> **Feature version**: 2.0.0 (contract) | **Contract version**: 2.0.0

## Endpoints (0)

None. This dialog performs a **client-side local action** only.

`api.yaml#/api` is intentionally empty (`api: []`). Fineract exposes no logout endpoint
(`POST /authentication/logout` does not exist) — logout clears the session and cached auth tokens
from local storage (`SessionManager` + `multiplatform-settings` DataStore). No network round-trip.

## Local Operations (no network)

| Step | Component | Effect |
|---|---|---|
| 1. Flush queued ops (best-effort, online only) | `SyncQueueRepository` (Store5 / SQLDelight) | drain pending SyncQueueItem rows to Fineract before wipe |
| 2. Clear session | `SessionManager` | delete session state |
| 3. Delete tokens | `multiplatform-settings` DataStore | remove cached auth tokens |
| 4. Navigate | `NavigationManager` | go to `login-signup` |

## DTOs

None. The dialog reads `SyncQueueItem` rows (defined in `sync-status`) only to decide whether to
warn about unsynced data before wiping.

## Error Handling

| Condition | Behaviour |
|---|---|
| Offline | Skip flush; still clear session (data may be lost) |
| Flush error | `LogoutFailed` message ("some data may not be synced"); session still cleared |
| Local clear failure | `LogoutFailed` inline error; `isLoggingOut=false`; dialog stays open |

## Full Contracts

See `idea-layer/screens/settings-logout-dialog/api.yaml` (empty `api: []`) — this is a local action.
