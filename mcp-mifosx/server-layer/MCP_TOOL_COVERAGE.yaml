# MCP_TOOL_COVERAGE.yaml — Gap Report: Fineract API vs MCP Tools
# Cross-references FINERACT_API_CATALOG.yaml with MCP_CAPABILITIES.yaml
# Used by: /mifos-bridge [S] Status, /idea-plan Phase 3.5 (pre-analysis)
# Last updated: 2026-05-01

_meta:
  fineract_total_endpoints: 186
  mcp_total_tools: 75
  overall_coverage_pct: 40
  domains_covered: 11
  domains_uncovered: 12

# ─── Per-Domain Gap Analysis ───

domains:

  clients:
    coverage: 31%
    covered:
      - {endpoint: "GET /search", tool: search_clients}
      - {endpoint: "GET /clients/{id}", tool: get_client}
      - {endpoint: "GET /clients/{id}/accounts", tool: get_client_accts}
      - {endpoint: "POST /clients", tool: create_new_client}
      - {endpoint: "POST /clients/{id}?command=activate", tool: activate_pending_client}
      - {endpoint: "PUT /clients/{id} (mobileNo)", tool: update_mobile}
      - {endpoint: "POST /clients/{id}?command=close", tool: close_client_profile}
      - {endpoint: "GET /clients/{id}/identifiers", tool: get_identifiers}
      - {endpoint: "POST /clients/{id}/identifiers", tool: add_identifier}
      - {endpoint: "GET /clients/{id}/documents", tool: list_documents}
      - {endpoint: "GET /clients/{id}/charges", tool: list_client_charges}
      - {endpoint: "POST /clients/{id}/charges", tool: apply_client_fee}
      - {endpoint: "GET /clients/{id}/transactions", tool: list_client_txns}
      - {endpoint: "GET /client/{id}/addresses", tool: get_addresses}
    gaps:
      tier1_candidates:
        - {endpoint: "PUT /clients/{id}", priority: high, reason: "Update client details (name, DOB, etc.)"}
        - {endpoint: "DELETE /clients/{id}/identifiers/{id}", priority: medium}
        - {endpoint: "POST /clients/{id}/documents", priority: medium, reason: "Upload documents"}
        - {endpoint: "GET /clients/{id}/documents/{id}/attachment", priority: medium, reason: "Download documents"}
        - {endpoint: "POST /client/{id}/addresses", priority: medium}
        - {endpoint: "PUT /client/{id}/addresses/{id}", priority: low}
        - {endpoint: "GET /clients/{id}/images", priority: low}
        - {endpoint: "POST /clients/{id}/images", priority: low}
        - {endpoint: "DELETE /clients/{id}", priority: low}
        - {endpoint: "POST /clients/{id}?command=reject", priority: low}
        - {endpoint: "POST /clients/{id}?command=withdraw", priority: low}
        - {endpoint: "POST /clients/{id}?command=reactivate", priority: low}
        - {endpoint: "GET /clients/{id}?template=true", priority: low}
        - {endpoint: "PUT /clients/{id}/identifiers/{id}", priority: low}
        - {endpoint: "GET /clients/{id}/charges/{id}", priority: low}

  groups:
    coverage: 28%
    covered:
      - {endpoint: "POST /groups", tool: create_lending_group}
      - {endpoint: "GET /groups/{id}", tool: get_group}
      - {endpoint: "GET /groups", tool: list_all_groups}
      - {endpoint: "POST /groups/{id}?command=activate", tool: activate_pending_group}
      - {endpoint: "POST /groups/{id}?command=associateClients", tool: add_member_to_group}
    gaps:
      tier1_candidates:
        - {endpoint: "PUT /groups/{id}", priority: high, reason: "Update group details"}
        - {endpoint: "DELETE /groups/{id}", priority: medium}
        - {endpoint: "POST /groups/{id}?command=disassociateClients", priority: high, reason: "Remove member"}
        - {endpoint: "POST /groups/{id}?command=close", priority: medium}
        - {endpoint: "POST /groups/{id}?command=assignStaff", priority: medium}
        - {endpoint: "POST /groups/{id}?command=unassignStaff", priority: low}
        - {endpoint: "GET /groups/{id}/accounts", priority: high, reason: "Group accounts summary"}
        - {endpoint: "GET /groups/{id}?associations=clientMembers", priority: high, reason: "List members with details"}
        - {endpoint: "POST /groups/{id}?command=transferClients", priority: low}

  centers:
    coverage: 30%
    covered:
      - {endpoint: "GET /centers", tool: list_all_centers}
      - {endpoint: "GET /centers/{id}", tool: get_center}
      - {endpoint: "POST /centers", tool: create_new_center}
    gaps:
      tier1_candidates:
        - {endpoint: "PUT /centers/{id}", priority: high}
        - {endpoint: "DELETE /centers/{id}", priority: low}
        - {endpoint: "POST /centers/{id}?command=activate", priority: high}
        - {endpoint: "POST /centers/{id}?command=close", priority: medium}
        - {endpoint: "GET /centers/{id}/accounts", priority: medium}
        - {endpoint: "POST /centers/{id}?command=associateGroups", priority: medium}
        - {endpoint: "POST /centers/{id}?command=disassociateGroups", priority: low}

  loans:
    coverage: 37%
    covered:
      - {endpoint: "GET /loans/{id}", tool: get_loan}
      - {endpoint: "GET /loans/{id}?associations=repaymentSchedule", tool: get_repayment_sched}
      - {endpoint: "GET /loans/{id}?associations=transactions", tool: get_loan_hist}
      - {endpoint: "POST /loans", tool: create_new_loan}
      - {endpoint: "POST /loans/{id}?command=approve+disburse", tool: approve_disburse_loan}
      - {endpoint: "POST /loans/{id}?command=reject", tool: reject_loan}
      - {endpoint: "POST /loans/{id}/transactions?command=repayment", tool: make_repayment}
      - {endpoint: "POST /loans/{id}/charges", tool: apply_loan_fee}
      - {endpoint: "POST /loans/{id}/transactions?command=waiveInterest", tool: waive_loan_interest}
      - {endpoint: "GET (overdue filter)", tool: get_overdue_loans_for_client}
      - {endpoint: "POST /loans (group)", tool: create_group_loan_app}
      - {endpoint: "POST /loans/{id}?command=undoapproval", tool: undo_approval}
      - {endpoint: "POST /loans/{id}?command=undodisbursal", tool: undo_disbursal}
      - {endpoint: "GET /loans/template", tool: get_loan_app_template}
      - {endpoint: "POST /rescheduleloans", tool: reschedule_loan_app}
    gaps:
      tier1_candidates:
        - {endpoint: "PUT /loans/{id}", priority: high, reason: "Update loan application"}
        - {endpoint: "GET /loans/{id}/charges", priority: high, reason: "List loan charges"}
        - {endpoint: "DELETE /loans/{id}/charges/{id}", priority: medium}
        - {endpoint: "POST /loans/{id}?command=foreclosure", priority: medium}
        - {endpoint: "POST /loans/{id}/transactions?command=writeoff", priority: medium}
        - {endpoint: "POST /loans/{id}?command=approve (separate)", priority: low}
        - {endpoint: "POST /loans/{id}?command=disburse (separate)", priority: low}
        - {endpoint: "GET /loans/{id}/guarantors", priority: low}
        - {endpoint: "POST /loans/{id}/guarantors", priority: low}
        - {endpoint: "GET /loans/{id}/collaterals", priority: low}

  savings:
    coverage: 41%
    covered:
      - {endpoint: "GET /savingsaccounts/{id}", tool: get_savings}
      - {endpoint: "GET /savingsaccounts/{id}?associations=transactions", tool: get_savings_txns}
      - {endpoint: "POST /savingsaccounts", tool: create_savings}
      - {endpoint: "POST /savingsaccounts/{id}?command=approve+activate", tool: approve_activate_savings}
      - {endpoint: "POST /savingsaccounts/{id}?command=close", tool: close_savings}
      - {endpoint: "POST /savingsaccounts/{id}/transactions?command=deposit", tool: deposit}
      - {endpoint: "POST /savingsaccounts/{id}/transactions?command=withdrawal", tool: withdraw}
      - {endpoint: "POST /savingsaccounts/{id}/charges", tool: apply_savings_fee}
      - {endpoint: "POST /savingsaccounts/{id}?command=calculateInterest+postInterest", tool: calc_post_interest}
    gaps:
      tier1_candidates:
        - {endpoint: "PUT /savingsaccounts/{id}", priority: high, reason: "Update savings account"}
        - {endpoint: "GET /savingsaccounts/{id}/charges", priority: high}
        - {endpoint: "POST /savingsaccounts/{id}/transactions?command=holdAmount", priority: medium}
        - {endpoint: "POST /savingsaccounts/{id}/transactions?command=releaseAmount", priority: medium}
        - {endpoint: "GET /savingsaccounts/{id}/transactions/{txnId}", priority: low}
        - {endpoint: "POST /savingsaccounts/{id}?command=approve (separate)", priority: low}
        - {endpoint: "POST /savingsaccounts/{id}?command=activate (separate)", priority: low}

  products:
    coverage: 50%
    covered:
      - {endpoint: "GET /loanproducts", tool: list_available_loan_products}
      - {endpoint: "GET /loanproducts/{id}", tool: get_loan_product_details}
      - {endpoint: "GET /savingsproducts", tool: list_available_savings_products}
      - {endpoint: "GET /savingsproducts/{id}", tool: get_savings_product_details}
    gaps:
      tier1_candidates:
        - {endpoint: "POST /loanproducts", priority: low, reason: "Admin only — create product"}
        - {endpoint: "PUT /loanproducts/{id}", priority: low}
        - {endpoint: "POST /savingsproducts", priority: low}
        - {endpoint: "PUT /savingsproducts/{id}", priority: low}

  charges:
    coverage: 80%
    covered:
      - {endpoint: "GET /charges", tool: list_all_charges}
      - {endpoint: "GET /charges/{id}", tool: get_charge}
      - {endpoint: "POST /charges", tool: create_new_charge}
      - {endpoint: "PUT /charges/{id}", tool: update_existing_charge}
    gaps:
      tier1_candidates:
        - {endpoint: "DELETE /charges/{id}", priority: low}

  accounting:
    coverage: 23%
    covered:
      - {endpoint: "GET /glaccounts", tool: list_accounts}
      - {endpoint: "GET /journalentries", tool: list_journal_entries}
      - {endpoint: "POST /journalentries", tool: record_journal_entry}
    gaps:
      tier1_candidates:
        - {endpoint: "POST /glaccounts", priority: medium}
        - {endpoint: "PUT /glaccounts/{id}", priority: medium}
        - {endpoint: "GET /glaccounts/{id}", priority: medium}
        - {endpoint: "POST /journalentries?command=reverse", priority: medium}
        - {endpoint: "GET /glclosures", priority: low}
        - {endpoint: "POST /glclosures", priority: low}
        - {endpoint: "GET /financialactivityaccounts", priority: low}
        - {endpoint: "GET /accountingrules", priority: low}

  staff:
    coverage: 100%
    covered:
      - {endpoint: "GET /staff", tool: list_all_staff}
      - {endpoint: "GET /staff/{id}", tool: get_staff}
      - {endpoint: "GET /offices", tool: list_all_offices}
      - {endpoint: "GET /offices/{id}", tool: get_office}
    gaps:
      tier1_candidates: []

  reports:
    coverage: 83%
    covered:
      - {endpoint: "GET /reports", tool: list_all_reports}
      - {endpoint: "GET /reports/{id}", tool: get_report_definition}
      - {endpoint: "GET /runreports/{name}", tool: run_fineract_report}
      - {endpoint: "POST /reports", tool: create_report_definition}
      - {endpoint: "PUT /reports/{id}", tool: update_report_definition}
    gaps:
      tier1_candidates:
        - {endpoint: "DELETE /reports/{id}", priority: low}

  codetables:
    coverage: 33%
    covered:
      - {endpoint: "GET /codes", tool: list_system_codes}
      - {endpoint: "GET /codes/{id}/codevalues", tool: get_code_values}
      - {endpoint: "GET /datatables", tool: list_all_datatables}
    gaps:
      tier1_candidates:
        - {endpoint: "POST /codes", priority: low}
        - {endpoint: "PUT /codes/{id}", priority: low}
        - {endpoint: "POST /codes/{id}/codevalues", priority: medium}
        - {endpoint: "PUT /codes/{id}/codevalues/{id}", priority: low}
        - {endpoint: "DELETE /codes/{id}/codevalues/{id}", priority: low}

  datatables:
    coverage: 63%
    covered:
      - {endpoint: "POST /datatables", tool: create_datatable}
      - {endpoint: "GET /datatables/{name}/{entityId}", tool: get_datatable_entries}
      - {endpoint: "POST /datatables/{name}/{entityId}", tool: create_datatable_entry}
      - {endpoint: "PUT /datatables/{name}/{entityId}", tool: update_datatable_entry}
      - {endpoint: "DELETE /datatables/{name}/{entityId}", tool: delete_datatable_entry}
    gaps:
      tier1_candidates:
        - {endpoint: "PUT /datatables/{name}", priority: low, reason: "Update datatable schema"}
        - {endpoint: "DELETE /datatables/{name}", priority: low, reason: "Delete datatable"}
        - {endpoint: "GET /datatables/{name}", priority: medium, reason: "Get datatable schema/columns"}

# ─── Uncovered Domains (0 MCP tools) ───

uncovered_domains:
  - domain: users
    endpoints: 5
    priority: low
    reason: "Admin — list/create/update/delete users, change password"

  - domain: roles
    endpoints: 5
    priority: low
    reason: "Admin — list/create/update/delete roles, enable/disable"

  - domain: permissions
    endpoints: 2
    priority: low
    reason: "Admin — list/update permissions"

  - domain: hooks
    endpoints: 5
    priority: low
    reason: "System — webhook registrations"

  - domain: surveys
    endpoints: 6
    priority: medium
    reason: "Field surveys and scorecards — useful for field officer app"

  - domain: audits
    endpoints: 2
    priority: low
    reason: "System — audit trail"

  - domain: scheduler
    endpoints: 8
    priority: low
    reason: "System — job scheduler"

  - domain: currencies
    endpoints: 2
    priority: medium
    reason: "List/manage currencies — needed for multi-currency apps"

  - domain: funds
    endpoints: 4
    priority: low
    reason: "Fund management"

  - domain: payment_types
    endpoints: 5
    priority: medium
    reason: "Payment type management — needed for transaction categorization"

  - domain: search
    endpoints: 2
    priority: low
    reason: "Global search (partially covered by search_clients)"

  - domain: configuration
    endpoints: 3
    priority: low
    reason: "System configuration"
